# claude.md

## 1. 🧠 SYSTEM OVERVIEW (Cognitive Summary)

这是一个基于 Flask 框架开发的大语言模型（LLM）聚合与性能对比 Web 应用程序。该系统允许用户输入提示词，同时或单独调用不同的 g4f Provider，并实时对比响应内容和响应时间。系统现已集成基于 Firebase 的完整用户认证模块，支持三种访问身份：匿名访客、游客用户和已登录用户。系统采用后端路由结合前端单页异步交互（AJAX/Fetch）的架构，认证子系统以 Flask Blueprint 形式解耦挂载。已登录用户的对话历史持久化功能已于 2026-07-02 全栈落地：数据层（`history` 集合 CRUD）位于 `auth/db.py`，HTTP 路由（`/api/history` 系列 + `/api/compare` 自动保存）位于 `main.py`，前端 ChatGPT/Claude 风格左侧边栏（Recents 时间分组列表 + 悬浮操作图标 + 骨架屏 + 移动端抽屉式导航）位于 `templates/index.html`；游客对话历史仅存于前端内存不落库，侧边栏对游客/未登录用户展示"登录以保存历史"提示而非发起请求。侧边栏交互层已于 2026-07-02 同日进一步升级为乐观更新（Optimistic Updates）模型：pin/rename/delete 操作点击后立即更新 DOM，后台异步请求失败时精确回滚并弹出 Toast 提示；重命名由原生 `prompt()` 弹窗改为原地 `<input>` 编辑（编辑图标或双击标题触发，Enter 提交/Escape 取消）；游客的增删改置顶操作全部改为对 `window.guestHistory` 内存数组的真实读写并驱动同一套渲染逻辑，完整模拟已登录体验且零网络请求。2026-07-03 完成了一轮 g4f 免 Key 文生图（text-to-image）Provider 可用性调研（`availability_g4f/find_image_providers.py` + `test_image_providers.py`），确认存在 5 个可直接接入的免 Key 组合、且证实 Gemini 生图无法免 Key 使用；同日晚些时候该调研**已落地为正式功能**：系统现已支持文生图聚合对比，不再只支持文本对话。落地范围——后端 `main.py` 新增独立的 `IMAGE_PROVIDERS`/`IMAGE_PROVIDER_MODELS_MAP` 配置表（复用调研确认的 5 个免 Key 组合：`PollinationsImage`/`BlackForestLabs_Flux1Dev`/`AnyProvider`/`StabilityAI_SD35Large`/`OperaAria`）、`test_g4f_image_provider()`（走 `g4f.client.Client().images.generate()`，与文本对话的 `g4f.ChatCompletion.create()` 完全独立的调用链路）、`POST /api/generate-images`（与 `compare_providers()` 同构的单阶段并发调度，无互评）、`GET /api/image-providers`；前端 `templates/index.html` 左侧边栏 "+ New Chat" 正下方新增 "🎨 Generate Image" 按钮，点击后切换到独立的图片生成表单区（复用同一套 Provider 勾选卡片 + 单选 Model 下拉交互模式，但绑定独立的 `imageProviderModelsMap`），结果区为固定两列的图片网格（`.image-results-grid`），每张图片自带一个纯客户端 Blob 下载按钮（不经服务器代理，规避对任意 URL 发起服务端请求的 SSRF 风险）。图片生成结果**不写入对话历史**（`history` 集合的 Firestore 文档结构是围绕文本 8-key result DTO 设计的，混入图片 DTO 需要单独的 schema 演进，本次未做），这是当前功能唯一的已知范围限制，详见第 4/6/10 节对应条目。同日（2026-07-03）针对 Recents 侧边栏又完成三项独立修复/新增：(1) 修复了"Recents 只显示第一页 20 条、更多今日对话会把'昨天'等更早记录挤出可见范围且无法再找回"的 Bug——`templates/index.html` 的侧边栏新增 ChatGPT 风格的懒加载分页（滚动到列表底部触发 `loadMoreSidebarHistory()` 抓取下一页并追加，而非一次性抓完或加大 limit），并给 `.sidebar-recents` 加上可见的深色主题滚动条样式；(2) 新增只读的 `templates/history.html` 详情页（`GET /history/<history_id>`，`main.py` 的 `view_history()` + `auth/db.py` 的新函数 `get_chat_history_by_id`）——点击 Recents 条目不再把历史快照加载进 `index.html` 本身可编辑的对比表单（那样残留的 Provider 勾选框/Model 下拉/Compare 按钮容易诱导用户误编辑并重新提交历史 prompt），而是整页导航到这个不含任何表单控件、纯只读展示 prompt + 结果的独立页面；(3) 由于点击 Recents 条目现在会整页导航（不再是纯 SPA），游客的 `window.guestHistory` 改为持续镜像进 `sessionStorage`（`persistGuestHistory()`/`loadGuestHistoryFromStorage()`），使其能在 `index.html` 与 `history.html` 之间跨页面存活，同时仍在关闭标签页时清空，不违反"游客数据不持久化"的身份不变量。同日（2026-07-03）随后又完成一轮纯前端的 UI 比例规范化调整，均未改动任何 Python 代码：`templates/history.html` 顶部说明文案由"挤在一行"的单句改为两句各占一行（`<br>` 分隔，与 `home.html` 既有的两行欢迎语写法一致），`.header p` 同步补上 `line-height:1.5`（`index.html` 同款规则同步补齐，保持两份 `<style>` 一致）；`#promptDisplayContainer`（只读 Prompt 展示区）内唯一的 `.form-group` 新增 `margin-bottom:0` 覆盖，消除了它与容器自身底部 `padding` 叠加造成的"文本下方空白明显比上方宽"的视觉不对称；审计三个认证页面（`login.html`/`register.html`/`profile.html`）发现它们的页面标题 `<h2>` 一直使用内联 `style="color: #333; margin-bottom: 2rem;"`，颜色和字重都未对齐全站统一使用的 `#09110e` + `font-weight:800` + 负字距排版体系，改为 `auth/base.html` 新增的 `.card-title` 复用类。同日（2026-07-03）晚些时候又完成第二轮针对 Recents 侧边栏与整页滚动条的修复，均为纯 CSS/JS 改动、`templates/index.html`/`templates/history.html` 逐字同步、未触碰任何 Python 代码：(1) Recents 黑色列右侧此前同时存在原生（Chrome overlay 模式）滚动条与自绘的 `#sidebarRecentsScrollThumb` 指示器，用户报告表现为"2 个 scrollbar"、且需要把浏览器缩放到 110% 左右才能看见自绘指示器——修复为彻底隐藏 `#sidebarRecents` 的原生滚动条（`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`，在"是否溢出"两种状态下都恒为 0 宽度，而不是之前只调整了外观仍可能被 Chrome 判定为 overlay 模式）、把自绘指示器改为真正可拖拽（新增 `setupThumbDrag()`，鼠标/触摸均可，且必须先做到"原生滚动条完全消失"这一步，否则原生滚动条会一直抢在自绘指示器之前拦截 mousedown，"可拖拽"根本无从谈起）；(2) 把 `.left-sidebar` 的高度计算从 JS 里对 `window.innerHeight` 的手工像素运算+`--page-zoom`除法，改为纯 CSS `height: calc(100vh / var(--page-zoom) - var(--sidebar-header-height) + 2px)`（`syncSidebarStickyMetrics()` 现在只测量并写入 `--sidebar-header-height` 这一个变量，`100vh` 交给浏览器原生解析），消除了"该区域是否真的溢出"这一判定本身对浏览器缩放的敏感性——这同时是"需要 110% 缩放才能看见指示器"的根因（`window.innerHeight` 是 JS 侧取整后的整数，与浏览器缩放叠加 `--page-zoom` 除法后的舍入误差，会让 `#sidebarRecents` 的可用高度在不同缩放级别下算出的数值略有出入），也是下面第 (3) 点的根因；(3) 仿照同一套方案，也给整页接上了一个自绘的可拖拽滚动指示器 `#pageScrollThumb`（新增 `updatePageScrollIndicator()`，复用同一个 `setupThumbDrag()`），原生的整页滚动条同样被彻底隐藏——`html` 上原来的 `overflow-y:scroll` 换成 `scrollbar-width:none`+`::-webkit-scrollbar{display:none}`，仍然保持"该处宽度在两种溢出状态下恒定不变、不引发水平位移"这条最初引入 `overflow-y:scroll` 的不变量，只是从"恒为某个非零值"改成了"恒为 0"；(4) 第 (2) 点的 CSS calc 高度修复同时解决了"150% 等高浏览器缩放下把整页滚动到底部时，Recents 黑色列下方会露出空缺、暴露出下方浅色背景"的 Bug，额外补了 `+2px` 安全余量让任何残余的亚像素误差偏向"侧边栏略高"而非"露出缝隙"。用真实 headless Chromium（Playwright）验证：两处原生滚动条实测占用宽度均为 0（`#sidebarRecents.offsetWidth===clientWidth`、`window.innerWidth===document.documentElement.clientWidth`）；两个自绘指示器无需任何滚动/悬停交互即可见（不再需要凑到某个特定缩放比例）；分别拖拽两个指示器确认真的驱动了 `#sidebarRecents.scrollTop`/`window.scrollY` 变化；模拟更高 `device_scale_factor`（近似高浏览器缩放下的取整误差）并把页面滚动到底部后，`.left-sidebar` 底边与视口底边的差值降到 1px 以内（此前这条路径没有专门测过）；额外复测了"加载长结果再 Clear 不引发水平位移"这条更早的不变量，`document.documentElement.clientWidth`/`.navbar` 宽度在滚动条隐藏方式变更前后保持一致；294 个既有 Python 测试全部保持通过。这一验证没有留下可重复运行的自动化测试文件（纯手动 Playwright 脚本），原因与之前多次同类修复一致：这类问题是"渲染后的滚动/溢出/拖拽行为是否正确"，jsdom 不做真实布局和指针事件模拟，无法覆盖。

## 2. 🧬 ARCHITECTURE MAP (MOST IMPORTANT SECTION)

系统由三个核心子系统构成：Flask 后端服务、Firebase 认证模块、以及 HTML5/JavaScript 前端交互界面。

### 后端服务（Flask，main.py）

- **路由层**：提供页面渲染路由（`/`、`/home`、`/history/<history_id>`，2026-07-03 新增只读历史详情页）、LLM API 接口（`/api/providers`、`/api/compare`、`/api/test-single`）、文生图 API 接口（`/api/image-providers`、`/api/generate-images`，2026-07-03 新增）、认证 API（`/api/auth/guest`），以及对话历史 API（`/api/history` 系列，2026-07-02 新增，仅已登录用户可访问）。
- **多线程并发调度器**：利用 `ThreadPoolExecutor` 并发调用多个 Provider 的请求，防止单点阻塞；文生图路由复用同一套调度骨架（单阶段，无互评）。
- **g4f 适配层**：封装对 `g4f.ChatCompletion`（文本对话）与 `g4f.client.Client().images.generate()`（2026-07-03 新增，文生图）两条完全独立的底层调用链路，分别处理各自的模型匹配逻辑和异常捕获。

### 认证子系统（Flask Blueprint，auth/）

- **蓝图注册**：`auth_bp` 以无前缀方式挂载，路由直接暴露为 `/login`、`/register`、`/logout`、`/profile`。
- **Firebase 适配层**：`auth/db.py` 在模块初始化时尝试连接 Firebase Firestore，并设置 `FIREBASE_AVAILABLE` 布尔标志。若标志为 `False`，所有认证路由返回 503 响应，不崩溃。
- **Session 管理**：用户身份通过 Flask `session` 在请求间传递，密钥从 `SECRET_KEY` 环境变量加载。

### 前端界面（Jinja2 + JS）

- **三态导航栏**：`auth/base.html` 和 `index.html` 均根据 `session.user_id` 与 `session.is_guest` 状态联动切换导航栏展示内容。
- **异步交互控制器**：通过 Fetch API 与后端进行非阻塞通信，动态更新 DOM。

```
[浏览器]
   |
   |-- GET /         --> [index() 身份检查] --> home.html / index.html
   |-- GET /home     --> [home() 清除 is_guest] --> 重定向 /
   |-- POST /login   --> [auth Blueprint] --> Firebase 验证 --> session 写入
   |-- POST /api/auth/guest --> session['is_guest']=True
   |-- POST /api/compare    --> ThreadPoolExecutor --> g4f Provider 适配层
                                                            |
                                                     (外部 LLM APIs)
   |-- POST /api/generate-images --> ThreadPoolExecutor --> g4f.client.Client().images.generate()
                                                            |
                                                     (外部文生图 APIs)
```

### 耦合风险与设计注意事项

- **硬编码映射**：`PROVIDER_MODELS_MAP` 属于硬编码。g4f 库升级后必须手动同步修改。
- **全局状态依赖**：系统依赖 `G4F_AVAILABLE` 和 `FIREBASE_AVAILABLE` 两个全局布尔标志。任一初始化失败都会导致对应功能降级。
- **Flash 消息消费规则**：`home.html` 和 `index.html` 均已加入 Flash 消息显示区。如果某个重定向目标页面不显示 Flash 消息，消息将在 session 中堆积，并在下一个 auth 页面集中出现，产生状态矛盾的假象。新增页面必须同步加入 Flash 消息显示区。

## 3. 🧰 TECHNICAL STACK (EVIDENCE-BASED ONLY)

- **编程语言**：Python, JavaScript
- **后端框架**：Flask
- **并发库**：`concurrent.futures.ThreadPoolExecutor`
- **核心依赖库**：g4f (GPT4Free)、firebase-admin (Firebase Admin SDK)、python-dotenv
- **认证与安全**：Werkzeug (`generate_password_hash` / `check_password_hash`)、Flask `session`
- **数据库**：Google Cloud Firestore（通过 Firebase Admin SDK 访问）
- **前端技术**：HTML5, CSS3 (Linear Gradients, Grid 布局, Flex 布局), Vanilla JavaScript
- **模板引擎**：Jinja2
- **运行环境配置**：通过 `os.environ.get('PORT')` 和 `os.environ.get('SECRET_KEY')` 读取环境变量；本地开发通过 `.env` 文件加载（python-dotenv）
- **部署平台**：Google App Engine (GAE Standard / Flexible compatible)

## 4. 📁 CODEBASE STRUCTURE (WITH INTENT)

```
llm_aggregator/
├── main.py                          # Flask 后端入口（含 auth 蓝图注册、session/secret_key 配置、2026-07-03 新增文生图路由）[MODIFIED]
├── auth/                            # 认证模块 Blueprint 目录 [NEW]
│   ├── __init__.py                  # 定义 auth_bp 蓝图，导入 routes 模块
│   ├── db.py                        # Firebase 初始化、FIREBASE_AVAILABLE 标志、4 个用户 CRUD 函数、6 个对话历史 CRUD 函数（含 2026-07-03 新增的单条只读取 get_chat_history_by_id）[MODIFIED]
│   └── routes.py                    # /login, /register, /logout, /profile 路由实现
├── templates/
│   ├── home.html                    # 欢迎页：未认证且非游客的唯一纯净入口 [NEW]
│   ├── index.html                   # LLM 聚合功能主页（含三态导航栏、Flash 消息区、左侧 Recents 历史侧边栏、懒加载分页、2026-07-03 新增文生图聚合模式）[MODIFIED]
│   ├── history.html                 # [NEW，2026-07-03] 只读历史详情页：基于 index.html 裁剪，仅保留导航栏/侧边栏/prompt 只读展示/结果区，不含 Provider 勾选、Model 下拉、Compare/Clear 表单，见下方专属条目
│   └── auth/                        # 认证模块前端模板目录 [NEW]
│       ├── base.html                # 认证页通用布局（三态导航栏：已登录 / 游客 / 未认证）
│       ├── login.html               # 登录表单（含返回欢迎页、游客体验快捷链接）
│       ├── register.html            # 注册表单（含返回欢迎页、游客体验快捷链接）
│       └── profile.html             # 个人资料页（仅已登录用户可访问，游客被拦截）
├── tests/                           # 自动化测试目录（基于 unittest，不部署）
│   ├── test_main_whitebox.py        # main.py 白盒单元测试（内部函数、模型降级规则）
│   ├── test_main_blackbox.py        # main.py 黑盒集成测试（HTTP 接口驱动）
│   ├── test_main_graybox.py         # main.py 灰盒测试（全局状态、线程池行为）
│   ├── test_auth_whitebox.py        # auth 模块白盒测试（密码哈希、Firestore Mock）
│   └── test_auth_blackbox.py        # auth 模块黑盒测试（路由、Session 读写、访问控制）
├── availability_g4f/                # Provider 可用性探测工具（开发辅助，不部署）[MODIFIED]
│   ├── find_providers_models.py     # 扫描 g4f 所有 working Provider 及其模型列表，输出写入*当前目录*的 available_providers_models.txt（脚本内 `output_file = "available_providers_models.txt"` 不含 `../`，按 §11 指示先 `cd availability_g4f` 再运行，因此真正的最新产物落在 `availability_g4f/available_providers_models.txt`，不是仓库根目录）
│   ├── test_providers.py            # 手动逐一测试候选 Provider 是否真正可用，脚本内 `log_file = "../provider_test_results_v2.txt"`，按 §11 指示的 CWD（`availability_g4f/`）计算，产物应落在仓库根目录的 provider_test_results_v2.txt（该文件截至 2026-07-03 审计时在当前工作树中并不存在，见下方 provider_test_results_v2.txt 条目的说明）
│   ├── find_image_providers.py      # [NEW，2026-07-03] 文生图版本的 find_providers_models.py：遍历 `g4f.Provider` 全部类，筛选出具备非空 `image_models` 或非空 `default_image_model` 属性的 Provider（不预先按 working/needs_auth 过滤，两个标志原样记录在输出里，交由 test_image_providers.py 或人工判断哪些值得实测），输出写入*当前目录*的 available_image_providers_models.txt，路径约定与 find_providers_models.py 完全一致（脚本内 `output_file` 不含 `../`，需先 `cd availability_g4f` 再运行）
│   ├── test_image_providers.py      # [NEW，2026-07-03] 文生图版本的 test_providers.py：对 find_image_providers.py 筛出的候选中 working=True 且 needs_auth=False 的组合，逐一发起真实 `g4f.client.Client().images.generate(prompt=..., model=..., provider=...)` 调用验证是否真的能生成图片（判定标准：返回的 `ImagesResponse.data[0]` 含非空 `url` 或 `b64_json`）。额外补充两条专门用于回答"Gemini 生图 / Google AI Studio 是否免费可用"这一问题的候选：`AnyProvider` + 模型名 `gemini-2.5-flash-image-preview`（验证 g4f 元路由 Provider 内部是否存在免 Key 后端）、`GeminiPro`（g4f 对 Google AI Studio 官方 API 的封装，`login_url` 指向 `https://aistudio.google.com/u/0/apikey`）。每次调用外包一层 `concurrent.futures.ThreadPoolExecutor(max_workers=1).submit(...).result(timeout=45)` 硬超时，防止个别候选命中 Gradio/HuggingFace Space 冷启动或排队导致脚本整体卡死（`test_providers.py` 没有这层保护，因为纯文本请求本身有 `timeout=20` 的 advisory 超时且实测从未挂起；图片生成请求经验证确实存在等待时间明显更长的情况，因此新增这层独立于 g4f 内部 timeout 的硬性保护）。结果写入 `../image_provider_test_results.txt`，路径约定与 test_providers.py 一致（CWD 为 `availability_g4f/`，产物落在仓库根目录）。
│   ├── available_providers_models.txt  # find_providers_models.py 的实际输出位置（gitignore 未锚定路径，仓库根目录下同名文件是历次手动复制的旧副本，两者内容可能不一致，不代表数据损坏）
│   ├── available_image_providers_models.txt  # [NEW，2026-07-03，gitignored] find_image_providers.py 的实际输出位置：静态扫描结果，含 needs_auth=True 的候选（如 `BingCreateImages`、`Together`、`Gemini`(g4f 网页版封装) 等，仅供参考，不代表可免 Key 使用），不代表真实网络可用性
│   ├── available_free_image_providers.txt    # [NEW，2026-07-03，gitignored] 人工汇总的最终结论文件，非任何脚本的直接输出：列出经 test_image_providers.py 实测确认真实可用的 5 个免 Key 文生图 Provider+Model 组合（`PollinationsImage`、`BlackForestLabs_Flux1Dev`、`AnyProvider`+`flux`、`StabilityAI_SD35Large`、`OperaAria`+`aria`）、"Gemini 生图经 `AnyProvider` 与 `GeminiPro` 两条路径实测均不可免费使用"的结论与证据、以及其余失败候选的具体原因分类（图生图模型误当文生图测试、后端 503、需要 `.har` 会话文件、被 WAF 拦截等）。详见本节下方"文生图 Provider 调研结论（2026-07-03）"说明
│   └── provider_test_results.txt    # test_providers.py 更早版本遗留的第一轮测试结果，仅存在于此目录，仓库根目录没有同名文件
├── available_providers_models.txt   # find_providers_models.py 输出的旧副本（非当前脚本的实际写入位置，见上）
├── provider_test_results_v2.txt     # test_providers.py 第三轮测试结果（含 PollinationsAI 验证）本应写入的位置（即按 §11 指示的 `cd availability_g4f && python test_providers.py` 流程运行后应落地之处）——**但截至 2026-07-03 审计，仓库根目录当前并不存在这个文件**（gitignored 的运行时产物，未随代码提交，本地未重新生成或已被清理）；`availability_g4f/provider_test_results_v2.txt` 里现存的同名文件是一份更早、内容不同的运行记录（`PollinationsAI` 在其中显示为失败：`ChatCompletion.create() missing 1 required positional argument`，即 PollinationsAI 加入 `G4F_PROVIDERS` 之前的探索性测试遗留），不应被当作本节其余位置提到的"第三轮、含 PollinationsAI 验证成功"那次结果的替代品
├── image_provider_test_results.txt  # [NEW，2026-07-03，gitignored] test_image_providers.py 的实际输出位置（完整测试日志，含全部 SUCCESS/FAIL/TIMEOUT 明细，本轮结果：5 成功 / 7 失败）
├── firebase-key.json                # Firebase 服务账号密钥（本地开发用，严禁提交 git）[NEW]
├── .env                             # 本地环境变量文件（SECRET_KEY 等，严禁提交 git）[NEW]
├── requirements.txt                 # 依赖锁定版本（新增 firebase-admin、python-dotenv）[MODIFIED]
├── app.yaml                         # GAE 部署配置（新增 SECRET_KEY env_variable）[MODIFIED]
└── env/                             # 本地 Python 虚拟环境（不提交 git）
```

### `main.py` [MODIFIED]

- **role**: 系统的核心后端驱动程序，负责初始化配置、注册认证蓝图、定义 LLM 路由、管理并发请求。
- **key logic**:
  - 启动时调用 `load_dotenv()` 加载 `.env` 文件。
  - `app.secret_key` 从 `SECRET_KEY` 环境变量加载，缺失时 fallback 为 `secrets.token_hex(32)`（每次重启会变，session 将失效）。
  - `from auth import auth_bp` + `app.register_blueprint(auth_bp)` 将认证蓝图以无前缀方式挂载。
  - `index()` 路由：检查 `session.user_id` 或 `session.is_guest`，两者皆无时渲染 `home.html`，否则渲染 `index.html`；渲染 `index.html` 时除既有的 `providers`/`provider_models_json`（文本对话）外，2026-07-03 新增同构的 `image_providers`/`image_provider_models_json`（文生图），分别遍历 `IMAGE_PROVIDERS`/`IMAGE_PROVIDER_MODELS_MAP` 构建，与文本那一份完全独立、互不影响。
  - `home()` 路由（`GET /home`）：清除 `session['is_guest']`，重定向到 `/`，用于"返回欢迎页"操作。
  - `view_history(history_id)` 路由（`GET /history/<history_id>`，2026-07-03 新增）：只读历史详情页，渲染 `history.html`。身份分三支：匿名（无 `user_id` 也无 `is_guest`）直接 `redirect(url_for('index'))`，交回 `index()` 的身份路由处理（最终落到 `home.html`）；已登录（`session['user_id']` 存在）调用 `get_chat_history_by_id(session['user_id'], history_id)`，返回 `None`（不存在/不属于该用户/Firebase 不可用，三种原因不区分，与其余对话历史 CRUD 函数契约一致）时 `flash('History entry not found', 'error')` 后重定向到 `/`，取到数据时把完整字典和 `is_guest_view=False` 一并传入模板，页面加载时无需再发一次请求；游客（只有 `is_guest`）不查询 Firestore（对话历史从不为游客持久化，没有东西可查），直接渲染模板并传 `history_entry=None, is_guest_view=True`，由客户端 JS 从 `sessionStorage` 里按 URL 中的 `history_id` 自行查找渲染。传给模板的 `history_id` 三种身份分支下都是 URL 路径参数原样透传（游客分支靠它才知道要在 `sessionStorage` 里找哪一条）。
  - `guest_login()` 路由（`POST /api/auth/guest`）：设置 `session['is_guest'] = True`，返回 JSON。
  - `_get_authenticated_user_id()`（2026-07-02 新增）：对话历史路由专用守卫辅助函数，返回 `(user_id, None)` 或 `(None, (jsonify错误体, 401))`。未登录（含游客，即只有 `is_guest` 无 `user_id`）一律判定为未认证。4 个对话历史路由（`get_history`、`update_history_title`、`delete_history`、`toggle_history_pin`）均在函数体首行调用它并在拿到非 `None` 的错误响应时立即 `return err_response`。
  - `get_history()`（`GET /api/history`）：`page`/`limit` 查询参数经 `request.args.get(..., type=int)` 解析，非数字值自动回退为默认值（Flask 内置行为）；`page` 夹到 `>= 1`，`limit` 夹到 `[1, 100]`，换算 `offset = (page - 1) * limit` 后调用 `get_chat_history_list`。
  - `update_history_title()`（`PATCH /api/history/<history_id>/title`）：从请求体读取 `new_title` 并 `strip()`，为空返回 400；调用 `update_chat_history_title(session['user_id'], history_id, new_title)`，返回 `False` 时统一按 404 处理（不区分"不属于该用户"和"Firebase 不可用"两种原因，详见 `auth/db.py` 对话历史 CRUD 函数契约的设计说明）。
  - `delete_history()`（`DELETE /api/history/<history_id>`）：调用 `delete_chat_history`，`False` 同样统一按 404 处理。
  - `toggle_history_pin()`（`POST /api/history/<history_id>/toggle-pin`）：调用 `toggle_pin_chat_history`，返回值为 `None` 时视为 404；返回 `True`/`False`（翻转后的新状态）时均视为成功，包装为 `{'is_pinned': <bool>}` 返回 200——**必须用 `is None` 判断失败，不能用 `not new_pinned`，否则翻转到 `False` 的成功结果会被误判为失败**。
  - `compare_providers()` 新增逻辑（2026-07-02）：排序完成后、`jsonify` 之前，若 `session.get('user_id')` 存在，调用 `save_chat_history(session['user_id'], prompt, results)` 并将返回的 `id` 写入响应体的 `history_id` 字段；此调用被独立 `try/except Exception` 包裹（`logger.error` 记录，不重新抛出），持久化失败不影响本次对比结果的正常返回。游客与匿名请求不触发保存，响应体 `history_id` 固定为 `None`。`history_id` 是 `/api/compare` 响应体新增的顶层字段，不属于 8-key result DTO 契约的一部分。
  - `determine_actual_model(provider_name, requested_model)`: 纯函数，封装规则 A/B/C 的模型决策逻辑。
  - `init_result_object(provider_name, model)`: 纯函数，统一初始化标准 Result 字典（7 个 key）。
  - `determine_actual_image_model(provider_name, requested_model)`（2026-07-03 新增）：文生图版的模型决策纯函数，读取独立的 `IMAGE_PROVIDER_MODELS_MAP`，只有规则 A/B（指定模型在列表内则用它，否则退化为列表第一个），**没有规则 C 式的通用兜底**——不存在一个像 `"gpt-3.5-turbo"` 那样对任意文生图 Provider 都适用的默认模型名，Provider 不在映射表里时直接返回 `None`，由调用方（`test_g4f_image_provider`）决定展示成 `'default'`。
  - `init_image_result_object(provider_name, model)`（2026-07-03 新增）：纯函数，统一初始化标准图片 Result 字典（8 个 key：`provider`/`success`/`url`/`b64_json`/`error`/`response_time`/`model`/`type`）。与文本的 7-key 契约的关键差异是用 `url`/`b64_json` 两个字段分别承载 g4f `ImagesResponse.data[0]` 的两种可能返回形式，取代文本 DTO 里单一的 `response` 字符串字段。
  - `detect_and_truncate(text)`: 纯函数，句级 + 滑动窗口重复检测，触发时截断并追加提示语（英文：`... (truncated automatically due to repeated content)`；敏感词命中时返回英文拦截提示 `Content contains sensitive information and has been blocked.`，2026-07-02 起两条提示语均已由中文改为美式英文）。
  - `parse_peer_review_json(text)`: 纯函数，从互评响应文本中提取 JSON，返回 `(score: int, comment: str)`；任何解析失败均容错返回 `(80, raw_text)`；score 被夹入 [1, 100]。
  - `test_g4f_provider()`: 核心 LLM 测试函数，调用上述辅助函数、应用隐形 Prompt 路由（`ROUTE_PROMPTS_MAP`）、将响应经 `detect_and_truncate` 处理后写入 result、统计响应耗时。异常分支（2026-07-03 新增内容策略判定）：优先检查 `CONTENT_POLICY_ERROR_KEYWORDS`（命中时返回 `"This provider's content filter blocked the response. Try rephrasing your prompt."`），未命中再检查 `NETWORK_ERROR_KEYWORDS`（命中时返回原有"系统正忙"英文提示），两者均未命中则原样返回 `str(e)`。**判定顺序不可颠倒**：内容策略类错误文本（如 Azure OpenAI 的 `content management policy`/`content_filter`）有时会与网络类关键词同时出现在同一条错误信息里，必须优先判定内容策略——重试对内容策略拦截无意义，若被误判成"系统正忙"会诱导用户重试一个注定再次被拦截的相同请求。
  - `test_single_provider()`: `/api/test-single` 路由处理函数，直接调用 `test_g4f_provider`，因此隐形 Prompt 路由、重复截断、内容策略友好提示与 `/api/compare` 完全同路径生效。外层 `except Exception` 返回英文友好 500 消息（`Service temporarily unavailable. Please try again later.`，2026-07-02 起由中文改为英文），不暴露原始异常。
  - `test_g4f_image_provider(provider, prompt, requested_model=None)`（2026-07-03 新增）：核心文生图测试函数，与 `test_g4f_provider()` 是两套完全独立的调用链路——走 `G4FImageClient()`（`from g4f.client import Client as G4FImageClient` 的模块级导入，与文本对话的 `g4f.ChatCompletion.create()` 无关）的 `.images.generate(prompt=, provider=, model=, timeout=IMAGE_GENERATION_ADVISORY_TIMEOUT)`。`actual_model == 'auto'`（当前仅 `PollinationsImage` 使用）时故意不传 `model` 关键字参数，让 Provider 自行走它的 `default_image_model`。成功判定：优先取 `response.data[0].url`，为空再取 `.b64_json`，两者都为空则视为失败并置 `error = 'Provider returned no image data.'`。异常分支复用已有的 `NETWORK_ERROR_KEYWORDS` 友好文案判定（不复用 `CONTENT_POLICY_ERROR_KEYWORDS`——文生图 Provider 尚未观察到类似 Azure OpenAI 内容审查的报错模式，保留后续按需补充的空间），未命中则原样返回 `str(e)`。不经过 `ROUTE_PROMPTS_MAP` 隐形路由或 `detect_and_truncate` 重复检测，两者都是针对文本回答设计的，对图片 URL/base64 数据没有意义。`response_time` 同样在 `finally` 块中统一计算。
  - `IMAGE_GENERATION_ADVISORY_TIMEOUT`（`40`）/ `IMAGE_GENERATION_OUTER_TIMEOUT`（`45`）（2026-07-03 新增，模块级常量）：文生图专用的 advisory/硬超时值，明显高于文本路径的 `20`/`21`——`BlackForestLabs_Flux1Dev`/`StabilityAI_SD35Large` 底层是 HuggingFace Space，存在真实的冷启动/排队延迟。两值需同步调整，外层必须始终留有缓冲（当前 5 秒），设计原则与互评阶段的 `25`/`32` 一致，详见第 10 节危险区。
  - `run_peer_review(reviewer_provider, reviewer_model, review_prompt)`: 单次互评请求，内置 429/queue-full 错误单次重试（检测到 `'429'` 或 `'queue'` 关键词时等待 2+random(0,1)s 后重试一次）。内部 `g4f.ChatCompletion.create` 的 advisory `timeout` 已于 2026-07-03 从 `15` 提升到 `25`（原因见下方"互评超时调优"条目）。重试耗尽或非 429/queue 异常直接跳出重试循环后，异常文案判定顺序与 `test_g4f_provider` 一致：先查 `CONTENT_POLICY_ERROR_KEYWORDS`（命中返回 `"This provider's content filter blocked the review. Try rephrasing your prompt."`），未命中再查 `PEER_REVIEW_NETWORK_ERROR_KEYWORDS`（即 `NETWORK_ERROR_KEYWORDS` 追加 `'429'`、`'queue'` 两项，命中触发英文友好消息 `The system is busy and trying to reconnect. Please try again shortly.`，2026-07-02 起由中文"系统正忙"改为英文），两者均未命中则返回 `f'Review failed: {str(last_exc)}'`（原中文"点评失败："前缀同步改为英文）。成功路径内部调用 `parse_peer_review_json` 解析响应，返回 `{reviewer_provider, reviewer_model, score, comment}`，不含 `response_time`。
  - `compare_providers()`: 两阶段并发执行。第一阶段 `ThreadPoolExecutor` 并发测试各 Provider，except 块区分 `TimeoutError`（`logger.warning` 无 traceback）和其他异常（`logger.error` 带 `exc_info`）；`TimeoutError` 分支的 fallback 友好消息同样是上述英文版"系统正忙"文案。第二阶段整体包裹在独立 `try-except` 中：任何崩溃（任务构建失败、Executor 异常等）均被捕获并记 `logger.error`，第一轮结果（含 `peer_reviews: []`）仍可安全返回，接口不会因互评失败而报 500。第二阶段内部每个 `future.result(timeout=32)`（2026-07-03 由 `25` 提升，见下方"互评超时调优"条目）也有独立 except，单条互评的 `TimeoutError` 单独 `logger.warning`，其他异常 `logger.error`。互评任务开始、每条完成（含裁判名、被评 Provider 名、score）、阶段整体完成均有 `logger.info`。最终按成功状态和耗时排序。外层 `except Exception` 的 500 响应返回英文友好消息（同 `test_single_provider`），不暴露原始异常信息给用户。
  - `generate_images()`（`POST /api/generate-images`，2026-07-03 新增）：与 `compare_providers()` 是同一种"并发调度 + 逐个兜底"骨架，但只有**一个阶段**（没有互评第二阶段），调用的是 `test_g4f_image_provider()` 而非 `test_g4f_provider()`。请求体契约与 `/api/compare` 对称：`prompt`（必填）、`providers`（可选，未传时默认测试 `IMAGE_PROVIDERS` 全部 5 个）、`model`（可选全局单选模型）、`max_workers`（同样双重约束 `min(请求值, 5)` 且不超过实际 Provider 数）。`providers` 的匹配范围严格限定在 `IMAGE_PROVIDERS`——传入一个只存在于 `G4F_PROVIDERS`（文本）里的名字（如 `Yqcloud`）不会命中任何图片 Provider，`providers_to_test` 为空时返回 400，两套 Provider 名字空间互不越界。`future.result(timeout=IMAGE_GENERATION_OUTER_TIMEOUT)` 超时或其他异常时，fallback 走 `init_image_result_object` + `determine_actual_image_model` 组装安全兜底（`TimeoutError` 用统一的"系统正忙"友好文案，其他异常用 `f'Execution error: {str(e)}'`）。排序契约与 `compare_providers()` 完全一致（成功优先，耗时短优先）。**不触发任何对话历史持久化**——`save_chat_history` 只在 `compare_providers()` 里调用，`generate_images()` 完全不涉及 `history` 集合，图片生成结果目前不可在 Recents 侧边栏中找回（已知范围限制，见第 6/10 节）。外层 `except Exception` 同样返回英文友好 500 消息，不暴露原始异常。
  - `get_image_providers()`（`GET /api/image-providers`，2026-07-03 新增）：与 `get_providers()` 同构，遍历 `IMAGE_PROVIDERS`/`IMAGE_PROVIDER_MODELS_MAP` 返回 `{name, models, default_model, type: 'g4f_image', status}` 列表，供前端 `image_providers`/`imageProviderModelsMap` 之外的独立轮询/调试场景使用（当前前端主路径走的是 `index()` 渲染时内联注入的 `image_provider_models_json`，不实际请求这个端点，但保留它是为了和文本侧的 `/api/providers` 保持接口对称，也便于未来脱离页面渲染单独消费）。
  - `NETWORK_ERROR_KEYWORDS` / `PEER_REVIEW_NETWORK_ERROR_KEYWORDS` / `CONTENT_POLICY_ERROR_KEYWORDS`（2026-07-03 新增，模块级常量，均在 g4f import try/except 块之外定义，与 `G4F_AVAILABLE` 无关）：前两者驱动"系统正忙"友好文案的关键词匹配（`PEER_REVIEW_NETWORK_ERROR_KEYWORDS = NETWORK_ERROR_KEYWORDS + ['429', 'queue']`，专供 `run_peer_review` 重试耗尽后的兜底判定使用，`test_g4f_provider` 只用不含 429/queue 的基础版本，因为它没有重试逻辑、无需与"是否值得重试"的语义挂钩）；后者驱动内容策略友好文案的关键词匹配（`'content management policy'`、`'content_filter'`、`'content filtering polic'`、`'response was filtered'`、`'responsible ai'`），命中时说明是 Provider 底层供应商（如 Azure OpenAI）自身的内容审查拦截了响应，与网络问题无关、重试无意义，因此 `test_g4f_provider` 和 `run_peer_review` 的异常分支都必须先判定内容策略关键词，再判定网络关键词。
  - `PEER_REVIEW_PROMPTS_MAP`: 互评裁判提示词表，key 为模型名称，value 为要求模型输出 `{"score": int, "comment": str}` JSON 格式的提示词前缀。2026-07-02 起表内 `gpt-4`/`gpt-3.5-turbo`/`aria` 三条提示词已由中文改写为英文（`openai-fast` 原本即为英文，未变动），语义与角色设定不变，仅语言变更。
  - `ROUTE_PROMPTS_MAP`: 隐形 Prompt 路由表，key 为 `(provider_name, model)` 元组，value 为追加到用户 prompt 尾部的风格提示词。**设计约束**：首句必须含"立刻/immediately"urgency 指令（防超时）；其次凸显各模型真实角色个性：gpt-4 扮演"严谨分析师"（结论→依据→反思三层结构，300 字），gpt-3.5-turbo 扮演"高效助手"（TLDR 一句话结论优先，口语化语气，150 字），aria 扮演"实战顾问"（跳过铺垫直接给 1-2 个可操作动作，200 字），openai-fast 扮演"极速速答者"（一句结论+一句理由，英文输出，100 字内）。新增条目须同时满足速度指令和角色鲜明两项要求，字数上限不得超过 300 字。2026-07-02 起 `gpt-4`/`gpt-3.5-turbo`/`aria` 三条提示词已由中文改写为英文（`openai-fast` 原本即为英文，未变动），角色设定、字数上限、urgency 指令均保持不变，仅语言变更；今后新增条目也应统一使用英文撰写。
  - `SENSITIVE_KEYWORDS`: 模块级敏感词列表，当前为空列表占位，可直接填充关键词生效，`detect_and_truncate` 读取此变量。命中后返回的拦截提示语已改为英文，见上方 `detect_and_truncate` 条目。
- **depends_on**: `flask`（含 `flash`，2026-07-03 新增导入，供 `view_history()` 的"未找到"分支使用）, `g4f`, `g4f.client.Client`（`as G4FImageClient`，2026-07-03 新增导入，供文生图路径使用）, `concurrent.futures`, `time`, `json`, `logging`, `re`, `os`, `secrets`, `random`, `dotenv`, `auth.auth_bp`, `auth.db`（`save_chat_history`、`get_chat_history_list`、`delete_chat_history`、`update_chat_history_title`、`toggle_pin_chat_history`，2026-07-02 新增；`get_chat_history_by_id`，2026-07-03 新增）
- **affects**: `home.html`、`index.html`（通过 Jinja2 注入变量，2026-07-03 新增 `image_providers`/`image_provider_models_json`），所有前端 API 请求。

### `auth/__init__.py` [NEW]

- **role**: 定义 `auth_bp = Blueprint('auth', __name__)`，并通过 `from . import routes` 触发路由注册。

### `auth/db.py` [MODIFIED]

- **role**: Firebase 适配层，负责初始化 Firebase Admin SDK 并暴露用户 CRUD 函数和对话历史 CRUD 函数。
- **key logic**:
  - 初始化策略：若项目根目录存在 `firebase-key.json`，优先使用它（本地开发）；否则使用 `ApplicationDefault()`（GAE 环境）。`ApplicationDefault()` 的凭据解析是惰性的，它在 `firestore.client()` 时才真正触发，因此必须优先检查 key 文件，不能依赖其构造函数的异常来做 fallback。
  - `FIREBASE_AVAILABLE`：模块级布尔标志。初始化成功为 `True`，任何异常（包括 `ImportError`）均设为 `False`。
  - 4 个用户 CRUD 函数：`get_user_by_username`、`get_user_by_email`、`create_user`、`get_user_by_id`。这 4 个函数内部不检查 `FIREBASE_AVAILABLE`（无守卫），仅在 `FIREBASE_AVAILABLE` 为 `True` 时被调用，调用方（`auth/routes.py`）负责守卫。
  - 6 个对话历史 CRUD 函数（`history` 集合，2026-07-02 新增 5 个，2026-07-03 追加 `get_chat_history_by_id`）：`save_chat_history`、`get_chat_history_list`、`get_chat_history_by_id`、`delete_chat_history`、`update_chat_history_title`、`toggle_pin_chat_history`。与用户 CRUD 函数不同，这 6 个函数**各自在函数体首行内部检查 `if not FIREBASE_AVAILABLE`**，不可用时直接返回安全的 fallback 值（`None`/`False`/`[]`，具体见下方"对话历史 CRUD 函数契约"）并 `logger.warning`，不依赖调用方守卫。这是有意的双重防护设计，因为这批函数未来可能被 `main.py`（LLM 路由）而非仅 `auth/routes.py` 调用，不能假设所有调用方都会先检查标志位。
  - `save_chat_history`、`get_chat_history_by_id`、`delete_chat_history`、`update_chat_history_title`、`toggle_pin_chat_history` 五者均以 `user_id` 为参数，且后四者（含 2026-07-03 新增的 `get_chat_history_by_id`，即便它只是读取也不例外）在执行操作前先 `.get()` 读取文档并校验 `doc.to_dict().get('user_id') == user_id`，不匹配或文档不存在时拒绝操作并返回 fallback 值，防止用户 A 通过猜测/伪造 `history_id` 删除、修改或**查看**用户 B 的历史记录。**路由层（`main.py`，2026-07-02 落地）未重复做归属校验（数据层已保证），但每个路由都先校验 `session['user_id']` 存在（即用户已登录，非游客）——两层校验合起来才是完整防线：路由层挡住未登录/游客，数据层挡住越权操作他人记录。**
  - `get_chat_history_by_id(user_id, history_id)`（2026-07-03 新增，供只读 `/history/<id>` 详情页使用）：单文档读取，契约与 `delete_chat_history`/`update_chat_history_title`/`toggle_pin_chat_history` 完全一致——同样先内部检查 `FIREBASE_AVAILABLE`（不可用返回 `None`），同样在 `.get()` 读取文档后校验 `doc.to_dict().get('user_id') == user_id`（不匹配或文档不存在同样返回 `None`，三种失败原因不区分），成功时返回含 `id` 键的完整字典。与其余四个写操作函数的区别只是它不执行任何 `.update()`/`.delete()`/`.add()`，纯读取、不修改任何 Firestore 状态。
  - **`pinned_at` 字段与 Pin 排序修复（2026-07-02 用户报告，仿照 Gemini 网页版逻辑）**：`toggle_pin_chat_history` 翻转 `is_pinned` 时，同步写入/清除 `pinned_at` —— 置顶（`new_pinned=True`）时设为 `firestore.SERVER_TIMESTAMP`，取消置顶（`new_pinned=False`）时用 `firestore.DELETE_FIELD` 整体删除该字段（不是设为 `None`，删除是为了让下次重新置顶时拿到全新时间戳，而不是残留一个陈旧值）。`pinned_at` 记录的是"这次置顶操作发生的时刻"，与 `created_at`（对话创建时刻）语义完全不同：`get_chat_history_list` 的 `sort_key` 已改为在置顶分组内按 `pinned_at` **升序**（最早被置顶的排最前）排序，而非像之前那样仍沿用 `created_at` 降序——旧逻辑会导致"先置顶的对话如果创建时间较早，会被后置顶但创建时间较新的对话顶到上面"，即用户报告的"第二个 pin 的顶替了第一个 pin 的位置"这一 Bug；修复后第一个被置顶的对话会稳定停留在置顶区最上方，后续每次新置顶的对话依次排在它下面。未置顶分组的排序（`created_at` 降序）不受影响。`sort_key` 返回 `(0, pinned_at_timestamp)` 给置顶项、`(1, -created_at_timestamp)` 给未置顶项，元组第一位保证置顶分组整体排在未置顶分组之前，第二位再分别按各自规则细排。`save_chat_history` 新建文档时不写入 `pinned_at`（缺省即未置顶），`get_chat_history_list` 读到缺失该字段的置顶项（理论上不该出现，但作为防御性兜底）会退化为时间戳 `0`，排最前而不会抛异常。
- **depends_on**: `firebase_admin`、`werkzeug.security`

### `auth/routes.py` [NEW]

- **role**: 实现 `/login`、`/register`、`/logout`、`/profile` 四条路由，管理 Session 状态转换。
- **key logic**:
  - 每条路由顶层都有 `try/except Exception` 兜底，错误通过 Flash 消息反馈，不返回裸异常。
  - 登录成功后写入 `session['user_id']`、`session['username']`，并清除 `session['is_guest']`。
  - 注册成功后写入 `session['user_id']`、`session['username']`，并清除 `session['is_guest']`。
  - 退出登录后清除 `session['user_id']`、`session['username']`、`session['is_guest']`，重定向到 `url_for('index')`（即根路由 `/`，根路由检测到无身份状态后渲染 `home.html`）。
  - `/profile` 路由先检查 `session['user_id']`，不存在时立即重定向到 `/login`，游客无法绕过访问。
- **depends_on**: `auth.db`、`werkzeug.security`、`flask.session`

### `templates/home.html` [NEW]

- **role**: 系统的第一入口，仅对"未认证且非游客"的用户展示。
- **key logic**:
  - 提供三个操作入口：Login（跳转 `/login`）、Sign Up（跳转 `/register`）、Continue as Guest（Fetch POST `/api/auth/guest`，成功后重定向 `/`）。
  - 页面内含 Flash 消息显示区，确保退出登录等操作的提示消息能在此页面被立即消费，不向下一个页面泄漏。Flash 消息在渲染后 3 秒自动淡出消失（opacity 渐变 0.4s，淡出后从 DOM 移除并清理父容器）。

### `templates/index.html` [MODIFIED]

- **role**: LLM 聚合功能主页，已登录用户和游客均可访问。
- **key logic**:
  - **两栏应用布局（`.app-layout`，2026-07-02 新增）**：`<nav>` 与可选的 `.guest-banner` 之后，页面主体由 `.app-layout`（`display:flex`）包裹，左侧 `<aside class="left-sidebar">`（固定 260px、深色 `#171717` 背景、ChatGPT/Claude 风格）+ 右侧 `.main-content`（`flex:1`，内部仍是原有的 `.container`，表单/结果/互评面板等既有功能未改动）。`.left-sidebar` 与 `.main-content` 是 `.app-layout` 的两个 flex 子项，`align-items:stretch` 让两者互相匹配高度——但这**只保证两者彼此相等**，不保证达到视口高度：结果区为空、页面本身内容矮于视口时，两者会一起矮下去，侧边栏的深色背景就会在视口底部之前截断，露出下方的浅色页面背景（2026-07-02 用户报告的 Bug，已修复）。修复方案是让 `body { display:flex; flex-direction:column; min-height:calc(100vh / var(--page-zoom)); }`，再给 `.app-layout { flex:1; }`，使其吃掉 body 除 `<nav>`/`.guest-banner` 之外的全部剩余高度，即便结果区为空也有一个视口高度的"地板"可以撑满；提交 Prompt 后结果区变高，`.app-layout` 的高度会自然随内容超过这个地板值继续增长，无需任何 JS 干预。**`--page-zoom`（`:root` 上定义，当前值 `0.88`——2026-07-03 当天已经历三次调整：先由曾经的 `0.8` 统一调整为 `1.1`，同日晚些时候又被用户要求改回 `0.8`，再晚些时候又被要求从 `0.8` 改为 `0.88`（即全屏 88% 缩放），详见本节末尾"全局缩放比例调整为 110%"、"全局缩放比例改回 80%"与"全局缩放比例从 80% 调整为 88%"三条目——对应 `body { zoom: var(--page-zoom); }` 这个开发者自定义的全屏缩放效果）是这里必须用 `calc(100vh / var(--page-zoom))` 而非直接 `100vh` 的原因**：`vh` 单位始终按真实浏览器视口计算，不会随 `zoom` 一起放大，一个 `min-height:100vh` 的盒子套上 `zoom:var(--page-zoom)` 后，实际渲染高度会变成 `100vh × zoom`——当 `zoom < 1`（当前的 `0.88`）时渲染高度小于真实视口，视口底部会露出空白；当 `zoom > 1`（如曾经的 `1.1`）时则相反，渲染高度大于真实视口，产生本不需要的额外可滚动空间，两种情况都和 flex 本身是否生效无关。预先把 `min-height` 按 `1/zoom` 的比例反向缩放，缩放渲染后就会精确还原成完整的 `100vh`，这一公式对任意 `--page-zoom` 数值都成立，不需要因为改了 zoom 数值而调整公式本身。若以后调整 `zoom` 的数值，必须只改 `--page-zoom` 这一处，`zoom` 声明和 `min-height` 的 `calc` 会自动保持同步，不能各自硬编码。
  - **侧边栏结构**：`.sidebar-top`（"+ New Chat" 按钮，点击时触发 `clearBtn` 的既有清空逻辑并在移动端顺带关闭抽屉；2026-07-03 新增 "🎨 Generate Image" 按钮 `#generateImageBtn` 平级排在其正下方，点击触发 `switchToImageMode()`）→ `.sidebar-section-label`（"Recents" 静态标题）→ `#sidebarRecents`（动态区域，初始渲染 `#sidebarSkeleton` 骨架屏占位，JS 加载完成后整体替换为分组列表 / 空态提示）。
  - **文生图模式（"Generate Image"，2026-07-03 新增）**：`.main-content` 内部现在是两个互斥显隐的顶层容器——`#compareModeContainer`（原有的文本对比表单 + 结果区，未改动任何既有逻辑，只是被包了一层 `<div>`）和 `#imageModeContainer`（新增，默认 `display:none`）。`switchToImageMode()`/`switchToCompareMode()`（页面脚本靠前位置，紧跟 `openSidebar`/`closeSidebar` 之后）是仅有的两个显隐切换入口：前者隐藏 `compareModeContainer`、显示 `imageModeContainer` 并顺带 `closeSidebar()`（移动端点击后自动收起抽屉）；后者反向操作。`#generateImageBtn` 绑定前者；`#newChatBtn` 的既有 click 监听器新增了对后者的调用（排在 `clearBtn.click()` 之前），因此从图片模式点击 "+ New Chat" 会先切回文本对比模式再清空表单，不会停留在图片模式里。`#imageModeContainer` 内部结构与 `#compareModeContainer` 故意保持同构（`.header` → `.form-container > form` → `.loading` → `.results`），复用几乎全部既有 CSS class（`.form-group`、`.btn`、`.provider-selection`、`.custom-select-wrapper`、`.results-header`、`.stats`、`.stat` 等），只有结果展示区改为 `.image-results-grid`（详见下一条），读起来是"同一个 App 的另一个模式"而非拼接进来的独立页面。
  - **文生图 Provider/Model 选择（`#imageForm`，2026-07-03 新增）**：与文本对比表单是完全平行但独立的一套 DOM/JS——Provider 勾选卡片用 `.image-provider-checkbox`/`.image-provider-trigger` 而非 `.provider-checkbox`/`.provider-trigger`（**必须是不同的 class 名**，因为文本表单的 `providerTriggers`/`providerCards` 是用 `document.querySelectorAll('.provider-trigger')`/`('.provider-checkbox')` 不加容器限定地全局查询的，若图片模式复用同名 class，会被这两个全局查询意外扫入文本表单的已选 Provider 列表和 Clear 重置逻辑里，即使两个模式从不同时可见；内层的 `.checkbox-box`/`.provider-meta` 视觉元素则可以放心共享，因为没有类似的全局 class 查询）。Model 下拉是独立的 `imageModelSelect`/`imageCustomSelectWrapper`/`imageCustomOptions`/`imageCustomSelectTrigger`，`updateImageModelDropdown()` 逻辑与文本侧的 `updateModelDropdown()` 同构（未勾选任何 Provider 时显示全部图片 Provider 模型的并集，勾选后收窄到已选 Provider 的模型交集），但绑定的是后端注入的独立 `imageProviderModelsMap`（`image_provider_models_json | tojson`），与文本侧的 `providerModelsMap` 完全隔离。为保持实现简单，图片模式的下拉**没有**复刻文本侧"黑色高亮延迟到面板收起动画结束后再切换"的 150ms 延迟技巧（`MODEL_HIGHLIGHT_DELAY_MS`/`syncSelectedOptionHighlight`）——那是专门修复过的文本表单历史 Bug，图片模式的下拉是全新代码、未观察到同类问题，选中态高亮同步切换。
  - **文生图结果展示（`.image-results-grid`，2026-07-03 新增）**：固定两列网格（`grid-template-columns: repeat(2, 1fr)`，`<=700px` 断点收窄为单列），每个格子（`.image-result-card`）对应一个 Provider+Model 组合的生成结果，复用文本结果区的 `.provider-header`/`.provider-name`/`.provider-status`/`.status-badge`/`.response-time`/`.provider-error` 通用样式展示 Provider 名、耗时、成功/失败徽章。成功时用 `<img class="generated-image">` 展示图片（`src` 取 `result.url`，为空时退化为 `data:image/png;base64,${result.b64_json}`）并附一个 "Download PNG" 按钮（`.image-download-btn`，`data-idx` 索引进 `window.__lastImageResults` 数组，避免把可能很长的 `b64_json` 塞进 DOM 属性）；失败时展示 `.provider-error` 友好错误文案（沿用后端 `NETWORK_ERROR_KEYWORDS` 判定出的"系统正忙"文案）。`displayImageResults(data)` 是渲染入口，与文本侧的 `displayResults(data)` 是两个独立函数（字段名不同：`result.response`/`peer_reviews` vs `result.url`/`b64_json`，无法共用一份渲染逻辑）。
  - **图片下载（`downloadImage(source, isBase64, filename)`，2026-07-03 新增）**：**刻意不做服务端代理下载**——图片 URL 来自第三方 g4f 后端（`pollinations.ai`/`*.hf.space`/`operacdn.com`），一个"服务端按客户端传来的任意 URL 发起请求"的接口是教科书式的 SSRF 攻击面（恶意客户端可以把 URL 换成内网/云元数据地址，诱导服务器代为发起请求），因此下载逻辑完全留在浏览器端完成：`url` 类型走 `fetch(source).then(r => r.blob())` 转成 Blob 再用一次性的 `<a download>` 触发保存；`b64_json` 类型直接在本地把 base64 字符串解码成 `Uint8Array` 构造 Blob，全程不发任何网络请求。若 `fetch` 因跨域（第三方图床通常不带 CORS 头）被浏览器拦截，`catch` 分支降级为 `window.open(source, '_blank')`（在新标签页打开图片，用户仍可手动右键"图片另存为"），不会抛出未捕获异常，也不会尝试绕过 CORS。
  - **`.sidebar-skeleton` 骨架屏**：4 条 `.skeleton-line`（末条 `.short` 宽度 60% 制造错落感），通过 CSS `linear-gradient` + `background-position` 动画（`@keyframes skeleton-loading`，1.4s 循环）模拟加载中的呼吸光效；`loadSidebarHistory()` 完成后会用 `recents.innerHTML = ...` 整体覆盖，骨架屏随之消失，不存在骨架屏卡死不消失的情况（无论请求成功、失败还是未登录都会走到覆盖分支）。
  - **Recents 时间分组**：不再展示条目下方的小字时间戳，改为按 `formatGroupLabel(created_at)` 计算出的分组名（`Today`/`Yesterday`/`Previous 7 Days`/`Older`，顺序固定，无数据的分组不渲染空标题）包裹一组 `.history-item`；`groupHistoryByDate(items)` 负责分桶。`created_at` 是后端 `/api/history` 返回的 Firestore Timestamp，经 Flask 默认 JSON encoder 序列化为 ISO 字符串，前端用 `new Date(...)` 解析；解析失败（`isNaN`）时归入 `Older`，不抛异常。
  - **条目行 Hover 交互 + Pin 的持久图标（2026-07-02 仿照 Gemini 网页版重构）**：`.history-item` 内部现在是"标题 + `.history-item-right`"两栏布局，`.history-item-right` 包裹两类图标：(1) `.history-item-actions`（重命名、删除按钮，未置顶时还包含"去 Pin"按钮）——沿用原有 hover-only 行为，默认 `opacity:0` 且 `translateX(4px)` 轻微右偏，`:hover`/`:focus-within` 时平滑淡入并归位；(2) `.pin-indicator`——**只在条目已置顶时渲染**，是一个独立于 `.history-item-actions` 之外、`opacity:1` 恒定可见的按钮（品牌绿 `#06c167`，沿用 `.pin-btn.is-pinned` 配色），固定停在行的最右侧，不需要 hover 才能看到，再次点击它即可取消置顶（`data-action="pin"`，走同一个 `toggleHistoryPin` 处理函数）。`renderHistoryItem(item)` 据此二选一渲染：`isPinned` 为真时只渲染 `.pin-indicator`（不渲染 hover-only 的 Pin 按钮，避免同一条目出现两个可点击的置顶控件）；为假时只在 `.history-item-actions` 内渲染 hover-only 的 Pin 按钮。键盘用户 Tab 到 `.history-item-actions` 内的按钮时 `:focus-within` 同样会显现，不依赖鼠标；`.pin-indicator` 本就恒定可见，不受此规则影响。
  - **Bug 修复：Pin 和 Unpin 没有图标区分（2026-07-02 用户报告，仿照 Gemini 网页版逻辑）**：修复前，Pin 按钮无论置顶与否都待在同一个 hover-only 的 `.history-item-actions` 组里，颜色靠 `.is-pinned` class 区分，但**必须先 hover 才能看到**——也就是说一个已置顶的条目在鼠标移开后，肉眼完全看不出它被置顶过，"图标区分"形同虚设。修复为上一条描述的持久 `.pin-indicator`：已置顶条目的绿色大头针图标脱离 hover 分组、常驻显示在行最右侧，未置顶条目则仍是 hover 才出现的灰色大头针图标，两种状态在不 hover 的情况下也能一眼区分。
  - **`currentHistoryItems`（2026-07-02 步骤四新增）**：侧边栏当前渲染内容的唯一数据源（一个 JS 数组）。已登录时是 `loadSidebarHistory()` 从 `/api/history` 拉取的那一页数据；游客时**直接等于 `window.guestHistory` 的数组引用本身**（不是拷贝），因此对 `currentHistoryItems` 里条目对象的原地修改（`item.is_pinned = ...`、`item.title = ...`）本身就是"保存"到 `window.guestHistory`，无需额外同步步骤。`renderCurrentHistory()` 是唯一的渲染入口：先用 `sortHistoryItems(currentHistoryItems)`（2026-07-02 新增，见下方 Bug 说明）**返回一份新数组**——置顶项整体排在未置顶项之前，置顶组内部按 `pinned_at` **升序**（最早置顶的排最前）、未置顶组内部按 `created_at` 降序，与后端 `get_chat_history_list` 排序契约保持一致——再按 `groupHistoryByDate(...)` 分组后整体覆盖 `#sidebarRecents.innerHTML`；`sortHistoryItems` 绝不原地修改 `currentHistoryItems`/`window.guestHistory` 本身（这两者的顺序必须保持插入顺序，供分页/回滚逻辑使用原始 index）。当 `currentHistoryItems` 为空且 `isLoggedIn === false` 时特殊渲染"Log in to save and view your conversation history."提示（游客一旦有条目就正常渲染列表，不再恒为登录提示）。
  - **Bug 修复：Pin 后条目未移动到 Recents 顶部，且第二个 Pin 会顶替第一个（2026-07-02 用户报告）**：这实际是两层问题的叠加。第一层（初次修复）：`toggleHistoryPin(id)` 原实现只翻转 `item.is_pinned` 再调用 `renderCurrentHistory()`，但渲染函数当时直接对 `currentHistoryItems` 做 `groupHistoryByDate`，同桶内保持数组原有顺序，从不因 pin 状态变化重排——条目图标切换了但位置没变。第二层（本轮修复，用户进一步指出）：即使加上了排序，`sortHistoryItems` 最初仍是按 `is_pinned` 降序 + 全局统一按 `created_at` 降序排的，这意味着"哪个条目排在置顶区最上面"取决于**对话创建时间**而非**置顶操作发生的时间**——先置顶一个较早创建的对话，再置顶一个创建时间更新的对话，后者会因为 `created_at` 更大而排到前者上面，表现为"第二个 Pin 顶替了第一个 Pin 的位置"。修复方式见上方 `pinned_at` 字段说明：`toggleHistoryPin` 置顶时在本地记录 `item.pinned_at = new Date().toISOString()`（客户端时间戳，纯粹用于本次会话内的相对排序；取消置顶时清为 `null`），`sortHistoryItems` 的置顶组改按 `pinned_at` 升序排序，与后端 `auth/db.py` 的 `toggle_pin_chat_history`/`get_chat_history_list` 保持同一套"先置顶的稳定在上、后置顶的排在下面"的语义。
  - **Bug 修复：点击 Pin 后条目快速闪烁两下（2026-07-02 用户报告）**：`toggleHistoryPin` 的乐观更新会立即 `renderCurrentHistory()` 一次（整体替换 `#sidebarRecents.innerHTML`，重建所有 DOM 节点）；修复前，`/api/history/<id>/toggle-pin` 请求成功返回后，`.then()` 回调里只要 `data.is_pinned` 是布尔值就**无条件**再 `renderCurrentHistory()` 一次——而这次网络请求通常在几十毫秒内就返回，且 `data.is_pinned` 几乎总是和刚才乐观设置的值相同，等于短时间内把整个列表的 DOM 完全重建了两遍。每次重建后，鼠标下的新按钮节点要等浏览器重新计算 `:hover` 状态、`.history-item-actions` 的 `opacity 0.15s` 过渡才会重新淡入，两次重建各触发一次这个淡入过程，肉眼看就是"闪烁两下"。修复为只有当 `data.is_pinned !== item.is_pinned`（服务端返回的真实状态与本地乐观值确实不一致，例如另一个标签页并发操作了同一条目）时才触发这次确认性重渲染，绝大多数情况下点击一次 Pin 只会重渲染一次。
  - **游客对话历史前端模拟（`window.guestHistory`，2026-07-02 步骤四新增）**：声明于顶层脚本、挂在 `window` 上（`window.guestHistory = window.guestHistory || []`）。`POST /api/compare` 成功且响应体 `history_id` 为空（即当前是游客）时，前端在 `.unshift()` 一条本地构造的记录（`id: 'guest-' + Date.now() + '-' + 随机后缀`、`title: prompt.length > 15 ? prompt.slice(0,15)+'...' : prompt`（2026-07-02 修复，见下方 Bug 说明）、`created_at: new Date().toISOString()`、`is_pinned: false`）后调用 `loadSidebarHistory()`，模拟"新对话立刻出现在 Recents 顶部"的已登录体验。游客的增删改查全部只操作这一内存数组，**从不触碰任何 `/api/history*` 端点**——`toggleHistoryPin`/`deleteHistoryItem`/`commitRename` 内部均以 `if (!isLoggedIn) return;` 在乐观更新之后短路，跳过网络请求分支（乐观更新本身对游客而言就是最终状态，无需回滚）。关闭标签页或刷新页面后 `window.guestHistory` 丢失，这是有意为之，与"游客数据不持久化"的身份不变量一致。
  - **Bug 修复：短 prompt 的标题被无意义地追加省略号（2026-07-02 用户报告）**：原逻辑无条件 `prompt.slice(0, 15) + '...'`，导致像 `"hi"` 这样明显短于 15 字符的 prompt 也会被显示成 `"hi..."`，暗示内容被截断，实际并未截断。修复为 `prompt.length > 15 ? prompt.slice(0, 15) + '...' : prompt`，只有真正超长时才追加省略号；同一规则的后端实现见 `auth/db.py` 的 `save_chat_history`。
  - **乐观更新（Optimistic Updates，2026-07-02 步骤四新增）**：`toggleHistoryPin(id)`、`deleteHistoryItem(id)`、`commitRename(id, newTitle)` 三者统一遵循"先改 `currentHistoryItems` 里的对象/数组 + 立即 `renderCurrentHistory()`，再异步发请求"的模式——用户点击后 DOM 瞬间更新，不等网络往返。已登录用户请求失败（`!response.ok` 或 `fetch` 抛异常）时，各自把改动的字段/条目**精确回滚**到操作前的值（pin 回滚 `is_pinned`，delete 用 `splice(index, 0, removedItem)` 插回原位置而非追加到末尾，rename 回滚 `item.title`），再 `renderCurrentHistory()` 复原 DOM，并调用 `showHistoryErrorToast(message)` 弹出一条 3 秒后自动消失的红色提示条（`.history-error-toast`，`opacity` 过渡，`z-index:400` 高于侧边栏抽屉的 300）。`toggleHistoryPin` 成功后额外用服务端返回的 `data.is_pinned` 覆盖本地值并重渲染，防止并发场景下客户端乐观值与服务端真实状态不一致。游客分支（`!isLoggedIn`）没有网络请求，因此也没有失败/回滚路径——本地写入即成功。
  - **原地重命名（`startInlineRename`，2026-07-02 步骤四起取代原 `prompt()` 弹窗方案）**：点击 ✏️ 图标或双击 `.history-item-title` 均触发 `startInlineRename(itemEl, id)`，把该行的 `<span class="history-item-title">` 就地替换为 `<input class="history-rename-input">`（浅色配色、品牌绿边框，2026-07-02 由深色配色改为浅色，见下方 Bug 说明）并自动 `focus()` + `select()` 全选原文字。
  - **Bug 修复：重命名输入框文字不可读（2026-07-02 用户报告）**：`.history-rename-input` 最初写成深色主题（`color:#ececec` 浅灰文字 + `background:#262626` 深色底），意图匹配侧边栏深色背景。但全局 `input:focus, textarea:focus` 规则（本文件 CSS 靠后位置）会把 `background-color` 强制改为 `#ffffff`，且该规则的选择器特异性（类型选择器 `input` + 伪类 `:focus` = (0,1,1)）高于仅有单个类选择器的 `.history-rename-input`（(0,1,0)），因此每次 `startInlineRename` 调用 `input.focus()`（几乎总是立即触发，因为聚焦发生在元素刚创建之后）后，背景立刻被抢占为白色，而文字仍是浅灰 `#ececec`，浅灰字几乎糊在白底上不可读。修复为 `.history-rename-input` 直接改用浅色配色（`color:#1a1a1a` 深色文字 + `background:#ffffff` 白底），并新增 `.history-rename-input:focus` 规则显式锁定同样的配色——它的特异性（类选择器 + 伪类 = (0,2,0)）无条件高于全局 `input:focus` 的 (0,1,1)，因此无论声明顺序如何都稳赢，聚焦态和非聚焦态文字颜色保持一致可读。`Enter` 触发 `input.blur()` 间接提交，`blur` 事件本身绑定 `commit`（新标题为空或与原标题相同时视为放弃，直接 `renderCurrentHistory()` 还原，不发请求）；`Escape` 走独立的 `cancel()` 路径，不提交也不触发 `blur` 的 commit（用一个 `settled` 布尔位在两条路径间互斥，防止 Escape 后又被随之而来的 blur 事件重复提交一次）。真正的提交逻辑在 `commitRename(id, newTitle)` 中，遵循与 pin/delete 相同的"本地先改、失败再回滚"乐观更新模式。
  - **事件委托而非内联 `onclick`（2026-07-02 步骤四重构）**：`renderHistoryItem(item)` 生成的按钮不再带 `onclick="...('${id}')"` 内联属性（旧写法在标题含单引号/双引号时有转义风险，且不支持双击标题触发重命名），改为纯 `data-action="pin|rename|delete"` 属性；`#sidebarRecents` 上各绑定一个委托的 `click`（读 `e.target.closest('[data-action]')` 分发到三个处理函数，未命中 `[data-action]` 时改为下方 `openHistoryEntry` 分支，2026-07-03 新增，同日晚些时候由内联加载改为整页导航，见下方专属条目）和 `dblclick`（读 `e.target.closest('.history-item-title')` 触发重命名）监听器，只挂载一次，不随每次 `renderCurrentHistory()` 重新绑定，因为委托监听器挂在容器 `#sidebarRecents` 本身（该节点不会被 `innerHTML` 替换整体移除）而非条目节点上。
  - **`openHistoryEntry(id)`（2026-07-03 新增；同日晚些时候从"内联加载快照"改为"整页导航"，见下方 Bug 说明）**：`#sidebarRecents` 的委托 `click` 监听器里，若点击目标不落在 `[data-action]`（固定/重命名/删除图标）也不落在 `.history-rename-input`（重命名中的输入框，避免点击光标定位被误判为导航）上，则视为点击了条目本身，调用 `openHistoryEntry(itemEl.dataset.id)`——函数体只有一行 `window.location.href = '/history/' + encodeURIComponent(id)`，整页导航到只读的 `templates/history.html` 详情页（见下方专属条目），不再在 `index.html` 自己的结果区里就地渲染。移动端点击后无需再手动 `closeSidebar()`（整页导航本身就会卸载当前页面，抽屉状态无意义）。`.history-item` 的 `cursor` 仍是 `pointer`，提示整行可点击。已登录用户的目标页面会直接从 Firestore 按 id 重新取数据（服务端已做归属校验），游客则完全依赖已持续镜像进 `sessionStorage` 的 `window.guestHistory`（见下方 `persistGuestHistory()` 条目）在目标页面自行按 id 查找，因此这里不需要再额外做任何"点击前先把数据搬运过去"的动作。**已知的与双击重命名的交互**：浏览器在派发 `dblclick` 之前必然先派发两次 `click`，因此双击标题触发重命名之前，`openHistoryEntry` 会先被调用（最多）两次——由于它是纯导航（`location.href` 赋值），额外调用等价于把同一个跳转请求了两次，浏览器会自然合并，不会真的跳转两次也不影响随后紧跟着触发的重命名编辑态，不视为 Bug。
  - **Bug 修复：点击历史条目会把它加载进可编辑的对比表单，存在被误编辑后重新提交产生错误的风险（2026-07-03 用户报告）**：修复前的 `loadHistorySnapshot(id)`（已被上面的 `openHistoryEntry(id)` 取代）把历史 prompt 和结果直接渲染进 `index.html` 自己的 `#prompt`/`#results`，但 Provider 勾选框、Model 下拉、Compare/Clear 按钮这套live 表单原样留在下方——用户看到的是"一条历史 prompt + 一个看起来随时可以再次提交的表单"，容易误以为编辑后点 Compare 是在"修正"这条历史记录，实际上只是拿这段文字发起了一次全新的、与原记录毫无关系的对比请求，且原本只读的历史记录被这套可编辑 UI 包围本身就违背了"历史只读"的预期。修复方式是彻底拆出一个不含任何表单元素的独立页面 `templates/history.html`（新路由 `GET /history/<history_id>`），点击 Recents 条目变成整页导航过去，物理上不可能再误触发提交；原来的 `loadHistorySnapshot` 就地渲染方式已完全移除。
  - **Recents 懒加载分页（2026-07-03 新增，修复"更多今日对话把'昨天'等更早记录挤出可见范围"的 Bug）**：`#sidebarRecents` 新增可见的深色主题细滚动条样式（`scrollbar-width: thin` + `::-webkit-scrollbar*`，模仿 ChatGPT 侧边栏），并绑定 `scroll` 事件监听器——滚动到距底部 48px 内时调用 `loadMoreSidebarHistory()`。`loadSidebarHistory()`（首次加载/每次刷新 Recents 时调用）重置 `currentHistoryPage = 1`、`hasMoreHistory = true` 后只请求 `page=1`；`loadMoreSidebarHistory()` 请求 `currentHistoryPage + 1`，把返回的 `data.history` 用 `.concat()` 追加到 `currentHistoryItems`（不是替换）后重新 `renderCurrentHistory()`，并在渲染前记录、渲染后立即恢复 `#sidebarRecents.scrollTop`（`renderCurrentHistory()` 靠整体覆盖 `innerHTML` 实现，会把 `scrollTop` 归零，不恢复的话每次加载更多都会把用户视觉上"甩回"列表顶部）。`hasMoreHistory` 由返回条目数是否 `>= HISTORY_PAGE_SIZE`（`20`，与后端默认 `limit` 一致）决定，一旦某页返回条目数不足一整页，视为已到底，此后滚动到底不再发请求；`isLoadingMoreHistory` 防止同一次滚动触发多个并发请求。**这就是"Recents 里更早的对话（含'昨天'）随着当天新对话增多而从视野中消失"这一用户报告的根因**：修复前侧边栏只在页面加载时请求过一次 `page=1&limit=20`，且没有任何机制去请求更多页——超过 20 条时，`get_chat_history_list`（后端排序：置顶优先，未置顶按 `created_at` 降序）返回的第一页必然全是最新的对话，更早的对话不是"丢失"而是"从未被再次请求过"，之前也确实没有任何 UI 让用户主动去翻页找回它们。修复本身完全复用既有的 `/api/history?page=&limit=` 分页参数（第 8 节已有的契约未变），不涉及任何 Python 改动，只是前端第一次真正用上了 `page > 1`。有意保持"轻量"：不引入除 `currentHistoryItems` 本身之外的任何客户端缓存层（没有 localStorage/IndexedDB），也不做提前预取，只在用户实际滚动到底部时才发一次请求。游客分支不参与这套分页——`window.guestHistory` 本就是当前会话内的完整数组，`loadSidebarHistory()` 对游客直接把 `hasMoreHistory` 强制设为 `false`，滚动到底不会发出任何请求。
  - **Bug 修复：Recents 分页机制本身从未被真正触发，滚动条也不出现（2026-07-03 用户复测报告，紧接上一条"Recents 懒加载分页"当天的同一功能）**：上一条描述的 `loadMoreSidebarHistory()`/`scroll` 监听器/`HISTORY_PAGE_SIZE` 等 JS 逻辑本身没有错，但用户实测后报告"新增更多记录时旧记录仍然逐渐被吞掉、`#sidebarRecents` 右侧完全不出现 scrollbar、也没有办法往下无限滚动找回它们"——即整套分页机制形同虚设。根因不在 JS，而在 CSS 布局：`.left-sidebar` 一直没有自己的高度，只是 `.app-layout`（`align-items: stretch`）里被动"拉伸"到与 `.main-content` 相同的高度；而 flex 布局对行内 `.app-layout` 的高度计算，是先各自按自身"假设不被拉伸"时的自然内容高度取最大值，再统一拉伸——也就是说，只要 `#sidebarRecents`（`.left-sidebar` 内 `flex:1` 的子元素）自身有足够多的历史条目，它自然内容高度本身就会成为这个最大值，`.left-sidebar` 于是被拉伸到"恰好装得下全部条目"的高度，`#sidebarRecents` 永远不会真正溢出（`scrollHeight === clientHeight`），`overflow-y:auto` 因此永远不生效，绑定在它身上的 `scroll` 事件监听器也永远不会触发——`loadMoreSidebarHistory()` 从一开始就是不可达代码路径。表现出来就是：`loadSidebarHistory()` 每次刷新 Recents（含每次新对话保存后）都只重新请求 `page=1&limit=20`，一旦总条数超过 20，用户既看不到任何"还能往下滚"的视觉提示（没有滚动条），也没有任何方式触发加载更多，超过 20 条的历史（含"昨天"）实质上从此再也够不到——这正是当天第一版修复遗留的暗病，并非新引入的独立 Bug。**修复**：让 `.left-sidebar` 拥有独立于 `.main-content` 的、绑定在视口上的固定高度，使 `#sidebarRecents` 真正可以在这个固定高度里溢出滚动——`.left-sidebar` 新增 `position: sticky; top: var(--sidebar-sticky-top, 0px); height: var(--sidebar-sticky-height, calc(100vh / var(--page-zoom))); align-self: flex-start;`（`align-self: flex-start` 是防御性写法，实际上只要显式指定了 `height` 就已经不会再被 `align-items: stretch` 拉伸），`#sidebarRecents` 新增 `min-height: 0;`（flex 子元素的自动最小高度默认按内容撑开，与 `overflow-y:auto` 天然冲突，虽然现代浏览器对设了非 `visible` 的 `overflow` 的 flex 子元素已经会自动把这个最小高度按 0 处理，这里显式声明是跨浏览器保险写法）。用 `position: sticky` 而非普通静态定位，是为了保留"结果区无论多长，`.left-sidebar` 的深色背景永远铺到视口底部、不被截断"这条已有不变量（见本节"侧边栏空结果区背景截断"条目与第 9 节对应风险）——如果只是简单给 `.left-sidebar` 一个固定 `height` 而不加 `sticky`，一旦 `.main-content` 因为长结果变得比一屏还高，`.left-sidebar` 的深色背景会在恰好一屏之后停止延伸，用户往下滚动到长结果的下半部分时，左侧会露出页面背景色——等于重新引入了那条已修复的截断 Bug，只是触发条件从"结果区为空"变成了"结果区异常长"。`top`/`height` 没有写死成某个 `rem`/`vh` 常量，而是通过新增的 JS 函数 `syncSidebarStickyMetrics()`（页面脚本靠后位置，`sidebarOverlay.addEventListener('click', closeSidebar);` 之后）在页面加载与每次 `window.resize` 时实测 `.navbar` + 可选的 `.guest-banner` 当前真实渲染高度，写入 `--sidebar-sticky-top`/`--sidebar-sticky-height` 两个 CSS 自定义属性——因为这段"顶部占用的高度"会随登录状态（Welcome/Profile/Logout vs Guest Mode 徽章 vs 匿名）、`.guest-banner` 是否存在、以及视口宽度（导航栏换行）动态变化，写死一个近似值容易在某些身份/宽度组合下产生几像素的缝隙或遮挡。**这里同样要绕开 `body` 上非标准的 `zoom: var(--page-zoom)` 缩放陷阱**（与本文件多处已记录的 `calc(100vh/zoom)` 手法同源）：`getBoundingClientRect()` 返回的是 zoom 已经生效后的"物理"像素，而 CSS 的 `top`/`height` 声明的是 zoom 生效前的"逻辑"像素、渲染时还会再被 zoom 乘一次，因此 `syncSidebarStickyMetrics()` 在把实测到的物理像素写回 CSS 变量前，要先除以 `--page-zoom` 的数值（`parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--page-zoom'))`）预先放大回去，否则渲染后的实际留白会比真实导航栏矮，出现遮挡。移动端（`<=520px`）`.left-sidebar` 早已在 `@media` 里整体覆盖为 `position:fixed; top:0; height:100vh;`（浮层抽屉），这个覆盖规则在样式表里出现在后面、字面量优先级天然胜过本次新增的桌面端 `var(...)` 声明，因此 `syncSidebarStickyMetrics()` 特意在 `window.innerWidth <= 520` 时直接跳过（省掉无意义的测量），不会与移动端抽屉冲突。`templates/history.html` 的 `<style>`/`<script>` 是 `index.html` 的逐行复制，因此这三处改动（`.left-sidebar`、`#sidebarRecents`、`syncSidebarStickyMetrics()`）在两个文件中各自独立、逐字对应地各改了一份。纯 CSS + JS 修复，未改动任何 Python 代码。用真实 headless Chromium（Playwright，沿用第 11 节记录的 `apt-get download libnspr4 libnss3 libasound2t64 libasound2-data` + `dpkg -x` 解压至用户目录 + `LD_LIBRARY_PATH` 指向解压出的 `.so` 文件这套免 root 沙箱方案）验证：(1) 游客身份下 `sessionStorage` 注入 40 条记录，`#sidebarRecents` 的 `scrollHeight`（1551px）确实大于 `clientHeight`（912px，此前二者恒等，从不溢出）；(2) 用 `SecureCookieSessionInterface` 签发一份使用运行中服务器同一份 `SECRET_KEY` 的伪造 session cookie 模拟已登录身份（不写入任何真实 Firestore 数据），mock `/api/history` 按 `page` 参数返回分好页的假数据（共 55 条），在 1400×650 视口下验证：初始只请求 `page=1`、渲染 20 条；把 `#sidebarRecents.scrollTop` 设到底部并派发 `scroll` 事件后确实追加请求了 `page=2`（渲染累计到 40 条）；再次滚到底部确实请求了 `page=3`（渲染累计到全部 55 条，最后一条 `"conversation 54"` 出现在 DOM 里）；`page=3` 之后（`newItems.length=15 < limit=20`）`hasMoreHistory` 正确翻转为 `false`，再滚一次不会发出 `page=4` 的多余请求；(3) 复测了"侧边栏背景不截断"这条已有不变量在本次改动后依然成立，额外覆盖了此前从未测过的"结果区异常长"场景（注入 80 段结果文本、`document.documentElement.scrollHeight` 达到视口高度 6 倍以上），在滚动到页面顶部/1/3/1/2/底部等多个位置分别测量 `.left-sidebar` 的 `boundingBox()`，其底边与视口底边的差值始终 `< 3px`；(4) 移动端 400×800 视口下确认 `.left-sidebar` 的 computed `position` 仍是 `fixed`（未被桌面端 `sticky` 规则污染）、汉堡按钮点击后抽屉仍能正常滑入。这一验证方式没有留下可重复运行的自动化测试文件（纯手动脚本），原因见第 11 节对应说明的更新。
  - **Bug 修复：滚动/溢出机制已生效，但滚动条本身在 Chrome 标签页里依然不可见（2026-07-03 用户当天第三次报告，紧接上一条）**：上一条修复让 `#sidebarRecents` 真正产生了内部溢出（`scrollHeight > clientHeight`，鼠标滚轮在该区域内滚动确实生效），但用户反馈"Chrome 标签页里还是看不到滚动条，VS Code 内置预览里却正常"——这条对比线索直接指向根因：**这不是溢出/滚动功能失效，而是 Chrome 的原生"悬浮式"（overlay）滚动条渲染模式**。Overlay 滚动条不占用任何布局空间、仅在用户主动滚动或悬停时短暂淡入约 1 秒后自动淡出，静态截图或"只是在读页面、没有滚动"的标签页里必然看不到；VS Code 内置的 webview 预览默认使用经典（非 overlay、始终保留轨道宽度）滚动条渲染，因此同一份 CSS 在两处观感不同。**关键诊断证据**：`#sidebarRecents::-webkit-scrollbar` 自定义样式（宽度、颜色）此前理论上"应该"能让 Chrome 放弃 overlay 模式改用经典模式，但实测（真实 headless Chromium）发现 `#sidebarRecents.offsetWidth === #sidebarRecents.clientWidth`（差值为 0px）——也就是说滚动条事实上没有占用任何布局宽度，证明自定义 `::-webkit-scrollbar` 样式在当前浏览器版本/平台下**并未**真正把 overlay 模式压制成经典模式（这是较新 Chromium 版本收紧非标准 `::-webkit-scrollbar` 覆盖能力后的已知限制，不是配置错误）。**第一层修复（不完整，但值得保留）**：把 `::-webkit-scrollbar-track` 的 `background` 从完全透明的 `transparent` 改为半透明的 `rgba(255,255,255,0.08)`（轨道本身不能透明——透明轨道正是触发 overlay 淡出模式的条件之一），thumb 透明度从 `0.25` 提到 `0.45`（hover 态 `0.4`→`0.65`），宽度从 `6px` 增到 `10px`，`scrollbar-color`（Firefox + 部分新版 Chrome 支持的标准属性）同步跟进。这层修复在 Firefox 和支持标准滚动条属性的浏览器上仍然有意义、予以保留，但**不能**指望它在所有 Chrome 版本/平台组合下都生效（已用 `offsetWidth`/`clientWidth` 差值实测证伪，见上）。**真正的修复（第二层，不依赖浏览器原生滚动条渲染）**：新增一个完全自定义、JS 驱动的视觉滚动指示器——`.left-sidebar` 内新增 `<div class="sidebar-recents-scroll-indicator" id="sidebarRecentsScrollThumb"></div>`（`#sidebarRecents` 的兄弟节点，不是子节点，因此 `renderCurrentHistory()` 用 `innerHTML` 整体替换 `#sidebarRecents` 内容时不会把它一并清空）。CSS `position: absolute; right: 3px; width: 5px; border-radius: 3px; background: rgba(255,255,255,0.35); pointer-events: none; opacity: 0; transition: opacity 0.2s ease;`，`.visible` class 切换 `opacity` 到 `1`——`.left-sidebar` 本身已经是 `position: sticky`（见上一条修复），天然为这个绝对定位的子元素提供了包含块，不需要额外包一层 `position: relative` 的容器。新增 JS 函数 `updateSidebarScrollIndicator()`：无溢出（`scrollHeight <= clientHeight + 1`）时移除 `.visible`（没有可滚动内容时不显示一条误导性的满高度指示条）；有溢出时，用 `#sidebarRecents`/`#leftSidebar` 的 `getBoundingClientRect()` 差值算出指示条应处的纵向轨道范围（`trackTop`/`trackHeight`），再按 `clientHeight/scrollHeight` 的比例算出 `thumbHeight`（下限 `24px`，避免内容极多时指示条细到看不见），按 `scrollTop/(scrollHeight-clientHeight)` 的比例算出 `thumbTop`，写入 `thumb.style.top`/`thumb.style.height` 并加上 `.visible`。触发调用点：页面首次加载（紧跟 `syncSidebarStickyMetrics()` 之后一次性调用）、`window.resize`（与 `syncSidebarStickyMetrics()` 合并进同一个监听器）、`#sidebarRecents` 自身的 `scroll` 事件（与既有的 `loadMoreSidebarHistory()` 触发逻辑共用同一个监听器，指示条位置随手动滚动实时跟随）、`renderCurrentHistory()` 末尾（覆盖初次渲染、`loadMoreSidebarHistory()` 追加下一页、以及 pin/rename/delete 触发的重排——这些都会改变 `scrollHeight`，需要重新计算指示条尺寸/是否该显示）。**踩坑记录（首次实现时的真实 Bug，修复过程中被验证脚本抓到）**：`updateSidebarScrollIndicator()` 最初直接把 `getBoundingClientRect()` 算出的差值原样赋给 `thumb.style.top`/`style.height`，结果指示条位置偏高约 15px、滚到底部时距离 `#sidebarRecents` 真实底边还差约 160px 才停下——这正是本文件其余多处已记录过的 `zoom` 二次缩放陷阱（`getBoundingClientRect()` 返回的是 `zoom` 已经生效后的"物理"像素，而赋给 `style.top`/`style.height` 的值是"逻辑"像素、渲染时还会再被 `zoom` 乘一次一遍），补上"除以 `--page-zoom` 数值"这一步（与 `syncSidebarStickyMetrics()` 处理 `top`/`height` 的手法完全一致）后，指示条顶部/底部与 `#sidebarRecents` 实际边界的误差降到 1px 以内。`scrollTop`/`scrollHeight`/`clientHeight` 三者本身**不需要**除以 zoom（它们是元素自身局部坐标系下的布局盒尺寸，与 `getBoundingClientRect()` 的视口坐标系不同，两者已经在同一套"物理"缩放下保持一致，用它们相除得到的比例本身是 zoom 无关的纯数字），只有把 `getBoundingClientRect()` 差值转换为要赋给 CSS `top`/`height` 的绝对像素值这一步才需要除以 zoom——这个区分是本次踩坑后确认的细节，`thumbHeight`/`thumbTop` 的计算式因此对"比例部分"（`clientHeight/scrollHeight`、`scrollTop/maxScrollTop`）和"轨道尺寸部分"（`trackTop`、`trackHeight`，来自 `getBoundingClientRect()` 且已除以 zoom）分别处理，不能混在一起简单地对最终结果整体除一次 zoom（比例部分不该被除，只有由 `getBoundingClientRect()` 得来的长度才需要）。`templates/history.html` 的对应三处（HTML 指示条节点、CSS 规则、`updateSidebarScrollIndicator()` 函数含同一处 zoom 修正）逐字同步了一份。纯 CSS+JS 修复，未改动任何 Python 代码。用真实 headless Chromium（Playwright）验证：(1) 鼠标完全移开 `#sidebarRecents`、不做任何滚动/悬停操作的静态状态下，指示条 `.visible` class 已存在、`getComputedStyle` 的 `opacity` 为 `1`、有真实非零的宽高（复现"完全不交互也能看见"这一诉求，直接对应用户报告的"静态标签页里看不到"场景）；(2) 指示条初始位置（`scrollTop=0`）与 `#sidebarRecents` 顶边对齐（误差 `0px`）；(3) 用 JS 把 `#sidebarRecents.scrollTop` 设到底部并派发 `scroll` 事件后，指示条下移且底边与 `#sidebarRecents` 底边对齐（误差 `<1px`）；(4) 复测了此前几轮修复引入的全部既有断言（游客/已登录分页触发、"背景不截断"不变量、移动端抽屉）均未受影响，294 个既有 Python 用例全部通过（未新增/修改任何 black/white/grey box 测试用例，本轮改动只涉及 `templates/index.html`/`templates/history.html` 的 CSS/JS）。
  - **游客历史改为持续镜像进 `sessionStorage`（`persistGuestHistory()`/`loadGuestHistoryFromStorage()`，2026-07-03 新增）**：在引入 `history.html` 之前，`window.guestHistory`只是纯粹的页面内内存数组——因为一切都发生在 `index.html` 这一个页面里，从不需要考虑"导航到另一个文档"的情况。点击 Recents 条目改为整页导航到 `/history/<id>` 后，这个假设不再成立：一次 `window.location.href` 赋值会销毁当前 `window`，任何纯内存的 JS 变量都会连带丢失。修复为 `window.guestHistory` 现在由 `sessionStorage`（键名 `guestHistory`）持续镜像——`persistGuestHistory()` 在每次 pin/rename/delete/新提交对话之后的 `if (!isLoggedIn) { ... }` 分支里被调用，把当前 `window.guestHistory` 整体 `JSON.stringify` 写入 `sessionStorage`；页面加载时 `window.guestHistory = (window.guestHistory && window.guestHistory.length) ? window.guestHistory : loadGuestHistoryFromStorage()`，优先复用 bfcache 回退时仍然存活的旧数组引用，否则从 `sessionStorage` 反序列化重建。`sessionStorage` 天然具备"随标签页/窗口关闭而清空、但同一标签页内跨页面导航存活"的语义，与"游客数据不持久化"这条身份不变量完全吻合——只是把持久化的作用范围从"单个页面的内存生命周期"精确扩大到"单个标签页的会话生命周期"，不多不少。`persistGuestHistory()` 内部包了 `try/catch`（`sessionStorage` 不可用或写满时静默降级，不抛出未捕获异常），因为这只是体验层面的锦上添花，不是任何路径的唯一真相来源。`index.html` 和 `history.html` 两个模板各自独立声明了完全相同的这套辅助函数（项目没有共享 JS 模块的约定，与其余 CSS/JS 重复保持一致的既有风格）。
  - **删除确认改为自定义居中模态框（`showDeleteConfirmModal(entryTitle)`，2026-07-03 新增，纯前端）**：`deleteHistoryItem(id)` 原先直接调用浏览器原生 `confirm('Delete this conversation?')`——一个无法定制样式、且不点名具体是哪条记录的蓝白系统弹窗。现改为 `async function deleteHistoryItem(id)`，先从 `currentHistoryItems` 找到目标条目，`await showDeleteConfirmModal(targetItem.title)` 拿到用户的选择（`Promise<boolean>`），`false` 时直接 `return`（不做任何后续的乐观删除/请求逻辑）。`showDeleteConfirmModal` 惰性创建并复用一个 `#confirmModalOverlay`（`.confirm-modal-overlay`，`position:fixed;inset:0`，半透明黑色背景 `rgba(0,0,0,0.55)`，`display:flex` 居中，`z-index:500`——高于历史操作失败的 `.history-error-toast`（`z-index:400`）和移动端侧边栏抽屉（`z-index:300`），确保不会被二者遮挡）+ 内部 `.confirm-modal` 对话框（深色 `#171717` 背景、`#ececec` 文字，配色与 `.left-sidebar` 一致），结构固定为：标题 `Delete this chat?` → 正文 `This will delete `+`<strong>`包裹的条目标题（`textContent` 赋值，不用 `innerHTML`，条目标题即使含 `<script>`/引号等字符也不会被解析为标签或破坏结构；标题为空字符串时兜底显示 `this conversation`）→ 底部 Cancel（`.confirm-modal-btn-cancel`，透明背景+浅色描边，样式与 `.new-chat-btn` 同源）和 Delete（`.confirm-modal-btn-delete`，纯色红 `#d93b3b`，为侧边栏在深色主题下少数的破坏性操作强调色，与 `.history-error-toast` 的红色系 `rgba(255,100,100,...)` 呼应但不完全相同，因为一个是纯色实心按钮、一个是暗底浅字提示条）两个按钮。函数返回一个 `Promise<boolean>`：点击 Delete → `resolve(true)`；点击 Cancel、点击遮罩背景（`e.target === overlay`，区别于点击对话框本身冒泡到 overlay 但 `target` 是内部元素的情况）、或按下 `Escape` 键（监听在 `document` 上，因为焦点可能不在对话框内部）→ 均 `resolve(false)`；四条路径共享同一个 `cleanup(result)` 收尾函数，负责隐藏遮罩（移除 `.visible` class）、解绑全部四个监听器（避免每次调用都在 `document` 上残留一个新的 `keydown` 监听器）、`resolve`。对话框打开时把焦点设到 Delete 按钮上（`deleteBtn.focus()`），便于键盘用户直接按 Enter 确认或 Tab 到 Cancel。`deleteHistoryItem` 之后的乐观删除 + 回滚逻辑（`splice`/`showHistoryErrorToast`）完全未变，只是从"同步执行、由 `confirm()` 挡在最前面"改为"由 `await` 挡在最前面"，游客分支（`!isLoggedIn` 时 `return`，无网络请求）同样未变。
  - **`clearBtn` 清空时重置 Model 下拉为"使用 Provider 默认模型"（2026-07-03 新增，纯前端）**：`clearBtn` 的 `click` 监听器原本只清空结果区、Prompt 输入框、Provider 勾选框，不触碰 Model 下拉——而 `updateModelDropdown()` 内部有"尽量保留刷新前已选中项"的逻辑（`if (availableModels.has(currentSelectedValue) && currentSelectedValue !== '')`），由于清空 Provider 勾选后 `selectedProviders.length === 0` 会退化为"显示全部 Provider 的模型并集"，用户之前选中的具体模型（如 `gpt-4`）大概率仍在这个并集里，于是清空/新建对话后 Model 下拉会诡异地保留着上一次对话选的模型，而不是回到"使用 Provider 默认模型"这个符合"新对话"语义的初始状态。修复为在调用 `updateModelDropdown()` 之前先执行 `modelSelect.value = ''`：这样 `updateModelDropdown()` 内部读到的 `currentSelectedValue` 就是空字符串，天然走进 `else` 分支，把 `modelSelect.value` 和 `customTrigger.textContent` 都重置为默认态，不需要额外重复设置自定义下拉组件的显示文本。`newChatBtn`（"+ New Chat"）通过 `document.getElementById('clearBtn').click()` 触发同一个监听器，因此同步获得这个重置行为，无需单独处理。
  - **`deleteHistoryItem` 确认删除后联动重置界面（2026-07-03 新增，纯前端）**：用户反馈删除一条历史记录后，屏幕上如果正显示着某次对比的结果（无论是刚提交的还是通过 `loadHistorySnapshot` 加载出的历史快照），残留内容与"这条记录已经不存在了"的状态不符，容易造成困惑。修复为在 `showDeleteConfirmModal(...)` 返回 `true`（用户点击 Delete 确认）之后、原有的乐观删除 `splice`/`renderCurrentHistory()`/`fetch DELETE` 逻辑之前，插入一行 `document.getElementById('clearBtn').click()`——复用 `newChatBtn` 已经在用的"以编程方式点击 clearBtn"手法，等价于用户手动点了一次"Clear results / + New Chat"，隐藏结果区、清空 Prompt、取消所有 Provider 勾选、把 Model 下拉重置为"使用 Provider 默认模型"（含上一条新增的重置行为）。取消删除（点击 Cancel/背景/`Escape`）时函数已在 `if (!confirmed) return;` 提前返回，不会触发这次界面重置。原有的历史记录删除逻辑（本地 `splice` 乐观移除、`DELETE /api/history/<id>` 请求、失败时按原 index `splice` 回滚并 `showHistoryErrorToast`）完全未改动，这次重置只是在其之前追加的一个独立副作用。
  - **`loadSidebarHistory()`**：读取顶层常量 `isLoggedIn`（由 Jinja `{{ 'true' if session.user_id else 'false' }}` 注入，游客与匿名均为 `false`）。`false` 时将 `currentHistoryItems` 指向 `window.guestHistory` 并调用 `renderCurrentHistory()`，**不发起任何 `/api/history` 请求**（避免必然 401 的无意义网络调用，也避免控制台报错噪音）；`true` 时 `fetch('/api/history?page=1&limit=20')`，成功时把 `data.history` 赋给 `currentHistoryItems` 再渲染，失败或响应非 2xx 时渲染"Could not load history."兜底提示，不抛出未捕获异常。首次调用发生在脚本末尾（页面加载完立即执行一次）；此外 `/api/compare` 成功后，已登录用户在 `history_id` 非空时、游客在把新记录 `unshift` 进 `window.guestHistory` 后，都会再次调用它刷新 Recents。
  - **移动端抽屉式导航（`@media (max-width: 520px)`，复用既有断点）**：`.hamburger-btn`（三条 `<span>` 画的汉堡图标，桌面端 `display:none`，仅在该断点内 `display:flex`）新增于 `.nav-container` 左侧（包在新增的 `.nav-left` 容器内，与 `.nav-logo` 分组，避免破坏原有 `justify-content:space-between` 两端对齐布局）。该断点内 `.left-sidebar` 切换为 `position:fixed`（`top:0; left:0; height:100vh; z-index:300`）+ `transform:translateX(-100%)` 默认滑出屏幕外，JS 通过 `openSidebar()`/`closeSidebar()` 切换 `.sidebar-open` class（`transform:translateX(0)`，`transition:transform 0.25s ease`）实现平滑滑入滑出；`#sidebarOverlay`（`position:fixed;inset:0`，半透明黑色遮罩，`z-index:250` 低于侧边栏的 300）在该断点被点击时同步关闭侧边栏，桌面端遮罩恒为 `display:none`（无 `.visible` class 时）不占用交互层。桌面端（`>520px`）`.left-sidebar` 保持默认的 flex 静态子项，`.sidebar-open`/`.visible` 两个 class 即使被意外打上也不会有任何视觉效果（这两条规则只在移动端媒体查询内生效）。
  - 顶部新增三态导航栏：已登录时显示用户名、Profile、Logout；游客时显示 Guest Mode 徽章、Login、Register。
  - **移动端响应式导航栏**：`@media (max-width: 520px)` 将 Logo 从"LLM Aggregator"切换为"G4F"（通过 `.logo-full` / `.logo-short` 双 span 实现），隐藏 `.nav-welcome`，压缩 nav 间距与字号（`Guest Mode` 徽章、Login、Register 字号均缩至 `0.82rem`/`0.72rem`），确保导航栏在手机竖屏单行显示。`.guest-badge` 始终设置 `white-space: nowrap` 防止"Guest Mode"被截断为两行。
  - **移动端响应式页面标题**：`@media (max-width: 520px)` 将 `.header h1` 中"G4F LLM Aggregator"切换为"G4F"（通过 `.header-full` / `.header-short` 双 span 实现，手机端隐藏 `.header-full`，显示 `.header-short`）。
  - 游客状态下在导航栏下方显示黑色提示条（`.guest-banner`），引导注册或登录；文案 2026-07-02 起由中文改为英文（"You are currently browsing as a guest and your data will not be saved. Log in or sign up to save your history."），链接文案同步改为 "Log in" / "sign up"。
  - 已登录用户的 header 区域展示个性化欢迎语（`Welcome back, {{ session.username }}`）。
  - 新增 Flash 消息显示区（位于 `.container` 内、`.header` 之上），确保注册成功等提示在此处被立即消费。Flash 消息在渲染后 3 秒自动淡出消失（opacity 渐变 0.4s，淡出后从 DOM 移除，若 `.flash-messages` 容器变空则一并移除）。
  - `escapeHtml(str)`: provider 名、model 名、error 均通过此函数转义后注入 DOM，防止 XSS。`response` 经 `marked.parse()` 渲染为 Markdown HTML，不经过 `escapeHtml`（内容来自受信 LLM，Markdown 渲染为有意为之）。
  - `renderPeerReviews(reviews, uid)`: 将 `peer_reviews` 数组渲染为可折叠面板，展示"Blind review from [Provider] [N pts]: [comment]"（2026-07-02 起面板文案由中文"来自 [Provider] 的盲评 [N分]：[comment]"改为英文，折叠按钮文案同步由"查看其他 AI 盲评 (N)"改为"View other AI blind reviews (N)"）。面板默认折叠，通过 `togglePeerReview(uid)` 切换显示状态。
  - `displayResults(data)` 在每个成功结果的 `.provider-response` 下方附加互评面板；失败结果不展示互评。
  - **Provider 勾选框自绘实现**：`<input type="checkbox" class="provider-trigger">` 本身被完全隐藏（`position: absolute; width: 0; height: 0; opacity: 0`），因为 `<input>` 属于替换元素（replaced element），规范上不保证支持 `::before`/`::after`，早期直接在 `input:checked::after` 上画对勾在部分浏览器下会残缺（只剩一条边，视觉上像斜线）。视觉勾选框改由紧跟其后的 `<span class="checkbox-box">`（普通非替换元素）承载：18×18 正方形、`border-radius: 2px`，通过兄弟选择器 `.provider-trigger:checked + .checkbox-box` 联动状态；对勾本身是 `.checkbox-box::after` 用 `border-width: 0 3px 3px 0` + `rotate(45deg)` 画出的黑色（`#1a1a1a`）对勾，未选中时 `opacity: 0` 隐藏、选中时 `opacity: 1` 显示。
  - **Provider 勾选框选中态配色**：选中时 `.checkbox-box` 背景从白色变为品牌绿 `#06c167`（与边框同色）；同时整张 `.provider-checkbox` 卡片的边框也变为与 `:hover` 态相同的 `#06c167`，通过 JS 维护的 `.is-checked` class 实现（`.provider-checkbox.is-checked { border-color: #06c167 }`），未采用 CSS `:has()` 选择器以规避旧版浏览器兼容问题。`syncCardChecked(card)` 函数负责让该 class 与 `input.checked` 保持同步，在以下所有路径中都会被调用：卡片初始化时（含浏览器 bfcache 回退恢复已勾选状态的情况）、直接点击原生 input（含键盘 Tab+Space 触发）、点击卡片其他区域触发的手动 toggle、以及 Clear 按钮批量清空所有勾选时（`clearBtn` 监听器在 `providerTriggers.forEach(cb => cb.checked = false)` 之后必须紧跟 `providerCards.forEach(syncCardChecked)`，否则卡片边框会在清空后残留绿色）。
  - **Model 自绘下拉框（`customSelectWrapper`/`customOptionsContainer`）的黑色高亮延迟切换（2026-07-03 用户报告）**：原实现在 `.custom-option` 的 `click` 监听器里同步执行三件事——`allCustomOpts.forEach(o => o.classList.remove('selected')); this.classList.add('selected');`（黑色高亮 `.custom-option.selected { background-color:#09110e }` 立即从旧选项跳到新选项）、更新 `customTrigger.textContent`/`modelSelect.value`、以及 `customWrapper.classList.remove('open')`（触发 `.custom-options` 0.15s 的收起过渡动画）。三者在同一次点击里同步触发，意味着黑色高亮的跳变发生在面板刚开始收起、仍部分可见的那一瞬间，用户会看到选项间黑色背景快速跳动后面板才淡出，视觉上显得紊乱。修复为拆分两个阶段：点击时只立即更新 `customTrigger.textContent`/`modelSelect.value`（功能性状态，须同步生效）并让面板开始收起，`.selected` class 的变更改为通过新增的 `syncSelectedOptionHighlight()`（遍历 `customOptionsContainer` 内全部 `.custom-option`，用 `data-value === modelSelect.value` 重新计算每个选项的 `.selected`）在 `setTimeout(..., MODEL_HIGHLIGHT_DELAY_MS)`（`150`，与 `.custom-options` 收起动画的 CSS `transition: all 0.15s ease` 时长对齐）之后才执行——面板此时已经完全淡出隐藏，高亮的跳变发生在用户看不见的地方。新增的 `modelHighlightTimer` 是这个延迟调用的唯一计时器句柄，在三处会被 `clearTimeout`：(1) `customTrigger` 的 `click` 监听器里，当检测到本次点击是"打开"操作（`!customWrapper.classList.contains('open')` 在 toggle 之前判断）时，立即调用一次 `syncSelectedOptionHighlight()` 并清掉任何还未触发的旧计时器——这是为了处理"选中后 150ms 内又立刻重新打开下拉框"的竞态：不清理旧计时器会导致重新打开后高亮已经是正确的（因为立即同步过一次），但之前排队的计时器仍会在之后触发一次多余的（但无害的）重复同步；(2) `updateModelDropdown()` 开头，因为该函数会用 `customOptionsContainer.innerHTML = ''` 整体重建所有 `.custom-option` 节点，任何还未触发的旧计时器所指向的（通过闭包引用 `customOptionsContainer` 而非缓存的节点列表，所以其实不会报错，但清理是为了避免时序上的冗余同步）分组已经过期；(3) 在新的计时器被设置之前（即每次点击选项时先清后设，避免连续快速点击不同选项时排队出多个计时器、最终互相覆盖但仍造成多余的 DOM 遍历）。`syncSelectedOptionHighlight()` 特意设计为每次调用都重新 `querySelectorAll('.custom-option')`（而不是复用 `updateModelDropdown()` 内部构建时缓存的 `allCustomOpts` 变量），因为该延迟回调可能在 `updateModelDropdown()` 重建过节点之后才执行，缓存的引用会指向已从 DOM 中移除的旧节点。纯前端修复，未改动任何 Python 代码；用真实 headless Chromium（Playwright，复用第 11 节记录的 `apt-get download` + `dpkg -x` + `LD_LIBRARY_PATH` 沙箱内免 root 解压系统共享库的方法）驱动一次真实点击，断言点击瞬间 `.custom-option.selected` 仍停留在旧选项（新选项尚未变黑）、`#customSelectWrapper` 的 `open` class 已被同步移除（面板已经开始收起，不依赖真实等待 CSS 过渡结束）、延迟窗口过后高亮才正确跳到新选项；另外验证了"选中后立刻重新打开"的竞态场景，重新打开时高亮已经等于最新选中值且之后不会再跳变。
  - **`html { overflow-y: scroll; }`：消除加载历史快照后再 Clear 导致的整页水平位移（2026-07-03 用户报告，同日经历一次技术方案调整）**：`clearBtn`/`loadHistorySnapshot` 都只是切换 `#results` 的 `display:none`/`block`，不会主动控制滚动条——点击 Recents 里一条内容较长的历史记录后，`#resultsContainer` 撑高整个文档，触发纵向滚动条出现；此时 `html`/`body` 均未设置任何 `overflow` 声明，浏览器按默认行为为滚动条让出轨道宽度，`.nav-container`、`.container` 这类 `max-width` + `margin:0 auto` 居中的元素因此可用宽度变窄、整体左移几像素；随后点击 Clear Results/+ New Chat 隐藏结果区，文档变矮，滚动条消失，两者又整体右移回原位——用户看到的就是"点了 Clear 之后页面整体往右挪了一点"。**第一版修复**用了更新的 `scrollbar-gutter: stable` 属性：让浏览器无论当前是否真的溢出，都始终为纵向滚动条预留同一份轨道空间，`clientWidth` 确实因此在"加载长结果（溢出）→ Clear（不溢出）"整个过程中保持恒定。但这个属性只做**静默预留**、不绘制任何内容——一旦内容矮于视口、当前其实不需要滚动条，那段被预留的宽度就是纯粹的空白，露出 `html`/`body` 的背景色；由于紧贴视口右边缘、背景纯白的 `.navbar` 这类通栏元素也会被这段预留空间往左挤，用户在结果区为空（最常见的初始状态）时看到的是"顶部导航栏右侧白色没有铺满，右边空缺出一块"——这是第一版修复自己引入的新回归，当天由同一用户报告。**改为 `html { overflow-y: scroll; }`**（更早、更朴素的经典写法）：它同样让浏览器始终为纵向滚动条预留固定宽度（不随内容是否溢出变化，因此依然能防止水平位移），但预留出的这段空间里画的是一个**真实存在的滚动条控件**（滚动槽/滑块图形），而不是无法归类的空白——即使当前内容不需要滚动，这段空间在视觉上也能被正确理解为"这是滚动条"，不会被误认成背景没铺满的 Bug。两版规则都紧跟在 `:root` 变量声明之后、在真实视口宽度上生效，与 `body` 上非标准的 `zoom: var(--page-zoom)` 缩放无关，不需要参与 `--page-zoom` 相关的 `calc()`。纯 CSS 修复，未改动任何 Python/JS 代码；用真实 headless Chromium（Playwright）验证两件事：(1) 沿用第一版修复时写好的位移检测——注入一条超长的 guest 历史记录、点击加载后再点击 Clear，断言 `document.documentElement.clientWidth` 在"初始 → 加载长结果（`scrollHeight` 确认已超出 `innerHeight`）→ Clear 后（`scrollHeight` 恢复到初始值）"三个时间点始终相等；(2) 新增的无缝检测——断言 `.navbar`/`.guest-banner`/`.app-layout` 的 `getBoundingClientRect().width` 始终等于 `document.documentElement.clientWidth`（即通栏元素确实边到边铺满，不留任何缺口），在 400/1400/1920 三种视口宽度下均成立。**（2026-07-03 晚些时候被下方"整页滚动条替换为自绘可拖拽指示器"这一轮修复取代——`html { overflow-y: scroll; }` 已改回 `scrollbar-width:none`/`::-webkit-scrollbar{display:none}`，理由与验证结论见下方对应条目，此处"始终恒定预留某个非零宽度"的结论不再成立，取而代之的是"恒为 0 宽度"，两者都满足"不引发水平位移"这一根本目的，见下方新条目。）**
  - **导航栏与两栏布局（`.app-layout`）对齐重构（2026-07-03 用户报告，纯前端）**：修复前 `.nav-container` 用 `max-width:1200px; margin:0 auto; padding:0 2rem;` 把自己box 成一条独立于下方 `.app-layout` 之外、按视口宽度居中的窄条——它的宽度、左右留白都和"260px 深色侧边栏 + flex:1 主内容区"这个边到边铺满视口的两栏结构毫无关系，logo 在视口上的水平位置纯粹取决于当前浏览器宽度，与侧边栏没有任何对齐关系；同时由于 `nav-container` 常常在宽屏上早早触顶 1200px 上限，`.nav-links`（Welcome/Profile/Logout 或 Guest Mode/Login/Register）右侧会残留大片未使用的空白，视觉上显得和右侧真实留白脱节。修复为：(1) `.nav-container` 去掉 `max-width`/`margin:auto`，改为边到边铺满 `.navbar`（与 `.app-layout` 同宽），只保留 `padding:0 2rem 0 0`（右侧留白，左侧留白交给下一条）；(2) `.nav-left` 新增 `width:260px; flex-shrink:0; justify-content:center;`——宽度与 `.left-sidebar` 完全一致，且因为 `.nav-container` 左侧无 padding，两者都从 `x=0` 起算，`justify-content:center` 因此让 logo 精确居中在黑色侧边栏所在的那一纵列正上方，实现"与黑色 sidebar 居中对齐"；(3) 因为 `.nav-container` 不再被 1200px 封顶，`.nav-links` 在 `justify-content:space-between` 下自然贴到视口右侧（仅留 2rem 呼吸间距），比修复前普遍更靠右，满足"稍微往右一点"的要求，同时让导航栏与下方边到边的 `.app-layout` 共享同一套"满宽框架"视觉语言，不再是两种不同宽度基准的拼接。移动端（`<=520px`）侧边栏退化为 `position:fixed` 浮层抽屉、不再是静态两栏之一，`.nav-left` 的定宽在这个断点没有对齐意义，已在既有的 `@media (max-width: 520px)` 块内新增 `.nav-left { width:auto; flex-shrink:initial; justify-content:flex-start; }` 覆盖为自然宽度、左对齐（与汉堡按钮的既有布局习惯一致）。纯 CSS/标记微调（`.nav-container`/`.nav-left` 规则本身，未新增/删除任何 DOM 节点或 class 名），未改动任何 Python 代码；用真实 headless Chromium（Playwright，1400×900 桌面视口）测得 `.left-sidebar` 中心点与 `.nav-logo` 中心点水平坐标完全相等（diff = 0px），`.nav-links` 右边缘相比修复前更贴近视口右边缘；另在 400×800 移动视口下验证汉堡按钮 + logo 左对齐、`.nav-links` 仍正常靠右，两个断点均截图核对。
  - **原生滚动条（Recents 侧边栏 + 整页）彻底替换为自绘可拖拽指示器（2026-07-03 晚些时候，用户报告 3 个相关问题，纯前端）**：本轮共修复三件事，均只涉及 `templates/index.html`/`templates/history.html` 的 CSS/JS，未改动任何 Python 代码。
    1. **"Recents 黑色列右侧现在有 2 个 scrollbar，只保留 1 个可以拖拽的"**：`#sidebarRecents` 此前仍保留了原生滚动条（靠 `scrollbar-width:thin`/`::-webkit-scrollbar` 系列自定义外观，见上方"滚动条本身在 Chrome 标签页里依然不可见"那轮修复），只是原生滚动条本身 Chrome 判定为 overlay 模式、平时不可见；但只要用户主动滚动/悬停，Chrome 仍会短暂淡入这个原生 overlay 滚动条，与恒定可见的自绘指示器 `#sidebarRecentsScrollThumb` 同时出现，形成"2 个滚动条"。且更根本的问题是：浏览器原生滚动条渲染在浏览器自己的 UI 合成层，天然盖在任何页面内容之上——只要它还占据哪怕一像素的可交互区域，自绘指示器的 `mousedown` 就永远抢不到那个像素点，"可拖拽"这个诉求在原生滚动条仍然存在的前提下根本做不到。修复为彻底隐藏 `#sidebarRecents` 的原生滚动条（`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`，在"是否溢出"两种状态下都恒为 0 宽度，而不是之前那样只调整外观、仍可能被 Chrome 判定为需要展示的 overlay），并新增 `setupThumbDrag(thumbEl, opts)` 通用拖拽函数（`mousedown`/`mousemove`/`mouseup` + 对应的 `touchstart`/`touchmove`/`touchend`，用 `opts.getScrollTop/getScrollHeight/getClientHeight/setScrollTop` 抽象出"被滚动的对象是谁"）赋予 `#sidebarRecentsScrollThumb` 真正的拖拽能力，CSS 同步加上 `cursor:grab`/`.dragging{cursor:grabbing}`。`setupThumbDrag` 内部的比例换算刻意不做任何 `--page-zoom` 除法：`e.clientY`（鼠标的屏幕坐标，已是"物理"缩放后像素）与 `scrollTop`/`scrollHeight`/`clientHeight`（元素自身局部布局盒尺寸，同样是"物理"缩放后的数值）本来就在同一套坐标空间里，只有把换算结果真正赋给 CSS `style.top`/`style.height` 时才需要除以 zoom（这一步仍然只发生在 `updateSidebarScrollIndicator()`/`updatePageScrollIndicator()` 内部，拖拽只负责调用 `setScrollTop()`，由随之而来的原生 `scroll` 事件驱动这两个函数重新定位指示器）。
    2. **"Chrome 用户要 110% zoom 才能看到这个 scrollbar，需要让它不受 zoom 大小限制"**：根因追溯到 `.left-sidebar` 的高度此前由 JS 在 `syncSidebarStickyMetrics()` 里手工计算 `(window.innerHeight - realHeaderHeight) / zoom`，注入为字面量 `--sidebar-sticky-height` 像素值；`window.innerHeight` 是 JS 侧取整后的整数，与浏览器自身的页面缩放（用户手动 Ctrl+/- 调整的百分比，不同于页面自带的 `--page-zoom` 这个 CSS `zoom` 效果，其具体数值随用户偏好多次调整过，与本条无关，具体见 :root 声明）叠加时，取整误差会导致 `#sidebarRecents` 的可用高度在不同缩放级别下算出的数值有细微出入——当算出的高度恰好比内容实际所需高度略大时，`updateSidebarScrollIndicator()` 判定"没有溢出"（`scrollHeight <= clientHeight + 1`）从而隐藏指示器，这正是用户描述的"需要缩放到 110% 左右才会被判定为溢出、指示器才出现"。修复为 `.left-sidebar` 的 `height` 改由纯 CSS `calc()` 对 `100vh` 求值：`height: calc(100vh / var(--page-zoom) - var(--sidebar-header-height) + 2px)`（`top: var(--sidebar-header-height)`），`syncSidebarStickyMetrics()` 相应简化为只测量并写入 `--sidebar-header-height` 这一个变量（即 `realHeaderHeight / zoom`），不再在 JS 里对 `window.innerHeight` 做减法——`100vh` 的解析完全交给浏览器渲染引擎在绘制时原生完成，不再经过 JS 侧取整数值的二次运算，从根源上消除了"是否真的溢出"这一判定对浏览器缩放级别的敏感性。
    3. **"Recents 黑色列下方黑色块在比如 150% zoom 后，滚动到底部会在左侧露出空缺，需要铺满"**：与第 2 点同一根因（`.left-sidebar` 高度计算的 JS 取整误差），只是触发条件反过来——之前是"算出的高度偏大导致误判未溢出"，这里是在更高缩放比例下取整误差可能让算出的高度偏小，`.left-sidebar` 的实际渲染高度就比视口需要的少那么几像素，滚动到一个足够长页面的最底部时，这几像素的差额就会在 `.left-sidebar` 底边和视口底边之间露出缝隙，透出下方的浅色页面背景。同一处 CSS calc 修复解决了这个问题，额外加了 `+2px` 的安全余量——让任何残余的亚像素误差稳定偏向"侧边栏渲染得略高一点"（无害，深色背景多铺几像素不会有人注意到）而不是"偏矮、露出缝隙"（这才是真正的 Bug）。
    - **整页滚动条同步接受相同的整改（"同样的，整体右侧的 scrollbar 也要进行类似修改"）**：`html` 上原来强制常驻显示、只做外观定制的 `overflow-y: scroll` 改为彻底隐藏（`-ms-overflow-style:none; scrollbar-width:none;` + `html::-webkit-scrollbar{display:none}`，在"是否溢出"两种状态下都恒为 0 宽度）；新增 `#pageScrollThumb`（`.page-scroll-indicator`，`position:fixed; right:3px;`，深色半透明、适配浅色主页面背景，与深色侧边栏上浅色的 `#sidebarRecentsScrollThumb` 相反配色）作为整页的自绘可拖拽滚动指示器，新增函数 `updatePageScrollIndicator()`（与 `updateSidebarScrollIndicator()` 同构，只是把 `#sidebarRecents` 的 `scrollTop`/`scrollHeight`/`clientHeight` 换成 `window.scrollY`/`document.documentElement.scrollHeight`/`window.innerHeight`）+ 复用同一个 `setupThumbDrag()`（`setScrollTop` 实现为 `window.scrollTo(0, v)`）。`html{overflow-y:scroll}` 这条规则最初是为了"无论是否溢出都恒定占用某个非零宽度，防止 nav-container/.container 因滚动条忽隐忽现而左右位移"；改为恒为 0 宽度同样满足"两种状态下宽度不变"这个真正的目的，只是从"恒为某非零值"换成"恒为 0"，因此不会重新引入更早修复过的水平位移 Bug——已用 Playwright 复测确认。`updatePageScrollIndicator()` 的显隐/重算触发点为：页面首次加载、`window.resize`、`window.scroll`（`passive:true`），以及一个挂在 `document.body` 上的 `ResizeObserver`（覆盖 `displayResults()` 渲染结果、`clearBtn` 收起结果区、flash 消息自动淡出等任何会改变文档渲染高度的场景，不需要逐一去这些调用点手动插入刷新调用）。
    - 用真实 headless Chromium（Playwright，复用第 11 节记录的 `apt-get download libnspr4 libnss3 libasound2t64 libasound2-data` + `dpkg -x` 解压至用户目录 + `LD_LIBRARY_PATH` 免 root 沙箱方案）验证：(1) `#sidebarRecents.offsetWidth === clientWidth` 且 `window.innerWidth === document.documentElement.clientWidth`，两处原生滚动条实测占用宽度均为 `0`；(2) 两个自绘指示器（`#sidebarRecentsScrollThumb`/`#pageScrollThumb`）在刚加载、未做任何滚动/悬停交互的情况下就已带有 `.visible` class（不再需要凑到某个特定缩放比例才能看见）；(3) 分别对两个指示器模拟 `mousedown`→`mousemove`→`mouseup` 拖拽序列，确认分别驱动了 `#sidebarRecents.scrollTop`（`0 → 701.25`）和 `window.scrollY`（`0 → 573`）的真实变化；(4) 用更高的 `device_scale_factor`（近似高浏览器缩放下的取整误差敏感度）模拟长结果页面、滚动到底部后测量 `.left-sidebar` 底边与视口底边的差值仅 `0.42px`（此前这条路径没有专门测过，属于本轮新增覆盖）；(5) 复测"加载长结果再 Clear 不引发水平位移"这条更早的不变量（`document.documentElement.clientWidth`/`.navbar` 宽度在滚动条隐藏方式变更前后保持一致，滚动条彻底隐藏并不会重新引入该 Bug）；(6) `templates/history.html` 独立复测了 `#pageScrollThumb` 存在性、`setupThumbDrag`/`updatePageScrollIndicator` 均已定义、`#sidebarRecentsScrollThumb` 的 `cursor` 为 `grab`、无 `pageerror` 事件。294 个既有 Python 测试全部保持通过（本轮改动只涉及模板文件的 CSS/JS，未新增/修改任何 black/white/grey box 测试用例，理由见下方 §11 对应说明）。这一验证没有留下可重复运行的自动化测试文件（纯手动 Playwright 脚本），与本文件此前多次同类修复的处理方式一致。
  - **全局缩放比例统一调整为 110%（2026-07-03，用户要求，纯前端）**：`--page-zoom`（`:root` 变量，驱动 `body { zoom: var(--page-zoom); }`）从曾经的 `0.8`（80% 缩小）统一改为 `1.1`（110% 放大）。这不是 Bug 修复，是用户主动要求的显示比例调整；改动本身只有一行数值替换（`templates/index.html` 与 `templates/history.html` 各自 `:root` 块内的 `--page-zoom` 声明各改一处），但顺带修正了两个文件里此前依赖具体数值（`0.8`/`80%`/`1/0.8`/`20%`）作说明的 CSS 注释和一段 JS 注释（`syncSidebarStickyMetrics()` 上方，`templates/index.html` 独有，`history.html` 的对应注释此前就没有硬编码具体数值，未受影响），把它们改写为对任意 `--page-zoom` 值都成立的通用表述（`zoom < 1` 会露出空白、`zoom > 1` 会产生多余可滚动空间，两种情况都通过 `calc(100vh / var(--page-zoom))`/`除以 zoom` 消除，公式本身与具体数值无关）——**这一修正同时消除了一处此前编辑中途留下的错误注释**：`templates/index.html` 在这次改动之前的工作树里，`--page-zoom` 已被改成 `1.1` 但周围注释未同步更新，逐字保留了原先针对 `0.8` 写的措辞、机械替换数字后产生了"余下的 `-10%` 视口高度会露出空白"这类不成立的表述（`zoom > 1` 时盒子渲染得比 `100vh` 更高，是溢出而非露出空白），本轮一并订正。`calc(100vh / var(--page-zoom))`（`body`/`.left-sidebar` 高度）与 `getBoundingClientRect() 结果 / zoom`（`syncSidebarStickyMetrics()`/`updateSidebarScrollIndicator()`/`updatePageScrollIndicator()` 等函数内部）这两套公式本身对任意 `--page-zoom` 数值都成立、不需要因为改了具体数值而修改代码逻辑本身，这也是当初把它们设计成读取 CSS 变量而非硬编码字面量的原因（详见第 10 节危险区"若以后调整 zoom 的数值，必须只改 `--page-zoom` 这一处"）。用真实 headless Chromium（Playwright，复用第 11 节记录的免 root 沙箱方案）验证：游客身份下 `index.html` 与 `history.html` 各自的 `--page-zoom`/`body` computed `zoom` 均读取为 `1.1`；1400×900 视口下 `.left-sidebar` 的渲染底边与视口底边的差值分别为 `-2.19px`（`index.html`，即略微超出视口 2px，符合 `+2px` 安全余量的设计意图）和 `0.36px`（`history.html`），均未露出视口底部的浅色页面背景，验证"侧边栏铺满到视口底部"这条不变量在新的缩放比例下依然成立；`history.html` 额外验证了通过 `sessionStorage` 注入的游客历史条目仍能正确渲染（`#promptDisplay` 文本匹配），确认这次改动没有影响与 zoom 无关的功能。294 个既有 Python 测试全部保持通过——本轮只是纯 CSS 数值/注释调整，未新增/修改任何 black/white/grey box 测试用例（无任何现存测试断言过 `--page-zoom`/`zoom` 的具体数值，详见第 11 节对应说明），这一验证同样没有留下可重复运行的自动化测试文件（纯手动 Playwright 脚本），与本文件此前历次同类 CSS 渲染验证的处理方式一致。
  - **全局缩放比例改回 80%（2026-07-03 晚些时候，用户要求，纯前端）**：`--page-zoom` 从上一条刚调整到的 `1.1`（110% 放大）改回 `0.8`（80% 缩小）——同一个变量在同一天内经历了 `0.8 → 1.1 → 0.8` 两次调整，均为用户主动要求的显示比例偏好、不是 Bug 修复。改动同样只有一行数值替换（`templates/index.html`/`templates/history.html` 各自 `:root` 块内的 `--page-zoom` 声明各改一处），并同步订正了三处此前写死"当前值"具体数字的注释（而非泛化表述 `zoom<1`/`zoom>1` 的那些，那些本身与数值无关，不受影响）：(1) `body` 规则上方解释 `min-height:calc(100vh/var(--page-zoom))` 由来的那段注释，把"当 `zoom < 1`（如曾经的 `0.8`）"/"当 `zoom > 1`（当前的 `1.1`）"两个举例互换，改为"当 `zoom < 1`（当前的 `0.8`）"/"当 `zoom > 1`（如曾经的 `1.1`）"；(2) `zoom: var(--page-zoom);` 声明上方的单行提示注释，由"即可实现全屏 110% 缩放"改为"即可实现全屏 80% 缩放"；(3) `syncSidebarStickyMetrics()` 函数体内解释"为什么要把 `getBoundingClientRect()` 的物理像素除以 zoom 换算回逻辑像素"的举例注释（`templates/index.html` 独有，`history.html` 对应位置只反向引用 `index.html` 的这份注释、未硬编码具体数值，不受影响），由"a real 56px navbar naively set as `top: 56px` would render at the wrong on-screen size once zoom is applied again (smaller than 56px if zoom<1, e.g. the former 0.8; larger than 56px at the current zoom:1.1)"改为"(smaller than 56px at the current zoom:0.8; larger than 56px if zoom>1, e.g. the former 1.1)"。**用户特别要求验证的核心诉求——"确保所有 scrollbar 在 80% zoom、Chrome 里可以看到，避免用户只能 zoom 到 110% 才出现 scrollbar 再手动缩小"——在这次改动之前就已经被上面"Recents 侧边栏 + 整页原生滚动条彻底替换为自绘可拖拽指示器"那轮修复满足**：当时已经把两处原生滚动条彻底隐藏（`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`，在"是否溢出"两种状态下恒为 0 宽度），改用完全自绘、JS 驱动、不依赖浏览器原生滚动条渲染的 `#sidebarRecentsScrollThumb`/`#pageScrollThumb`，且两者的可见性判定（`updateSidebarScrollIndicator()`/`updatePageScrollIndicator()`）与位置计算已经改成读取 `--page-zoom` CSS 变量动态换算、不再有任何硬编码的 zoom 数值假设——也就是说"需要 110% 缩放才能看到指示器"这个历史症状的**根因**（`.left-sidebar` 高度曾经由 JS 对 `window.innerHeight` 做手工整数运算再叠加浏览器缩放产生的舍入误差）已经被替换成纯 CSS `calc(100vh / var(--page-zoom))` 彻底消除，这个修复本身与 `--page-zoom` 具体数值无关、对 `0.8`/`1.1`/任意其他值都同样成立。本轮改动因此**不需要**（也没有）重新触碰任何 JS 滚动指示器逻辑，只需确认这套已经验证过 zoom 无关性的机制在新的 `0.8` 数值下确实如预期工作。用真实 headless Chromium（Playwright，复用第 11 节记录的免 root 沙箱方案，本地重新执行了一次 `apt-get download libnspr4 libnss3 libasound2t64 libasound2-data` + `dpkg -x` 解压 + `LD_LIBRARY_PATH` 的免 root 步骤，因为解压产物落在会话级 scratchpad 目录、不跨会话持久化）验证：(1) 游客身份下 `index.html`/`history.html` 渲染后 `--page-zoom`/`body` computed `zoom` 均读取为 `0.8`；(2) 向 `window.guestHistory` 注入 40 条记录使 `#sidebarRecents` 产生真实溢出（`scrollHeight`=1551 > `clientHeight`=914）后，`#sidebarRecentsScrollThumb` 在**没有任何滚动/悬停交互**的情况下就已带有 `.visible` class 且有非零的 `top`/`height`（不再需要缩放到某个特定比例才能看见）；(3) 两处原生滚动条实测占用宽度均为 0（`#sidebarRecents.offsetWidth===clientWidth`、`window.innerWidth===document.documentElement.clientWidth`）；(4) 分别对 `#sidebarRecentsScrollThumb`/`#pageScrollThumb` 模拟真实的 `mousedown→mousemove→mouseup` 拖拽序列，确认确实驱动了 `#sidebarRecents.scrollTop`（`0→508.75`）和 `window.scrollY`（`0→1033`）的真实变化；(5) 复测了"`.left-sidebar` 深色背景铺满到视口底部、不露出下方浅色背景"这条历次修复反复守护的核心不变量：`index.html` 在 1400×900 视口下滚动到底部后差值仅 `0.42px`，`history.html` 在额外叠加 `device_scale_factor=2`（模拟高分屏/浏览器缩放场景的取整误差敏感度）后差值仅 `0.41px`，均未出现可见缺口；(6) 复测了"加载长结果再导航离开不引发水平位移"这条更早的不变量（`document.documentElement.clientWidth` 在初始/加载 400 段长结果后两个时间点保持恒定为 `1400`，`.navbar` 的渲染宽度与 `clientWidth` 完全相等）。改动前后运行 `python -m unittest discover -s tests`，294 个用例全部保持通过；全仓库 `grep` 确认没有任何 `.py` 文件或 `tests/` 用例引用过 `--page-zoom` 或其具体数值，因此本轮同样未新增/修改任何 black/white/grey box 测试用例——与上一条"全局缩放比例统一调整为 110%"完全同类的纯 CSS 数值+注释调整，此前已验证过的公式（`calc(100vh/var(--page-zoom))`、`getBoundingClientRect()` 结果除以 zoom）本身与具体数值无关，不需要因为这次改动而修改任何代码逻辑。这一验证同样没有留下可重复运行的自动化测试文件（纯手动 Playwright 脚本）。
  - **全局缩放比例从 80% 调整为 88%（2026-07-03 又更晚些时候，用户要求，纯前端；当前值）**：`--page-zoom` 从上一条改回的 `0.8`（80% 缩小）再次调整为 `0.88`（88% 缩小），同一个变量在同一天内经历了 `0.8 → 1.1 → 0.8 → 0.88` 三次调整，均为用户主动要求的显示比例偏好、不是 Bug 修复。改动同样只有一行数值替换（`templates/index.html`/`templates/history.html` 各自 `:root` 块内的 `--page-zoom` 声明各改一处），并同步订正了三处此前写死"当前值"具体数字（而非泛化表述 `zoom<1`/`zoom>1` 的那些，那些本身与数值无关，不受影响）的注释：(1) `body` 规则上方解释 `min-height:calc(100vh/var(--page-zoom))` 由来的那段注释，把"当 `zoom < 1`（当前的 `0.8`）"改为"当 `zoom < 1`（当前的 `0.88`）"；(2) `zoom: var(--page-zoom);` 声明上方的单行提示注释，由"即可实现全屏 80% 缩放"改为"即可实现全屏 88% 缩放"；(3) `syncSidebarStickyMetrics()` 函数体内解释 56px navbar 换算的举例注释（`templates/index.html` 独有，`history.html` 对应位置只反向引用 `index.html` 的这份注释、未硬编码具体数值，不受影响），由"smaller than 56px at the current zoom:0.8"改为"smaller than 56px at the current zoom:0.88"。**这次改动同样不需要触碰任何 JS 滚动指示器逻辑或高度计算公式本身**：与上一条"改回 80%"条目里论证的原理完全一致——`.left-sidebar` 的高度已经是纯 CSS `calc(100vh / var(--page-zoom) - var(--sidebar-header-height) + 2px)`、两处原生滚动条已经彻底隐藏改用读取 `--page-zoom` 动态换算位置的自绘可拖拽指示器（`#sidebarRecentsScrollThumb`/`#pageScrollThumb`），这套机制对 `0.8`/`0.88`/任意其他值都同样成立，只需要确认它在新数值下确实如预期工作。用真实 headless Chromium（Playwright，本地重新执行了一次 `apt-get download libnspr4 libnss3 libasound2t64 libasound2-data` + `dpkg -x` 解压 + `LD_LIBRARY_PATH` 的免 root 步骤，因为解压产物落在会话级 scratchpad 目录、不跨会话持久化）验证：(1) 游客身份下 `index.html`/`history.html` 渲染后 `--page-zoom`/`body` computed `zoom` 均读取为 `0.88`；(2) 向 `window.guestHistory` 注入 40 条记录使 `#sidebarRecents` 产生真实溢出（`scrollHeight`=1552 > `clientHeight`=812）后，`#sidebarRecentsScrollThumb` 在**没有任何滚动/悬停交互**的情况下就已带有 `.visible` class 且 `opacity` 计算值为 `1`、有非零的宽高；(3) 两处原生滚动条实测占用宽度均为 0（`#sidebarRecents.offsetWidth===clientWidth`、`window.innerWidth===document.documentElement.clientWidth`）；(4) 拖拽 `#sidebarRecentsScrollThumb` 确认驱动了 `#sidebarRecents.scrollTop`（`0→573.86`）的真实变化；(5) 复测了"`.left-sidebar` 深色背景铺满到视口底部、不露出下方浅色背景"这条历次修复反复守护的核心不变量：`index.html` 滚动到页面底部后差值仅 `0.25px`，`history.html` 差值为 `-1.75px`（侧边栏略微超出视口，符合 `+2px` 安全余量的设计意图），均未出现可见缺口；(6) `history.html` 额外验证了通过 `sessionStorage` 注入的游客历史条目仍能正确渲染（`#promptDisplay` 文本匹配"hello world prompt"），确认这次改动没有影响与 zoom 无关的功能。改动前后运行 `python -m unittest discover -s tests`，294 个用例全部保持通过；全仓库 `grep` 确认没有任何 `.py` 文件或 `tests/` 用例引用过 `--page-zoom` 或其具体数值，因此本轮同样未新增/修改任何 black/white/grey box 测试用例——纯 CSS 数值+注释调整，此前已验证过的公式本身与具体数值无关，不需要因为这次改动而修改任何代码逻辑。这一验证同样没有留下可重复运行的自动化测试文件（纯手动 Playwright 脚本）。

### `templates/history.html` [NEW，2026-07-03]

- **role**: 只读历史详情页，展示某一条已保存对话的原始 prompt 和完整对比结果快照；已登录用户和游客均可访问（匿名访客被 `main.py` 的 `view_history()` 重定向，永远到不了这个模板）。
- **key logic**:
  - 文件级别上是 `index.html` 的裁剪版本：`<head>` 里的 `<style>` 整块原样复制（未删减任何规则，包括本页用不到的 `.provider-selection`/`.custom-select-wrapper` 等 —— 保留是为了最大程度降低裁剪引入新 Bug 的风险，代价是少量死 CSS，可接受），`<body>` 里的导航栏、`.guest-banner`、`.app-layout`/`.left-sidebar`/`.sidebar-overlay`（含骨架屏、Recents 分页、pin/rename/delete 的乐观更新与失败回滚、移动端抽屉、`.left-sidebar` 的 `position:sticky`/`syncSidebarStickyMetrics()`，2026-07-03 同日修复 Recents 分页从未真正触发的 Bug 时同步改了这里；`#sidebarRecentsScrollThumb` 自定义滚动指示器 + `updateSidebarScrollIndicator()`，同日紧接着修复"滚动条在 Chrome 标签页里不可见"时同步改了这里；`#sidebarRecents`/`html` 的原生滚动条彻底隐藏 + `setupThumbDrag()` 拖拽逻辑 + 整页专属的 `#pageScrollThumb`/`updatePageScrollIndicator()`，2026-07-03 晚些时候再次修复"2 个 scrollbar"/"需 110% zoom 才可见"/"150% zoom 滚到底部露出缝隙"三个问题时同步改了这里——均见 `templates/index.html` 条目下对应说明）与 `index.html` 完全一致、逐行复制。**唯一的实质差异在 `.main-content` 内部**：移除了 `<form id="compareForm">` 整块（Provider 勾选 `#providerSelection`、Model 自定义下拉 `#customSelectWrapper`、`#compareBtn`/`#clearBtn` 按钮组、`#loading` 加载态全部不存在于这个模板里），改为一个固定展示原始 prompt 的只读区块（`#promptDisplayContainer` 内的 `.prompt-readonly-text`，白底描边样式模仿 `textarea` 但没有任何输入行为）+ 一个"未找到"提示区块（`#historyNotFound`，默认隐藏）+ 原样保留的 `#results`/`#stats`/`#resultsContainer`。**这是"历史只读、不能在原历史 prompt 基础上再次提交产生错误"这一需求的直接实现**：物理上没有提交入口，不存在"编辑历史 prompt 后误触发新对比"的可能性。
  - **数据来源与两条路径**：`isGuestView`（由 `main.py` 的 `is_guest_view` 注入）、`targetHistoryId`（URL 路径参数原样注入）、`serverHistoryEntry`（仅登录路径非空，`view_history()` 已用 `get_chat_history_by_id` 查过并做过归属校验的完整字典，`tojson` 直接注入模板，无需页面加载后再发一次请求）三个顶层常量驱动分流：`initHistoryPage()` 先 `await loadSidebarHistory()`（游客走 sessionStorage 同步重建 `window.guestHistory`/`currentHistoryItems`；已登录走 `/api/history?page=1` 常规请求，与 `index.html` 完全同一份逻辑，用于渲染侧边栏 Recents 列表本身，与本页主内容展示的是"哪一条"无关），再据 `isGuestView` 决定：游客从刚刚重建好的 `currentHistoryItems` 里按 `targetHistoryId` 查找目标条目；已登录直接用 `serverHistoryEntry`（不查 `currentHistoryItems`，因为服务端已经单独查过这一条，没必要依赖侧边栏那一页是否恰好包含它）。`renderHistoryEntry(entry)` 是唯一渲染入口：`entry` 为 `null`（游客 `sessionStorage` 里找不到该 id——多半是被清空过、或该 id 根本不是这个浏览器创建的）时显示 `#historyNotFound` 并隐藏 prompt/results 区块；否则把 `entry.prompt` 塞进 `#promptDisplay`（`textContent`，不解析 Markdown，因为这是用户自己的原始输入而非模型回复），`entry.results` 非空时复用 `displayResults()`（与 `index.html` 完全同一份函数，包含互评面板渲染），为空数组时（防御性分支，正常流程下 `save_chat_history` 总是带完整 `results`）隐藏结果区。
  - **"+ New Chat"（`#newChatBtn`）与删除当前正在查看的条目**：本页没有 `#clearBtn` 可供复用，`#newChatBtn` 的点击处理器直接 `window.location.href = '/'`（离开这个只读页面，回到可以真正发起新对比的 `index.html`）。`deleteHistoryItem(id)` 沿用与 `index.html` 相同的乐观删除+确认模态框逻辑，但有一处必要分叉：确认删除后，若 `id === targetHistoryId`（删的正是当前页面正在展示的这一条），直接 `window.location.href = '/'` 离开，不走"本地 splice + 重渲染侧边栏"那一套（本页主内容已经没有意义留着，应当离开而不是留在一个刚被自己删除的历史记录的详情页上）；删除侧边栏里其他条目时行为与 `index.html` 完全一致。
  - **顶部说明文案两行化 + Prompt 展示区底部空白收窄（2026-07-03，用户报告，纯前端）**：`.header` 下方的说明文案原先是 `Read-only view of a saved comparison. Nothing here can be edited or resubmitted.` 一整句挤在同一行，现改为 `<br>` 分隔的两句各占一行（`Read-only view of a saved comparison.` / `Nothing here can be edited or resubmitted.`），写法与 `home.html` 的 `.welcome-card .subtitle` 现有的两行欢迎语一致；`.header p` 同步补上 `line-height:1.5`（`index.html` 的同款规则也同步补齐这一行，确保两份 `<style>` 保持逐行一致，即便 `index.html` 目前的欢迎语都是单句、暂时用不到多行行距）。另外修复了 `#promptDisplayContainer`（只读展示原始 Prompt 的容器）内部空白不对称的问题：容器内唯一的子元素 `.form-group` 沿用了 `index.html` 里给"多个 `.form-group` 依次堆叠"设计的 `margin-bottom:26px`，但本页容器内 `.form-group` 之后再没有任何兄弟元素，这个本该用来分隔下一个字段的下边距只是单纯叠加在容器自身的底部 `padding` 之上，导致 Prompt 文本框下方的空白比上方（label 到容器顶边）明显更宽。新增 `#promptDisplayContainer .form-group { margin-bottom: 0; }` 覆盖后，上下空白都收敛为容器自身的 36px padding，视觉上对称。两处改动都只影响 `history.html` 自身的 `<style>`（`.header p` 的 `line-height` 例外，为保持两份 `<style>` 同步而在 `index.html` 里也加了一份），不影响 `index.html` 的 `#compareForm` 区域（那里的 `.form-group` 后面确实还跟着别的表单元素，`margin-bottom:26px` 在那边仍然是必要的分隔间距，因此该规则本身未被改动，只是新增了一条更高特异性的 `#promptDisplayContainer` 局部覆盖）。
- **depends_on**: 与 `index.html` 相同的前端依赖（`marked.js`、`escapeHtml`、Recents 分页/乐观更新/游客 sessionStorage 全套辅助函数），额外多出 `main.py` 通过 Jinja2 注入的 `history_id`/`history_entry`/`is_guest_view` 三个模板变量。
- **affects**: 无独立后端路由之外的影响面；被 `main.py` 的 `view_history()` 渲染，被 `index.html`/自身的 Recents 侧边栏通过 `openHistoryEntry(id)` 链接过来。

### `templates/auth/base.html` [NEW]

- **role**: 认证模块所有页面的通用布局基础模板。
- **key logic**: 导航栏根据 `session.user_id` 和 `session.is_guest` 进行三态切换：已登录显示 Profile + Logout；游客显示 Guest Mode 徽章 + Login + Register；未认证显示 Login + Register。Flash 消息（`.alert` 类）统一在 `.card` 容器顶部渲染，3 秒后自动淡出消失（opacity 渐变 0.4s，淡出后从 DOM 移除）。**移动端响应式导航栏**：`@media (max-width: 520px)` 压缩 nav-container padding、Logo 字号（`1.05rem`）、nav-links 间距与 a 标签字号（`0.82rem`）、`.guest-badge` 字号（`0.72rem`），与 `index.html` 保持一致。`.guest-badge` 始终设置 `white-space: nowrap` 防止"Guest Mode"换行。
  - **`.card-title`（2026-07-03 新增，用户报告的 UI 比例统一审计）**：`login.html`/`register.html`/`profile.html` 三个页面的 `<h2>` 页面标题此前各自内联 `style="color: #333; margin-bottom: 2rem;"`，颜色（`#333`）和字重（未设置，走浏览器默认的 `<h2>` 粗体）都没有对齐全站统一使用的排版体系——`.welcome-card h1`（`home.html`）、`.header h1`（`index.html`/`history.html`）、`.user-info h3`（本文件）全部是 `color:#09110e` + `font-weight:800` + 负字距（`-0.02em` 到 `-0.04em`），唯独这三处标题因为是内联样式而各自游离在这套系统之外，视觉上比其余标题更细、更灰、字距更松。新增 `.card-title`（`color:#09110e; font-size:1.6rem; font-weight:800; letter-spacing:-0.03em; margin-bottom:28px;`）统一承载这三处标题，字号 `1.6rem` 介于 `.user-info h3`（`1.1rem`，卡片内小标题）与 `.welcome-card h1`（`2.2rem`，独立欢迎页大标题）之间，符合"卡片内的页面级标题"这一定位。

### `templates/auth/login.html` 和 `templates/auth/register.html` [NEW]

- **role**: 登录和注册表单页。
- **key logic**: 表单下方提供两个快捷链接。"Back to welcome page" 链接指向 `url_for('home')`（即 `GET /home` 路由），该路由会清除游客状态后重定向到根路由，确保游客点击后能看到 `home.html` 而非 `index.html`。"Continue as guest" 链接通过 Fetch POST `/api/auth/guest` 切换为游客身份。页面标题 `<h2>` 已于 2026-07-03 从内联 `style="color: #333; margin-bottom: 2rem;"` 改为 `class="card-title"`（复用 `auth/base.html` 新增的类，详见该文件条目），不再有任何内联样式。`templates/auth/profile.html` 的页面标题同样改用了这个类。

### `availability_g4f/` [MODIFIED] 和 `tests/`

`tests/` 目录的功能与原有描述保持不变，不部署到 GAE。`availability_g4f/` 于 2026-07-03 新增一组文生图（text-to-image）可用性探测脚本，定位与既有的文本 Provider 探测脚本完全一致——纯开发辅助工具，不被 `main.py`/`auth/` 导入、不被 `tests/` 覆盖、不部署到 GAE。

**文生图 Provider 调研结论（2026-07-03，g4f 7.6.4）**：本次调研的产出是**纯粹的可用性调研**，main.py 未新增任何图片生成路由，前端也未新增任何生图入口——LLM 聚合系统目前仍然只支持文本对话。调研分两步：`find_image_providers.py` 静态扫描出 g4f 库内所有具备图片生成能力标记的 Provider 类；`test_image_providers.py` 对其中 `working=True` 且 `needs_auth=False` 的候选发起真实的 `g4f.client.Client().images.generate()` 请求验证。结果（完整日志见 gitignored 的 `image_provider_test_results.txt`，人工汇总见 gitignored 的 `availability_g4f/available_free_image_providers.txt`）：

- **确认可用（5 个，均免 Key）**：`PollinationsImage`（不传 model，响应最快无冷启动，与项目已集成的 PollinationsAI 文本 Provider 同源）、`BlackForestLabs_Flux1Dev`（model=`flux-dev`，底层 HuggingFace Space，有冷启动延迟）、`AnyProvider`（model=`flux`，g4f 元路由，实测命中的后端与 `BlackForestLabs_Flux1Dev` 相同域名，属于等价 fallback、不建议重复接入）、`StabilityAI_SD35Large`（model=`sd-3.5-large`，同样是 HuggingFace Space 后端）、`OperaAria`（model=`aria`，与项目现有文本 Provider 同名但走的是完全不同的 `client.images.generate()` 接口而非 `g4f.ChatCompletion.create()`，两者不可混用同一套调用逻辑）。
- **Gemini 生图 / Google AI Studio（用户提供的调研目标之一，链接 `https://aistudio.google.com/prompts/new_chat`）：实测不可免费使用。** 两条路径均已验证并失败：(1) `AnyProvider` + `model="gemini-2.5-flash-image-preview"` → `Model ... not found in any provider`，说明 AnyProvider 的模型清单里虽然列出了该 Gemini 图片模型名，但没有任何免 Key 后端真正实现它；(2) `g4f.Provider.GeminiPro`（g4f 对 Google AI Studio 官方 API 的封装，`login_url` 直接指向 `https://aistudio.google.com/u/0/apikey`，源码内 `models_needs_auth = True`）→ `Error 404: models/gemini-2.5-flash is not found ... or is not supported for predict`，说明该 Provider 类虽然 class 级别的 `needs_auth` 标记为 `False`（允许发起请求尝试），但 Google 后端在没有真实用户 API Key 时直接拒绝服务；且 `GeminiPro` 本身 `image_models` 为空，即便配了 Key 也不是走图片生成接口。结论：若未来要接入 Gemini 生图，必须由用户自行在 Google AI Studio 申请并配置 API Key，这与本项目 g4f 适配层"零凭证调用免费渠道"的既有设计原则（见第 8 节）不符，本轮不予集成。
- **其余候选失败原因（非 Gemini 相关，均记录在案供未来复测参考）**：`BlackForestLabs_Flux1KontextDev` 是图生图/图片编辑模型（Kontext 系列），必须传入已有图片，不支持纯文字生图，属于测试候选选型不当而非故障；`DeepseekAI_JanusPro7b` 返回 503（可能是临时过载，可复测）；`HuggingSpace` 未指定 model 时走默认后端但未返回媒体内容；`OpenaiChat` 需要用户预先在本地浏览器登录并导出 `.har` 会话文件，本质仍是人工凭据，不算真正免 Key；`Qwen` 请求被阿里云 WAF 识别为自动化流量并拦截，与"是否需要 Key"无关。

## 5. 🔄 EXECUTION & DATA FLOW (CRITICAL)

### 1. 初始化阶段

- Flask 应用启动，`load_dotenv()` 加载 `.env` 文件。
- `app.secret_key` 从环境变量读取，Session 加密功能激活。
- `auth_bp` 蓝图注册，`/login`、`/register`、`/logout`、`/profile` 路由挂载。
- `auth/db.py` 初始化：检测 `firebase-key.json`，优先使用它连接 Firebase；否则使用 ADC。
- Flask 应用尝试导入 `g4f`，设置 `G4F_AVAILABLE` 标志。

### 2. 首次访问与身份路由

- 用户访问根路由 `/`。
- `index()` 路由检查 `session.user_id`（已登录）和 `session.is_guest`（游客）。
- 两者皆无时渲染 `home.html`（欢迎页）；任一存在时渲染 `index.html`（功能页）。

### 3. 身份建立

- **游客路径**：用户在 `home.html` 点击"Continue as Guest"，前端 Fetch POST `/api/auth/guest`，后端设置 `session['is_guest'] = True` 并返回 JSON，前端重定向到 `/`，此时 `index.html` 被渲染。
- **登录路径**：用户填写 `/login` 表单，后端验证密码哈希，成功后写入 `session['user_id']` 和 `session['username']`，清除 `is_guest`，重定向到 `/`。
- **注册路径**：与登录路径类似，写入相同的 session 键后重定向到 `/`。

### 4. LLM 聚合请求流程

- 用户在 `index.html` 输入 Prompt，勾选 Provider，点击对比按钮。
- 前端 Fetch POST `/api/compare`，后端启动第一个 `ThreadPoolExecutor` 线程池。
- 各子线程并发执行 `test_g4f_provider`，模型降级规则 A/B/C 在此应用，隐形 Prompt 路由（`ROUTE_PROMPTS_MAP`）追加风格提示词。
- 主线程收集第一阶段结果，为每条 result 初始化 `peer_reviews: []`。
- 互评触发条件：`tested_providers >= 2` 且 `successful_results >= 2`。满足时启动第二个 `ThreadPoolExecutor`，每个成功者 B 对成功者 A 执行 `run_peer_review`（B 不对自身评）；互评响应经 `parse_peer_review_json` 解析为 `{score, comment}`，挂载到 A 的 `peer_reviews` 列表。
- 两阶段完成后按 `(失败状态, 耗时升序)` 排序，返回 JSON。
- 前端 `displayResults()` 渲染统计卡片、响应内容，并在每个成功结果下方附加可折叠互评面板。

### 5. 退出登录流程

- 用户点击导航栏 Logout，GET `/logout`。
- 后端清除 `session['user_id']`、`session['username']`、`session['is_guest']`，Flash "You have been logged out"，重定向到 `/`。
- `index()` 检测到无身份状态，渲染 `home.html`。
- `home.html` 的 Flash 消息显示区渲染并消费"You have been logged out"提示。

## 6. 🧠 CORE LOGIC / DOMAIN RULES

### 用户身份三层不变量（Identity State Contract）

系统在任意时刻有且仅有以下三种互斥的身份状态：

**状态 1：匿名/未认证（Anonymous）**

- `session` 中不含 `user_id`，也不含 `is_guest`。
- 访问根路径 `/` 时，`index()` 渲染 `home.html`。
- 系统不为此状态存储任何用户数据。

**状态 2：游客（Guest）**

- `session['is_guest'] == True`，且 `session` 中不含 `user_id`。
- 访问根路径 `/` 时，`index()` 渲染 `index.html`，展示 Guest Mode 徽章和黄色提示条。
- 游客数据不持久化，仅限单次会话内使用 LLM 聚合功能。
- 游客可访问 `/login` 和 `/register`，但无法访问 `/profile`（后端拦截，重定向到 `/login`）。

**状态 3：已登录用户（Authenticated）**

- `session['user_id']` 存在（Firestore 文档 ID 字符串）。
- `session['username']` 同步存储，用于前端展示，无需额外数据库查询。
- 身份跨请求持久化，数据与 Firebase Firestore `users` 集合同步。
- 已登录用户可访问 `/profile`，导航栏展示 Profile 和 Logout 链接。

### Session 状态转换规范（Flash 冲突预防）

以下规则防止 Flash 消息在 session 中堆积：

- **任何完成身份切换的重定向目标页面，必须包含 Flash 消息显示区。** `home.html` 和 `index.html` 均已加入显示区。新增页面如需作为重定向目标，必须同步加入。
- **游客切换为已登录状态时**，必须在同一个请求内执行：`session['user_id'] = ...`、`session['username'] = ...`、`session.pop('is_guest', None)`，三步必须原子执行，不能遗漏任何一步。
- **已登录状态退出时**，必须清除全部三个键：`session.pop('user_id', None)`、`session.pop('username', None)`、`session.pop('is_guest', None)`。
- **`GET /home` 路由**的唯一职责是清除 `is_guest` 并重定向到 `/`，不操作 `user_id`。已登录用户访问此路由不会被退出登录。

### 模型自适应降级规则

当用户发起 LLM 请求时，系统遵循以下决策树决定最终传递给 g4f 的模型名称。该逻辑已提取为纯函数 `determine_actual_model(provider_name, requested_model)`：

- **规则 A**：若用户指定的模型包含在当前 Provider 的支持映射表内，则使用该指定模型。
- **规则 B**：若用户指定的模型不被支持或未指定，则自动选取映射表中第一个作为默认模型。
- **规则 C**：若该 Provider 没有任何模型配置，则兜底降级为 `"gpt-3.5-turbo"`。

### AI 盲评触发与评分规则

互评阶段在 `compare_providers()` 第一阶段完成后执行，遵循以下不变量：

- **触发条件**：`len(providers_to_test) >= 2` 且 `len(successful_results) >= 2`。任一条件不满足，所有 `peer_reviews` 保持空列表。
- **互评配对**：对每个成功者 A，由其余所有成功者 B 扮演裁判发起点评（B 不对自身评）。2 个成功者时各有 1 条互评；N 个成功者时各有 N-1 条。
- **提示词格式**：`PEER_REVIEW_PROMPTS_MAP[reviewer_model]` 提供裁判人设前缀，要求模型严格输出 `{"score": int, "comment": str}` JSON，不含其他文字。
- **JSON 容错解析**：`parse_peer_review_json` 从响应中扫描首个 `{...}` 块尝试解析。score 被夹入 [1, 100] 并强转为 int。任何解析失败（格式错误、缺失 score 字段、异常）均 fallback 为 `(80, raw_text)`，接口不崩溃。
- **失败者不参与互评**：第一阶段失败的 Provider 既不作为被评对象，也不作为裁判，其 `peer_reviews` 永远为空列表。

### 结果排序权重规则

- **第一优先级**：`success` 状态。成功的请求必须排在失败的请求前面。
- **第二优先级**：`response_time`。相同成功状态下，耗时越短的 Provider 排在越前面。
- 此规则同样适用于 `POST /api/generate-images`（2026-07-03 新增）——`generate_images()` 复用与 `compare_providers()` 完全相同的排序表达式，两条路由的结果列表在"如何排序"这一点上行为一致，只是排序的对象一个是文本 result、一个是图片 result。

### 文生图并发调度规则（2026-07-03 新增）

- **无互评阶段**：`generate_images()` 只有 `compare_providers()` 的第一阶段（并发调用各 Provider），没有对应的第二阶段互评——图片之间不存在类似"用另一个模型评判文本质量"的天然类比，本次未设计任何图片互评机制。
- **Provider 名字空间隔离**：`providers` 请求参数只在 `IMAGE_PROVIDERS` 范围内匹配，与文本对话的 `G4F_PROVIDERS` 是两个完全独立的名字空间——传入一个纯文本 Provider 的名字（如 `Yqcloud`）不会被文生图路由误识别为合法值。
- **不持久化**：成功的图片生成结果不会触发 `save_chat_history`，因此也不会出现在 Recents 侧边栏或 `/history/<id>` 详情页里，这是当前版本的已知范围限制（见第 10 节）。

## 7. 🧾 DATA MODELS / STATE DESIGN

### 核心数据传输对象 (DTO)：LLM Result

`test_g4f_provider` 返回的字典结构如下，此 7-key 契约严禁增删（`peer_reviews` 字段由 `compare_providers` 在外层追加，不属于此契约）：

```python
{
    'provider': str,        # Provider 类的名称
    'success': bool,        # 是否请求成功
    'response': str,        # 模型返回的文本内容（成功时）
    'error': str,           # 异常信息简述（失败时）
    'response_time': float, # 响应耗时，单位秒，保留两位小数
    'model': str,           # 实际使用的模型名称
    'type': 'g4f'           # 固定类型标识
}
```

`compare_providers` 在第一阶段结束后为每条 result 追加 `peer_reviews` 字段，使最终 `/api/compare` 响应中每条 result 共有 8 个 key：

```python
result['peer_reviews'] = [
    {
        'reviewer_provider': str,  # 裁判 Provider 名称
        'reviewer_model': str,     # 裁判使用的模型名称
        'score': int,              # 评分（1-100，由 parse_peer_review_json 夹入范围）
        'comment': str,            # 一句话点评（JSON 解析失败时为原始响应文本）
    },
    ...
]
```

互评 DTO key 集合（`reviewer_provider`、`reviewer_model`、`score`、`comment`）为前后端契约，严禁增删。

### 核心数据传输对象 (DTO)：Image Result（2026-07-03 新增）

`test_g4f_image_provider` 返回的字典结构如下，与上方文本 Result 是两套完全独立的契约（字段名不通用，不可混用/互相转换），此 8-key 契约同样严禁增删：

```python
{
    'provider': str,          # Provider 类的名称
    'success': bool,          # 是否生成成功
    'url': str | None,        # 图片 URL（成功且 Provider 返回 URL 形式时）
    'b64_json': str | None,   # 图片 base64 编码（成功且 Provider 返回 base64 形式时；与 url 互斥，一次成功调用只会有其中一个非 None）
    'error': str,             # 异常信息简述（失败时）
    'response_time': float,   # 响应耗时，单位秒，保留两位小数
    'model': str,             # 实际使用的模型名称（未知 Provider 兜底为 'default'）
    'type': 'g4f_image'       # 固定类型标识，区别于文本的 'g4f'
}
```

`/api/generate-images` 响应体不追加任何类似 `peer_reviews` 的字段——文生图没有互评阶段，也不触发 `save_chat_history`，因此响应体也没有 `history_id`。

### Firestore 用户文档结构（`users` 集合）

```python
{
    'username': str,          # 用户名，唯一索引字段
    'email': str,             # 邮箱，唯一索引字段
    'password_hash': str,     # Werkzeug generate_password_hash 生成的哈希值
    'created_at': Timestamp   # Firestore SERVER_TIMESTAMP
}
```

文档 ID 由 Firestore 自动生成，存储为 `session['user_id']`。

### Firestore 对话历史文档结构（`history` 集合，2026-07-02 新增）

```python
{
    'user_id': str,           # 所属用户的 Firestore 文档 ID（users 集合），归属校验字段
    'title': str,              # save_chat_history 写入时固定为 prompt[:15] + ('...' if len(prompt) > 15 else '')（2026-07-02 修复：短 prompt 不再被强行加省略号），支持后续手动重命名
    'prompt': str,              # 原始完整提示词
    'results': list,            # /api/compare 返回的完整聚合结果列表，严格保持现有 8-key result DTO 契约不变（含 peer_reviews）
    'created_at': Timestamp,    # Firestore SERVER_TIMESTAMP
    'is_pinned': bool,          # 默认 False，由 toggle_pin_chat_history 翻转
    'pinned_at': Timestamp,      # 仅置顶项存在（2026-07-02 新增）；置顶时写入 SERVER_TIMESTAMP，取消置顶时整个字段被 DELETE_FIELD 删除；新建文档（save_chat_history）不写入此字段
}
```

文档 ID 由 Firestore 自动生成。**游客（`is_guest`）的对话历史不写入此集合**，仅在前端内存中维护，随会话结束丢失；只有 `session['user_id']` 存在的已登录用户的历史才持久化。

### 对话历史 CRUD 函数契约（`auth/db.py`，2026-07-02 新增 5 个，2026-07-03 追加 `get_chat_history_by_id`）

| 函数 | 参数 | 成功返回 | Firebase 不可用 / 校验失败时返回 |
|---|---|---|---|
| `save_chat_history` | `user_id, prompt, results` | 含 `id` 键的完整历史 dict | `None`（仅 Firebase 不可用；无归属校验，因为是新建） |
| `get_chat_history_list` | `user_id, limit=20, offset=0` | 历史 dict 列表，排序规则：先按 `is_pinned` 分组（置顶在前），置顶组内按 `pinned_at` **升序**（最早置顶的排最前，2026-07-02 由 `created_at` 降序改为此规则，见下方 System Risks 条目），未置顶组内按 `created_at` 降序；每项含 `id` 键（排序与分页均在 Python 应用层完成，Firestore 查询本身只做单字段等值过滤，见下方 9. SYSTEM RISKS 的"复合索引"条目） | `[]` |
| `get_chat_history_by_id` | `user_id, history_id`（2026-07-03 新增，供只读 `/history/<id>` 详情页使用） | 含 `id` 键的完整历史 dict（单条，不分页不排序） | `None` |
| `delete_chat_history` | `user_id, history_id` | `True` | `False` |
| `update_chat_history_title` | `user_id, history_id, new_title` | `True` | `False` |
| `toggle_pin_chat_history` | `user_id, history_id` | 翻转后的 `is_pinned` 布尔值（`True`/`False`）；同步写入/清除 `pinned_at`（置顶时 `SERVER_TIMESTAMP`，取消置顶时 `DELETE_FIELD`），但该字段本身不在返回值里，只影响 Firestore 文档 | `None` |

`get_chat_history_by_id`、`delete_chat_history`、`update_chat_history_title`、`toggle_pin_chat_history` 四者返回 `None`/`False` 时无法区分"Firebase 不可用"和"文档不属于该用户/不存在"两种原因（均只 `logger.warning` 不抛异常），调用方若需要区分需自行先查询。这是有意简化：路由层目前只需要知道操作是否成功即可决定 HTTP 状态码/Flash 消息（`get_chat_history_by_id` 的调用方 `view_history()` 把 `None` 统一转换为"History entry not found" flash + 重定向）。

### Flask Session 键规范

```python
session['user_id']   # str：Firestore 文档 ID，已登录用户独有
session['username']  # str：用于前端展示，已登录用户独有
session['is_guest']  # bool：固定为 True，游客状态独有
```

三个键互斥：`user_id` 存在时 `is_guest` 必须已被清除；`is_guest` 存在时 `user_id` 必须不存在。

### 全局布尔标志

- `G4F_AVAILABLE`：g4f 库是否可用，决定 LLM 功能是否降级。
- `FIREBASE_AVAILABLE`：Firebase 是否初始化成功，决定认证功能是否可用。

## 8. 🔌 EXTERNAL INTERFACES

### 后端 API 接口规范（完整列表）

**LLM 聚合接口：**

- `GET /api/providers`：返回所有可用 Provider 的元数据列表。
- `POST /api/compare`：接收 `prompt`、`providers`、`model`、`max_workers`，返回并发测试后的聚合排序结果；响应体新增顶层字段 `history_id`（2026-07-02 新增，已登录且保存成功时为字符串，游客/匿名/保存失败时为 `null`）。
- `POST /api/test-single`：接收 `prompt`、`provider`、`model`，单独测试某一通道并返回标准 Result 对象。
- `GET /health`：健康检查，返回系统状态、`g4f_available`、Provider 列表、`image_providers`（2026-07-03 新增，`IMAGE_PROVIDERS` 名称列表）、`routing_rules_loaded`（`ROUTE_PROMPTS_MAP` 是否非空）、`peer_review_rules_loaded`（`PEER_REVIEW_PROMPTS_MAP` 是否非空）及时间戳。

**文生图聚合接口（2026-07-03 新增）：**

- `GET /api/image-providers`：返回所有可用文生图 Provider 的元数据列表（`{name, models, default_model, type: 'g4f_image', status}`），与 `/api/providers` 同构但读取独立的 `IMAGE_PROVIDERS`/`IMAGE_PROVIDER_MODELS_MAP`。
- `POST /api/generate-images`：接收 `prompt`、`providers`（可选，限定在 `IMAGE_PROVIDERS` 名字空间内，与 `G4F_PROVIDERS` 的文本 Provider 名字不通用）、`model`、`max_workers`，返回并发生成后的聚合排序结果；响应体不含 `history_id`（图片结果不持久化到对话历史）。每条 result 是 8-key 图片 DTO（见第 7 节），而非文本的 8-key `result + peer_reviews` 契约——两套 DTO 完全独立，字段名不通用（`response`/`peer_reviews` vs `url`/`b64_json`）。

**页面路由：**

- `GET /`：身份检查入口，渲染 `home.html` 或 `index.html`。
- `GET /home`：清除游客状态，重定向到 `/`（"返回欢迎页"专用路由）。
- `GET /history/<history_id>`（2026-07-03 新增）：只读历史详情页，渲染 `history.html`。匿名访客重定向到 `/`；已登录用户经 `get_chat_history_by_id` 校验归属后渲染，未找到时 flash + 重定向到 `/`；游客渲染空壳模板，由前端从 `sessionStorage` 自行按 id 查找，服务端不查询 Firestore。

**认证接口（auth Blueprint）：**

- `GET /login`：渲染登录页。
- `POST /login`：处理登录表单，验证密码哈希，建立 session。
- `GET /register`：渲染注册页。
- `POST /register`：处理注册表单，创建 Firestore 用户文档，建立 session。
- `GET /logout`：清除 session，重定向到 `/`。
- `GET /profile`：渲染个人资料页，要求 `session['user_id']` 存在。

**游客接口：**

- `POST /api/auth/guest`：设置 `session['is_guest'] = True`，返回 `{"status": "ok"}`。

**对话历史接口（2026-07-02 新增，均要求 `session['user_id']` 存在，否则 401）：**

- `GET /api/history?page=1&limit=20`：分页查询当前用户的历史记录，返回 `{"history": [...], "page": int, "limit": int}`；`limit` 上限 100，`page` 下限 1，非数字参数回退默认值。
- `PATCH /api/history/<history_id>/title`：接收 JSON `{"new_title": str}` 重命名；`new_title` 为空返回 400；目标不存在或不属于当前用户返回 404。
- `DELETE /api/history/<history_id>`：删除指定历史记录；不存在或不属于当前用户返回 404。
- `POST /api/history/<history_id>/toggle-pin`：翻转置顶状态，返回 `{"is_pinned": bool}`；不存在或不属于当前用户返回 404。

### 第三方集成

- **g4f 库**：通过模拟浏览器或逆向接口，无凭证调用各大免费 AI 渠道（如 `Yqcloud`、`OperaAria`、`PollinationsAI`）。
- **Firebase Admin SDK**：连接 Google Cloud Firestore，管理 `users` 集合的读写。本地开发使用 `firebase-key.json`，GAE 生产环境使用 Application Default Credentials（ADC）。

## 9. ⚠️ SYSTEM RISKS / CODE QUALITY AUDIT

- ~~**超时机制不一致**~~（**已修复**）：`future.result()` 的外层等待超时已从 `25` 秒调整为 `21` 秒，与内部 `g4f.ChatCompletion.create(timeout=20)` 保持一致，仅预留 1 秒线程调度缓冲。

- ~~**前端异常捕获漏洞**~~（**已修复**）：`index.html` 的 Fetch 处理逻辑已在调用 `response.json()` 前检查 `response.ok`。非 2xx 响应时先尝试解析 JSON `error` 字段，若 body 非 JSON 则回退到 `Server error: 状态码`，不再导致前端崩溃。

- ~~**异常回滚伪造**~~（**已修复**）：`compare_providers` 的 `except` 块已改为复用 `determine_actual_model()` 和 `init_result_object()` 两个辅助函数，模型决策规则与正常流程完全一致，key 集合严格统一。

- ~~**Flash 消息堆积 Bug**~~（**已修复**）：`home.html` 和 `index.html` 原本没有 Flash 消息显示区，导致退出登录等操作产生的 Flash 消息滞留 session，在下一个 auth 页面（使用 `auth/base.html`）上集中出现，引发"注册成功 + 已退出登录"同时显示的假象。两个文件均已加入 Flash 消息显示区。

- ~~**侧边栏空结果区时背景截断**~~（**已修复，2026-07-02 用户报告**）：`.app-layout` 原来只有 `display:flex; align-items:stretch;`，这只能让 `.left-sidebar` 与 `.main-content` 两者**互相**等高，不能让二者一起达到视口高度。结果区为空（未提交 Prompt）时 `.main-content` 只有表单那么高，`.left-sidebar` 的深色背景也就只延伸到表单底部，视口剩余部分露出页面浅色背景，视觉上呈现"黑色边栏被从下方截断"；提交 Prompt 出结果后 `.main-content` 变高，`.left-sidebar` 才跟着"延展"下去——这正是用户观察到的现象。修复为 `body` 改成 `display:flex; flex-direction:column;` 并给 `.app-layout` 加 `flex:1`，使其吃掉 `<nav>`/`.guest-banner` 之外的全部剩余高度，为空结果区场景提供一个视口高度的"地板"。由于 `body` 本身还叠了一个非标准的 `zoom: var(--page-zoom)`（此次修复发生时为 `0.8`，页面自带的 80% 缩放效果，与本次 Bug 无关但会干扰修复；`--page-zoom` 在 2026-07-03 同一天内先被统一调整为 `1.1`、随后又改回 `0.8`、再改为当前值 `0.88`（见 §4 `templates/index.html` 条目下"全局缩放比例统一调整为 110%"、"全局缩放比例改回 80%"与"全局缩放比例从 80% 调整为 88%"三条目），但 `calc(100vh / var(--page-zoom))` 这个公式本身对任意 zoom 值都成立，不受这些后续调整影响），`min-height` 必须写成 `calc(100vh / var(--page-zoom))` 而不能直接 `100vh`，否则缩放后仍会在视口底部留出空白（`vh` 按真实视口计算、不随 `zoom`放大，这是两者叠加时的已知陷阱）。此问题不涉及任何 Python 代码，纯 CSS 修复；由于 jsdom 不做真实布局渲染（无法产出高度数值），本次改用真实 headless Chromium（Playwright）测量渲染后的像素高度做验证，详见第 11 节。

- ~~**`get_chat_history_list` 缺失 Firestore 复合索引导致 500**~~（**已修复，2026-07-02 生产事故**）：原实现对 `history` 集合链式调用 `.where('user_id', '==', ...).order_by('is_pinned', ...).order_by('created_at', ...)`。这是一个 Firestore 复合查询（一个等值过滤 + 两个排序字段），Firestore 要求为其**手动**在控制台创建复合索引——该索引不属于代码库的一部分，也不会随部署自动创建，每个 Firebase 项目（本地、GAE 生产等）都需要各自单独创建一次。索引缺失时 `query.stream()` 直接抛出 `google.api_core.exceptions.FailedPrecondition: 400 The query requires an index`，导致 `GET /api/history` 恒定返回 500（前端表现为 Recents 侧栏卡在"Could not load history."，且 `/api/compare` 成功保存后侧栏也不会刷新，因为刷新同样要走这条 500 的查询）。修复方式：`get_chat_history_list` 现在只对 Firestore 发起单字段等值查询（`.where('user_id', '==', user_id)`，Firestore 对每个字段都有自动单字段索引，无需额外配置），取回该用户的全部历史记录后，排序（`is_pinned` 降序、`created_at` 降序）和分页（`offset`/`limit` 切片）改为在 Python 应用层完成。这样彻底消除了对手动创建复合索引这一外部环境配置步骤的依赖，新 Firebase 项目开箱即可用。代价是每次调用都会取回该用户的全部历史文档而非只取一页，但单用户对话历史量级很小，可忽略。

- ~~**Recents 短 prompt 标题被强行加省略号**~~（**已修复，2026-07-02 用户报告**）：`save_chat_history`（`auth/db.py`）与前端游客模拟逻辑（`templates/index.html`）原先都无条件执行 `prompt[:15] + '...'` / `prompt.slice(0, 15) + '...'`，导致像 `"hi"` 这种远短于 15 字符的 prompt 也被显示成 `"hi..."`，误导用户以为标题被截断。两处均已改为条件判断：仅当 `len(prompt) > 15` 时才追加省略号，否则标题就是 prompt 原文。

- ~~**重命名输入框文字不可读（浅灰字叠白底）**~~（**已修复，2026-07-02 用户报告**）：`.history-rename-input` 最初设计为深色主题（`color:#ececec` + `background:#262626`），但全局 `input:focus` 规则的选择器特异性 `(0,1,1)` 高于仅有单一类选择器的 `.history-rename-input` 的 `(0,1,0)`，聚焦时（`startInlineRename` 创建后立即 `input.focus()`）背景被强制改为白色而文字仍是浅灰，导致输入框视觉上一片空白难以辨认文字。修复为直接改用浅色配色（`color:#1a1a1a` + `background:#ffffff`），并新增特异性 `(0,2,0)` 的 `.history-rename-input:focus` 规则显式锁定同样配色，确保稳赢全局 `input:focus`。

- ~~**Pin 置顶后条目未移动到 Recents 顶部**~~（**已修复，2026-07-02 用户报告，第一轮**）：`toggleHistoryPin` 只翻转 `item.is_pinned` 字段就调用 `renderCurrentHistory()`，但渲染函数直接对 `currentHistoryItems` 做 `groupHistoryByDate`——分组只按日期落桶，桶内顺序沿用数组原有顺序，从不因 `is_pinned` 变化而重排，因此图标切换成"已固定"样式后条目仍停在原位，用户观察到"闪烁两下但没真的置顶"。修复为新增 `sortHistoryItems(items)`，在分组前对**拷贝**按 `is_pinned` 降序重新排序，不改变 `currentHistoryItems`/`window.guestHistory` 自身的元素顺序（分页/回滚逻辑仍依赖原始 index）。

- ~~**第二个 Pin 会顶替第一个 Pin 的位置**~~（**已修复，2026-07-02 用户报告，第二轮**）：第一轮修复只解决了"置顶后完全不移动"的问题，但当时置顶组内部仍沿用未置顶组的排序字段 `created_at` 降序——这意味着置顶区内谁排最上面，取决于**对话创建时间**而不是**用户点击 Pin 的先后顺序**：先置顶一个较早创建的对话，再置顶一个创建时间更新的对话，后者会因为 `created_at` 更大反而排到前者上面，表现为"第二个 Pin 顶替了第一个"。修复为引入 `pinned_at` 字段（专门记录"这次置顶操作发生的时刻"，与 `created_at` 语义分离）：后端 `toggle_pin_chat_history` 置顶时写入 `firestore.SERVER_TIMESTAMP`、取消置顶时用 `firestore.DELETE_FIELD` 删除该字段；`get_chat_history_list` 的排序改为置顶组内按 `pinned_at` 升序（最早置顶的排最前）；前端 `toggleHistoryPin` 同步在本地记录 `item.pinned_at`（客户端时间戳，用于本次会话内的乐观排序），`sortHistoryItems` 同样按 `pinned_at` 升序排置顶组。此后第一个被置顶的对话会稳定停留在最上面，后续每次新置顶依次排在其下方，仿照 Gemini 网页版的置顶行为。

- ~~**Pin/Unpin 无图标区分，必须 hover 才能看出置顶状态**~~（**已修复，2026-07-02 用户报告，仿照 Gemini 网页版**）：修复前 Pin 按钮无论置顶与否都待在同一个 hover-only 的 `.history-item-actions` 组里，仅靠 `.is-pinned` class 变色区分——但整个组默认 `opacity:0`，鼠标移开后已置顶条目和未置顶条目在视觉上完全一样，"图标区分"形同虚设。修复为新增 `.pin-indicator`：已置顶条目改渲染一个独立于 hover 分组之外、`opacity:1` 恒定可见的绿色大头针按钮，固定停在行最右侧，不需要 hover 即可看到置顶状态，再次点击它触发取消置顶；未置顶条目保持原有的 hover-only 灰色 Pin 按钮。两种状态互斥渲染（`renderHistoryItem` 依据 `isPinned` 二选一），不会同一条目同时出现两个 Pin 控件。

- ~~**点击 Pin 后条目快速闪烁两下**~~（**已修复，2026-07-02 用户报告**）：`toggleHistoryPin` 的乐观更新会立即 `renderCurrentHistory()`（整体替换 `#sidebarRecents.innerHTML`，重建所有 DOM 节点）一次；修复前，`/api/history/<id>/toggle-pin` 请求成功返回后，`.then()` 回调只要 `data.is_pinned` 是布尔值就**无条件**再 `renderCurrentHistory()` 一次——该请求通常几十毫秒内就返回，且 `data.is_pinned` 几乎总是和刚设置的乐观值相同，等于短时间内把整个列表的 DOM 完全重建两遍，每次重建后 `.history-item-actions` 的 `opacity 0.15s` 过渡都要重新走一遍淡入动画，肉眼可见"闪烁两下"。修复为只有当 `data.is_pinned !== item.is_pinned`（服务端真实状态与本地乐观值确实不一致，例如另一个标签页并发操作了同一条目）时才触发这次确认性重渲染。

- ~~**点击 Recents 条目不会加载对应的历史快照**~~（**已修复，2026-07-03 用户报告；同日晚些时候这套"内联加载"的实现方式本身又被下面的"历史只读分离"条目取代**）：侧边栏 Recents 列表此前只支持 pin/rename/delete 三个悬浮图标的操作，点击条目本身（标题或行内空白处）没有绑定任何行为——用户点开一条历史记录，页面上的 Prompt 输入框和结果区完全没有反应，看起来像是"点了没用"。当时修复为在 `#sidebarRecents` 既有的委托 `click` 监听器里新增一个 fallback 分支：点击目标未命中 `[data-action]` 图标、也未命中重命名中的 `.history-rename-input` 时，调用 `loadHistorySnapshot(id)`，把 `#prompt` 替换为该条目的原始 `prompt` 字段、把结果区整体替换为该条目保存时的 `results` 快照。这一实现方式（把历史快照加载进 `index.html` 自己可编辑的表单里）后来被证实存在"容易被误编辑后重新提交"的风险，已被下面的"历史只读分离"条目整体取代——`loadHistorySnapshot` 函数已不存在，改为整页导航到独立的 `templates/history.html`。

- ~~**Recents 隐藏的显示上限：新对话增多后更早的记录（含"昨天"）从可见范围消失**~~（**已修复，2026-07-03 用户报告**）：侧边栏首次加载时只请求过一次 `/api/history?page=1&limit=20`，且没有任何 UI/逻辑允许请求更多页。`get_chat_history_list` 的排序规则是"置顶优先，未置顶按 `created_at` 降序"，因此当天新增的对话越来越多、总数超过 20 条后，第一页天然全是最新的对话，用户之前看到的"昨天"甚至"今天早些时候"的记录并没有被删除或损坏，只是永远排在第 21 条之后、而侧边栏又从未请求过第 2 页，表现为"随着请求越来越多，之前的记录消失了"。修复为侧边栏新增 ChatGPT 风格的懒加载分页：`.sidebar-recents` 滚动到距底部 48px 内时触发 `loadMoreSidebarHistory()`，用递增的 `page` 参数继续请求同一个既有的 `/api/history` 端点并把结果追加（`.concat()`）到 `currentHistoryItems`，直至某一页返回条目数不足 `limit`（记为 `hasMoreHistory = false`）。同时给 `.sidebar-recents` 加了明显的深色细滚动条样式（`scrollbar-width: thin` + `::-webkit-scrollbar*`），提示"这里可以往下滚"。纯前端改动，未涉及任何后端 API 契约变更（`/api/history?page=&limit=` 参数早已存在，只是此前从未被 `page > 1` 调用过）；游客的 `window.guestHistory` 本就是当前会话的完整数组，不参与这套分页（`hasMoreHistory` 对游客恒为 `false`）。详见第 4 节 `templates/index.html` 条目下的"Recents 懒加载分页"说明。

- ~~**上一条修复本身没有真正生效：Recents 分页/滚动条依然不出现（2026-07-03 用户当天复测后再次报告）**~~（**已修复**）：上一条描述的懒加载分页 JS 逻辑完全正确，但从未被触发过——根因是 `.left-sidebar` 一直没有自己的高度，只是被 `.app-layout` 的 `align-items: stretch` 拉伸到与 `.main-content` 相同的高度；只要历史条目足够多，`#sidebarRecents`（`flex:1` 子元素）自身的内容高度就会成为这次拉伸计算里的最大值，于是 `.left-sidebar` 被动"长"到刚好装得下全部条目，`#sidebarRecents` 因此从未真正溢出过（`scrollHeight === clientHeight` 恒成立），`overflow-y:auto` 不生效、绑定的 `scroll` 监听器也永远不会触发，`loadMoreSidebarHistory()` 从一开始就是不可达代码。真正的修复是让 `.left-sidebar` 拥有独立于 `.main-content` 的、视口绑定的固定高度：`.left-sidebar` 新增 `position: sticky; top: var(--sidebar-sticky-top, 0px); height: var(--sidebar-sticky-height, calc(100vh / var(--page-zoom))); align-self: flex-start;`，`#sidebarRecents` 新增 `min-height: 0;`（消除 flex 子元素默认按内容撑开最小高度、与 `overflow-y:auto` 冲突的经典陷阱）。用 `position: sticky` 而非普通定高，是为了不引入新的截断回归——结果区异常长时深色背景仍要延伸到视口底部（这条不变量见下方"侧边栏空结果区背景截断"条目）。`top`/`height` 由新增的 `syncSidebarStickyMetrics()`（在 `.navbar`/可选的 `.guest-banner` 渲染完成后测量真实高度，写入两个 CSS 自定义属性，登录态/游客/移动端下值都不同，且要按 `--page-zoom` 反向缩放，详见第 4 节 `templates/index.html` 对应条目）在页面加载与 `window.resize` 时同步。`templates/history.html` 的 `<style>`/`<script>` 是 `index.html` 的逐字复制，因此这三处改动在两个文件里各自独立地都改了一份。纯 CSS+JS 修复，未改动任何 Python 代码。详见第 4 节 `templates/index.html`/`templates/history.html` 条目下的对应说明，以及第 11 节的验证方法记录。

- ~~**滚动/溢出机制已生效，但滚动条本身在 Chrome 标签页里依然不可见（2026-07-03 用户当天第三次报告，紧接上一条）**~~（**已修复**）：上一条修复已经让 `#sidebarRecents` 真正产生内部溢出（鼠标滚轮在该区域内滚动确实生效），但用户反馈"Chrome 标签页里还是看不到滚动条，VS Code 内置预览里却正常"。根因是 Chrome 的原生 overlay（悬浮式）滚动条渲染模式——不占用布局空间，仅在主动滚动/悬停时短暂淡入后自动淡出，静态标签页里必然看不到；VS Code 内置 webview 默认用经典（始终保留轨道宽度）滚动条渲染，因此同一份 CSS 观感不同。实测证实自定义 `::-webkit-scrollbar` 样式并未能压制这个 overlay 行为（`#sidebarRecents.offsetWidth === #sidebarRecents.clientWidth`，即滚动条实际未占用任何布局宽度）——这是较新 Chromium 版本收紧非标准滚动条覆盖能力后的已知限制。第一层修复（轨道背景从 `transparent` 改为半透明、thumb 透明度/宽度提升）保留作为 Firefox 等仍支持标准滚动条属性的浏览器的加成，但不足以解决 Chrome 的问题。真正的修复是新增一个完全自定义、JS 驱动、不依赖浏览器原生滚动条渲染的视觉指示器：`#sidebarRecentsScrollThumb`（`.left-sidebar` 内、`#sidebarRecents` 的兄弟节点）+ 新增 JS 函数 `updateSidebarScrollIndicator()`，按 `#sidebarRecents` 的滚动比例实时计算并写入该指示器的 `top`/`height`，在页面加载、`window.resize`、`#sidebarRecents` 自身的 `scroll` 事件、以及 `renderCurrentHistory()`（初次渲染/分页追加/pin-rename-delete 重排）末尾都会重新计算。实现过程中一度踩中本文件反复出现的 `zoom` 二次缩放陷阱（`getBoundingClientRect()` 返回已缩放的物理像素，直接赋给 `style.top`/`style.height` 会被渲染时的 `zoom` 再乘一次），已按 `syncSidebarStickyMetrics()` 的同款手法（除以 `--page-zoom`）修正，指示器与 `#sidebarRecents` 实际边界的误差降到 1px 以内。`templates/history.html` 逐字同步了一份。纯 CSS+JS 修复，未改动任何 Python 代码。详见第 4 节 `templates/index.html` 条目下对应说明，以及第 11 节的验证方法记录。

- ~~**点击历史条目会把它加载进可编辑的对比表单，存在被误编辑后重新提交产生错误的风险**~~（**已修复，2026-07-03 用户报告**）：`loadHistorySnapshot(id)` 把历史 prompt/结果渲染进 `index.html` 自己的 `#prompt`/`#results`，但 Provider 勾选框、Model 下拉、Compare/Clear 按钮这套可交互表单原样留在下方，容易让用户误以为可以编辑这条"历史" prompt 并重新提交，实际上那只是拿这段文字发起一次全新的、与原记录无关的对比，且这套可编辑 UI 包围一条本该只读的历史记录本身就不符合预期。修复为新增独立的只读页面 `templates/history.html`（新路由 `GET /history/<history_id>`，`main.py` 的 `view_history()` + `auth/db.py` 新函数 `get_chat_history_by_id`），物理上不包含任何表单元素（无 Provider 勾选、无 Model 下拉、无 Compare/Clear 按钮）；点击 Recents 条目从"内联加载快照"改为整页导航（`openHistoryEntry(id)`，`window.location.href = '/history/' + id`）。由于导航会销毁当前 `window`，游客的 `window.guestHistory` 相应改为持续镜像进 `sessionStorage`（见下方 `persistGuestHistory()` 说明），否则整页导航会把游客的历史记录清空。详见第 4 节 `templates/history.html` 专属条目。

- ~~**互评阶段对 Yqcloud 等较慢 Provider 频繁误判超时**~~（**已修复，2026-07-03 用户报告**）：修复前 `run_peer_review` 内部 `g4f.ChatCompletion.create` 的 advisory `timeout=15`、外层 `future.result(timeout=25)`。互评 prompt 远长于普通请求（额外拼接了被评价者的完整回答文本），比首轮直接提问更容易超过 15s 的 advisory 上限；一旦 g4f 内部因 advisory 超时抛出异常，`run_peer_review` 只在命中 429/queue 关键词时才重试，纯粹的耗时超限不会重试，直接进入兜底文案分支或被外层 25s 硬截断打断，日志表现为 `Peer review for <provider> timed out after 25s`，用户报告 Yqcloud 经常在互评阶段拿不到分数/点评。修复为把内部 advisory 上调到 `timeout=25`、外层 `future.result` 同步上调到 `timeout=32`（约 7 秒调度缓冲）。**429 重试时序**同步重新核算：429 通常在 0.1s 内即返回，重试等待 2-3s，第二次尝试的 advisory 上限是新的 25s，两次尝试总耗时上限约 2-3s + 25s ≈ 28s，仍安全在新的 32s outer timeout 以内。纯 Python 改动，`TestPeerReviewOuterTimeoutValue`（`test_main_graybox.py`，2026-07-03 新增）直接 spy `concurrent.futures.Future.result` 断言互评阶段确实以 `timeout=32` 调用（且不再出现旧值 `25`），另配一个 `TimeoutError` 场景验证单条互评超时不影响其余结果的 200 返回。

- ~~**Provider 底层内容审查（如 Azure OpenAI 内容过滤）报出裸异常文本**~~（**已修复，2026-07-03 用户报告**）：修复前 `PollinationsAI`（`openai-fast`，底层走 Azure OpenAI）遇到内容审查拦截时，`g4f.ChatCompletion.create` 抛出的原始异常（`Error 400: ... azure-openai error: The response was filtered due to the prompt triggering Azure OpenAI's content management policy...`）未命中 `NETWORK_ERROR_KEYWORDS` 中任何关键词，落入 `else` 分支被原样透传给前端，用户直接看到一段带内部实现细节（Azure、BAD_REQUEST、文档链接）的裸英文报错，且这类错误重试没有意义（同一 prompt 大概率再次被拦截）。修复为新增模块级常量 `CONTENT_POLICY_ERROR_KEYWORDS`（`content management policy`、`content_filter`、`content filtering polic`、`response was filtered`、`responsible ai`），`test_g4f_provider` 和 `run_peer_review` 的异常分支均**优先**检查此列表（早于网络类关键词判定），命中时分别返回 `"This provider's content filter blocked the response. Try rephrasing your prompt."` / `"This provider's content filter blocked the review. Try rephrasing your prompt."`，不再暴露 Azure/g4f 的内部实现细节，也不再误导用户"重试"一个注定被拦截的请求。纯 Python 改动，白盒新增 4 个用例覆盖友好文案本身、内容策略关键词与网络关键词同时出现时的优先级判定、以及互评路径下重试逻辑不会因内容策略错误而被触发（详见 §11）。

- ~~**Model 自绘下拉框点击切换选项时黑色高亮瞬间跳变**~~（**已修复，2026-07-03 用户报告**）：`templates/index.html` 的 Model 自定义下拉框（`#customSelectWrapper`/`#customOptions`）点击某个 `.custom-option` 时，原实现同步做了两件事：把黑色高亮（`.custom-option.selected { background-color:#09110e }`）从旧选项立即切换到新选项，同时让下拉面板开始 0.15s 的收起过渡动画——面板尚未完全隐藏时高亮就已经跳变完毕，用户能看到黑色背景在选项间快速跳动后面板才淡出，视觉上显得紊乱。修复为新增 `syncSelectedOptionHighlight()`，把 `.selected` class 的重新计算从"点击时同步执行"改为"用 `setTimeout(..., 150)` 延迟到面板收起动画结束后才执行"（`150` 与 `.custom-options` 的 CSS `transition: all 0.15s ease` 时长对齐），点击时只立即更新功能性状态（`customTrigger.textContent`/`modelSelect.value`/关闭面板），高亮变更这时用户已经看不到面板了。新增的 `modelHighlightTimer` 计时器在"重新打开下拉框"（会立即同步一次高亮并清理旧计时器，处理选中后 150ms 内又重新打开的竞态）和 `updateModelDropdown()` 重建选项列表（旧计时器可能指向已被替换的节点）两处会被清理。纯前端修复，未改动任何 Python 代码；用真实 headless Chromium（Playwright）验证了点击瞬间高亮仍停留旧选项、面板已同步开始收起、延迟窗口后高亮才切换到新选项，以及"选中后立刻重新打开"场景下高亮不会出现二次跳变。详见第 4 节 `templates/index.html` 条目下的对应说明。

- ~~**加载历史快照后点击 Clear 导致整页水平位移**~~（**已修复，2026-07-03 用户报告，修复方案当天经历一次调整**）：`html`/`body` 此前均未声明任何 `overflow`，滚动条是否出现完全由内容是否溢出决定。点击 Recents 里一条内容较长的历史记录会让 `#resultsContainer` 撑高文档、触发纵向滚动条出现，浏览器为滚动条让出轨道宽度，`.nav-container`/`.container` 这类 `max-width` + `margin:0 auto` 居中的元素可用宽度变窄、整体左移几像素；随后点击 Clear Results/+ New Chat 隐藏结果区，文档变矮，滚动条消失，两者又整体右移回原位——这一来一回就是用户报告的"点了 Clear 之后页面整体往右挪了一点"。第一版修复新增 `html { scrollbar-gutter: stable; }`，让浏览器无论当前是否真的溢出都始终为纵向滚动条预留同一份轨道空间，`clientWidth` 确实因此保持恒定，**但这个属性只做静默预留、不绘制任何东西**——内容不需要滚动条时那段预留空间就是空白，导致贴着视口右边缘的 `.navbar`（白色通栏）被这段空白往左挤出一条缝隙，露出下面的背景色，看起来像"导航栏白色没有铺满"（另一条独立 Bug，见下方"导航栏白色背景右侧出现缺口"条目）。最终改为经典写法 `html { overflow-y: scroll; }`：同样始终预留固定宽度、同样能防止水平位移，但这段空间里画的是一个真实的滚动条控件而非空白，天然不会产生"背景没铺满"的观感。纯 CSS 修复，与 `body` 上非标准的 `zoom: var(--page-zoom)` 缩放无关（该规则在真实视口宽度上生效，不需要参与 `--page-zoom` 的 `calc()`）；用真实 headless Chromium（Playwright）注入超长 guest 历史记录验证 `clientWidth` 在三个关键时间点（初始、加载长结果后、Clear 后）恒定不变，且 `.container`/`.nav-container` 的 `boundingBox().x` 在 Clear 前后完全一致，同时验证 `.navbar`/`.guest-banner`/`.app-layout` 的宽度始终等于 `clientWidth`（不留缺口）。详见第 4 节 `templates/index.html` 条目下的对应说明。

- ~~**导航栏白色背景右侧出现缺口**~~（**已修复，2026-07-03 用户报告，为上一条 Bug 修复方案的直接回归**）：上一条"整页水平位移"Bug 的第一版修复用 `scrollbar-gutter: stable` 让浏览器始终为纵向滚动条预留固定宽度的轨道空间，但该属性只做静默预留、不实际绘制滚动条——当页面内容矮于视口、当前其实不需要滚动条时（例如刚打开页面、结果区为空的最常见状态），那段被预留的宽度就是一段纯粹空白，露出 `html`/`body` 的背景色（`rgba(0,0,0,0)`/透明，实际呈现取决于浏览器画布默认色），而紧贴视口右边缘、背景纯白的 `.navbar`、`.guest-banner`、`.app-layout` 这几个通栏元素的实际渲染宽度会被这段预留空间挤窄（用 Playwright 实测：`viewport 1400px` 下 `.navbar` 的 `getBoundingClientRect().width` 只有 `1385px`，短了 `15px`，且 `document.documentElement.clientWidth` 仍报告满宽 `1400px`——即 `clientWidth` 本身不反映这段预留，但依赖 `width:auto`/`100%` 布局的通栏元素的可用宽度确实被压缩了），视觉上呈现为"顶部导航栏右侧白色没有铺满，右边空缺出一块"。修复为把 `html` 的 `scrollbar-gutter: stable` 换成 `overflow-y: scroll`（详见上一条"整页水平位移"Bug 条目里的完整对比）：两者预留的空间宽度相同、同样能防止水平位移，区别在于 `overflow-y: scroll` 会让浏览器在没有内容溢出时也渲染一个真实的滚动条控件占满这段空间，而不是留白，因此不再产生这条视觉裂缝。纯 CSS 修复，未改动任何 Python/JS 代码，未新增/删除任何 DOM 节点或 class 名；用真实 headless Chromium（Playwright）在 400/1400/1920 三种视口宽度下验证 `.navbar`/`.guest-banner`/`.app-layout` 的 `getBoundingClientRect().width` 均等于 `document.documentElement.clientWidth`（无缺口），并截图核对视觉效果。

- ~~**导航栏与两栏布局（侧边栏 + 主内容区）对不上，logo 与视口宽度绑定、和侧边栏毫无对齐关系**~~（**已修复，2026-07-03 用户报告**）：修复前 `.nav-container` 用 `max-width:1200px; margin:0 auto` 把自己box 成一条独立于下方 `.app-layout`（260px 深色侧边栏 + flex:1 主内容区，边到边铺满视口）之外、按视口宽度单独居中的窄条，logo 水平位置纯粹取决于当前浏览器宽度，与侧边栏没有任何对齐关系；`.nav-links`（Welcome/Profile/Logout 等）在宽屏上也常年停在 1200px 上限内侧，右侧留有大片与视口真实边缘脱节的空白。修复为 `.nav-container` 去掉 `max-width`/`margin:auto` 改为边到边铺满、`.nav-left` 新增 `width:260px; flex-shrink:0; justify-content:center`（与 `.left-sidebar` 同宽且同样从 `x=0` 起算，令 logo 精确居中在侧边栏正上方），`.nav-links` 因此自然贴近视口右侧（仅留 2rem 呼吸间距）。移动端（`<=520px`，侧边栏退化为浮层抽屉）新增 `.nav-left { width:auto; flex-shrink:initial; justify-content:flex-start; }` 覆盖桌面端定宽。纯 CSS 微调，未新增/删除任何 DOM 节点或 class 名，未改动任何 Python 代码；用真实 headless Chromium 测得 `.left-sidebar` 与 `.nav-logo` 的水平中心点完全重合（diff = 0px），400×800 移动视口下汉堡按钮 + logo 左对齐、`.nav-links` 仍正常靠右。详见第 4 节 `templates/index.html` 条目下的对应说明。

- ~~**`history.html` 顶部说明文案挤在一行、Prompt 展示区下方空白明显大于上方**~~（**已修复，2026-07-03 用户报告**）：`.header` 下方的说明文案原是一整句 `Read-only view of a saved comparison. Nothing here can be edited or resubmitted.`，未分行显示；`#promptDisplayContainer` 内唯一的 `.form-group` 沿用了 `index.html` 里为"多个 `.form-group` 依次堆叠"设计的 `margin-bottom:26px`，但本页该元素后面没有任何兄弟节点，这份下边距单纯叠加在容器自身的 36px 底部 `padding` 上，导致 Prompt 文本框下方空白明显宽于上方（label 到容器顶边只有 36px）。修复为说明文案用 `<br>` 拆成两行（与 `home.html` 既有的两行欢迎语写法一致，`.header p` 同步补上 `line-height:1.5`，`index.html` 同款规则同步补齐以保持两份 `<style>` 一致），并新增 `#promptDisplayContainer .form-group { margin-bottom: 0; }` 让容器上下空白都收敛为 36px。纯 CSS/文案修复，未改动任何 Python 代码。
- ~~**认证页面标题内联样式与全站排版体系脱节**~~（**已修复，2026-07-03 用户要求的 UI 比例统一审计发现**）：`login.html`/`register.html`/`profile.html` 的页面标题 `<h2>` 此前各自内联 `style="color: #333; margin-bottom: 2rem;"`——颜色 `#333` 和未设置的字重/字距，都没有对齐全站统一使用的 `color:#09110e` + `font-weight:800` + 负字距排版体系（`.welcome-card h1`、`.header h1`、`.user-info h3` 均遵循这套体系），视觉上这三处标题比其余标题更细、更灰。修复为 `auth/base.html` 新增 `.card-title` 复用类（`color:#09110e; font-size:1.6rem; font-weight:800; letter-spacing:-0.03em; margin-bottom:28px;`），三个页面的 `<h2>` 改用 `class="card-title"`，不再有任何内联样式。纯 CSS/标记修复，未改动任何 Python 代码。

- ~~**Recents 侧边栏出现 2 个 scrollbar；自绘滚动指示器需要浏览器缩放到约 110% 才可见；150% 缩放下滚到页面底部时侧边栏黑色背景露出空缺**~~（**已修复，2026-07-03 用户报告，紧接前几轮 Recents 滚动条相关修复**）：三个问题共享/交叉两个根因。(1) **2 个 scrollbar**：`#sidebarRecents` 此前仍保留了原生滚动条（只是外观被定制、Chrome 判定为默认隐藏的 overlay 模式），一旦用户主动滚动/悬停，Chrome 会短暂淡入这个原生滚动条，与恒定可见的自绘指示器 `#sidebarRecentsScrollThumb` 同时出现；而且只要原生滚动条还占据任何可交互区域，它必然盖在页面内容之上抢先拦截 `mousedown`，自绘指示器就永远做不到"可拖拽"。修复为彻底隐藏原生滚动条（`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`，恒为 0 宽度）并新增 `setupThumbDrag()` 让 `#sidebarRecentsScrollThumb` 真正可拖拽。(2)(3) **需 110% 缩放才可见 / 150% 缩放滚到底部露出空缺**：两者同一根因——`.left-sidebar` 的高度此前由 JS 手工计算 `(window.innerHeight - realHeaderHeight) / zoom` 后注入字面量像素值，`window.innerHeight` 是 JS 侧取整后的整数，与浏览器自身缩放叠加后产生的舍入误差会让算出的高度在不同缩放级别下有细微出入——算多了会让"是否溢出"被误判为"未溢出"从而隐藏指示器（对应"需 110% 才可见"），算少了则会让侧边栏实际渲染高度不够、滚到长页面底部时露出缝隙（对应"150% 缩放露出空缺"）。修复为 `.left-sidebar` 的 `height` 改用纯 CSS `calc(100vh / var(--page-zoom) - var(--sidebar-header-height) + 2px)`，`100vh` 的解析交给浏览器原生完成而非 JS 侧的 `window.innerHeight` 取整数值，`syncSidebarStickyMetrics()` 相应简化为只测量并写入 `--sidebar-header-height` 一个变量；`+2px` 安全余量让任何残余的亚像素误差稳定偏向"侧边栏略高"而非"露出缝隙"。整页的原生滚动条（`html` 上的 `overflow-y:scroll`）同步替换为同样彻底隐藏 + 新增的自绘可拖拽指示器 `#pageScrollThumb`/`updatePageScrollIndicator()`（详见下一条"用户要求的整页滚动条同类整改"）。纯 CSS+JS 修复，未改动任何 Python 代码；用真实 headless Chromium（Playwright）验证两处原生滚动条实测占用宽度均为 0、两个自绘指示器无需任何交互即可见、拖拽指示器确实分别驱动 `#sidebarRecents.scrollTop`/`window.scrollY` 变化、模拟更高 `device_scale_factor` 后滚动到底部测得 `.left-sidebar` 底边与视口底边差值仅 0.42px。294 个既有 Python 测试全部保持通过。详见第 4 节 `templates/index.html` 条目下的对应说明。

- ~~**整页右侧的原生滚动条未随 Recents 侧边栏同步整改（"同样的，整体右侧的 scrollbar 也要进行类似修改"）**~~（**已修复，2026-07-03 用户要求**）：`html` 上原本的 `overflow-y: scroll`（详见上方"消除加载历史快照后再 Clear 导致的整页水平位移"条目）只做了"恒定占用某个非零宽度防止水平位移"这一件事，没有解决与 Recents 侧边栏同源的"原生滚动条天然盖在页面内容之上、自绘指示器无法真正可拖拽"的问题（当时整页还没有任何自绘指示器）。修复为彻底隐藏整页的原生滚动条（`-ms-overflow-style:none; scrollbar-width:none;` + `html::-webkit-scrollbar{display:none}`，在"是否溢出"两种状态下都恒为 0 宽度），新增 `#pageScrollThumb`（`.page-scroll-indicator`，深色半透明配色，适配浅色主页面背景）+ `updatePageScrollIndicator()`（与 `updateSidebarScrollIndicator()` 同构，只是把 `#sidebarRecents` 换成 `window`/`document.documentElement`）+ 复用 `setupThumbDrag()`。恒为 0 宽度同样满足"两种状态下宽度不变、不引发水平位移"这个原规则真正的目的，因此不会重新引入更早修复过的水平位移 Bug（已用 Playwright 复测确认）。触发重算的时机为页面加载、`window.resize`、`window.scroll`，以及挂在 `document.body` 上的 `ResizeObserver`（覆盖任何改变文档渲染高度的场景，无需逐一在每个调用点手动插入刷新调用）。纯 CSS+JS 修复，未改动任何 Python 代码。详见第 4 节 `templates/index.html` 条目下的对应说明。

- **互评阶段双层保护**：互评阶段采用两层独立 try-except。外层兜住整个阶段（任务构建崩溃、Executor 初始化失败等），内层兜住单条 `future.result(timeout=32)`（2026-07-03 由 `25` 上调，见上方"互评阶段对 Yqcloud 等较慢 Provider 频繁误判超时"条目）的超时或执行异常。任何一层失败均不影响第一轮 LLM 结果返回，`peer_reviews` 字段在互评 try 块之前已初始化为 `[]`。`run_peer_review` 内部 `g4f.ChatCompletion.create(timeout=25)`（2026-07-03 由 `15` 上调）为 advisory 超时（非硬截断）；外层硬截断留有约 7 秒调度缓冲。互评 prompt 远长于普通请求（含完整回答文本），若再次调整任一超时值须同步评估另一侧的缓冲余量。**429 重试时序**：429 通常在 0.1s 内即返回，重试等待 2-3s，第一个并发请求通常已完成，因此第二次尝试几乎不会再遇到队列满。两次尝试总耗时上限约 2-3s + 25s ≈ 28s，安全在新的 32s outer timeout 以内。

- **线程池潜在安全隐患**：`max_workers` 的计算逻辑为 `min(data.get('max_workers', 3), 5)`。`G4F_PROVIDERS` 目前有 3 个（`Yqcloud`、`OperaAria`、`PollinationsAI`），实际最大线程数不超过 3。

- **SECRET_KEY 持久化风险**：若 `SECRET_KEY` 环境变量未设置，每次服务重启都会生成新的随机密钥，导致所有已登录用户的 session 失效。生产环境必须在 `app.yaml` 的 `env_variables` 中固定设置 `SECRET_KEY`，本地开发必须在 `.env` 文件中设置。

- **Firebase 凭据惰性解析**：`credentials.ApplicationDefault()` 的构造函数不立即验证凭据，凭据解析在 `firestore.client()` 时才真正触发。因此 `auth/db.py` 必须优先检测 `firebase-key.json` 文件，而不能依赖 `ApplicationDefault()` 构造函数的异常作为本地开发的 fallback 信号。

## 10. 🧭 EXTENSION & MODIFICATION GUIDE (VERY IMPORTANT)

### 🟢 安全区（Safe Zones）：如何安全地添加新 LLM 通道

若需要引入新的 g4f 支持的 Provider，只需修改 `main.py` 的初始化部分：

1. 先运行 `availability_g4f/find_providers_models.py` 扫描当前 g4f 库中 `working=True` 且 `needs_auth=False` 的 Provider，再更新 `availability_g4f/test_providers.py` 加入候选条目，运行后取成功的 Provider。（参考：PollinationsAI `openai-fast` 于 2026-06-28 经第三轮测试验证通过）
2. 将验证通过的 Provider 追加进 `G4F_PROVIDERS` 列表中。
3. 在 `PROVIDER_MODELS_MAP` 中添加对应的模型数组（第一个为默认模型）。
4. 在 `ROUTE_PROMPTS_MAP` 中为 `(provider_name, model)` 元组添加隐形风格提示词（可选）。
5. 在 `PEER_REVIEW_PROMPTS_MAP` 中为该 Provider 使用的模型名称添加互评裁判提示词（可选；缺失时使用默认值 `'Please evaluate the quality of the following answer, noting its strengths and weaknesses.'`，2026-07-02 起该默认值已由中文改为英文）。新增条目必须要求模型输出 `{"score": int, "comment": str}` JSON 格式，否则 `parse_peer_review_json` 将 fallback 为 80 分；新增条目也应使用英文撰写，与表内既有条目保持语言一致。
6. 前端具备完全动态的联动机制，无需修改任何 HTML/JS 代码。

### 🟢 已落地：文生图（text-to-image）聚合功能（2026-07-03）

2026-07-03 完成 g4f 免 Key 文生图 Provider 的可用性调研（见第 4 节 `availability_g4f/` 条目）后，同日晚些时候已将其**正式落地为产品功能**，不再只是调研文档：

1. 复用调研确认的 5 个免 Key 组合，全部接入 `IMAGE_PROVIDERS`/`IMAGE_PROVIDER_MODELS_MAP`（`main.py`）：`PollinationsImage`（不传 model）、`BlackForestLabs_Flux1Dev`（model=`flux-dev`）、`AnyProvider`（model=`flux`）、`StabilityAI_SD35Large`（model=`sd-3.5-large`）、`OperaAria`（model=`aria`）。调研文档里"`AnyProvider`+`flux` 命中的后端与 `BlackForestLabs_Flux1Dev` 相同、不建议重复接入"的提醒依然成立——两者会各自发起一次独立请求、大概率返回相似的图，这是已知的、故意保留的行为（用户在 UI 上可以只勾选其中一个，本次未强制互斥），而不是 Bug。
2. 图片生成走 `G4FImageClient()`（即 `from g4f.client import Client as G4FImageClient`）的 `.images.generate(prompt=, model=, provider=)`，与文本对话的 `g4f.ChatCompletion.create()` 是完全独立的两条调用链路，封装在 `test_g4f_image_provider()` 里，**不与** `test_g4f_provider()` 共享任何逻辑（包括 `ROUTE_PROMPTS_MAP` 隐形路由、`detect_and_truncate` 重复检测，两者都是文本专属，对图片数据没有意义）。
3. **仍然没有接入 Gemini 生图**，原因不变：`AnyProvider` 内部没有免 Key 的 Gemini 图片后端，`GeminiPro`（对应 Google AI Studio）需要用户自备 API Key，与现有 g4f 适配层"零凭证"的设计原则冲突。如果产品需求上必须支持 Gemini 生图，需要先设计一套新的用户凭据管理机制（如让用户在个人设置里填入自己的 API Key），这已超出当前 g4f 适配层的既有模式，应作为独立功能设计，不要塞进 `IMAGE_PROVIDERS`/`IMAGE_PROVIDER_MODELS_MAP` 这套面向免 Key Provider 设计的现有结构里。
4. **已知范围限制（本轮有意未做，非遗漏）**：图片生成结果不写入 `history` 集合，因此不会出现在 Recents 侧边栏或 `/history/<id>` 只读详情页里——`history` 集合的 Firestore 文档结构（`results` 字段）是围绕文本 8-key result DTO 设计的，混入图片 DTO（`url`/`b64_json` 字段）需要单独的 schema 演进（例如给文档加一个 `content_type: 'text' | 'image'` 判别字段，前端渲染时分支处理），本次为控制范围未做。若未来要做，不要直接把图片 result 塞进现有 `save_chat_history(user_id, prompt, results)` 的 `results` 参数——那会让 `get_chat_history_by_id`/Recents 渲染逻辑在读到一条历史记录时无法区分里面装的是文本回答还是图片链接。
5. **下载按钮刻意不做服务端代理**：图片 URL 来自第三方域名（`pollinations.ai`/`*.hf.space`/`operacdn.com`），若做一个"服务端按客户端传来的 URL 发起请求再转发"的下载代理端点，会是一个可被利用来访问内网/云元数据地址的 SSRF 攻击面。因此下载完全在浏览器端用 `fetch()` + Blob（或本地 base64 解码）完成，见 `templates/index.html` 的 `downloadImage()`；跨域被拦截时降级为在新标签页打开图片，不做任何服务端网络请求兜底。

### 🟢 已完成：对话历史 HTTP 路由（`main.py`，2026-07-02 新增 4 个 API + 2026-07-03 追加只读页面路由）

`auth/db.py` 的 6 个对话历史 CRUD 函数已全部由 `main.py` 的路由暴露：

| 路由 | 方法 | 函数 | 认证要求 |
|---|---|---|---|
| `/api/compare` | POST | `compare_providers`（已登录时自动附带保存） | 可选（游客/匿名不保存） |
| `/api/history` | GET | `get_history` | 必须 `session['user_id']` |
| `/api/history/<history_id>/title` | PATCH | `update_history_title` | 必须 `session['user_id']` |
| `/api/history/<history_id>` | DELETE | `delete_history` | 必须 `session['user_id']` |
| `/api/history/<history_id>/toggle-pin` | POST | `toggle_history_pin` | 必须 `session['user_id']` |
| `/history/<history_id>` | GET | `view_history`（2026-07-03 新增，页面路由非 JSON API） | 无强制要求：匿名重定向、游客渲染 client-only 空壳、已登录经 `get_chat_history_by_id` 校验归属后渲染 |

后续若要继续扩展这批路由（如批量删除、搜索历史），沿用以下已验证的模式：

1. 路由体首行调用 `_get_authenticated_user_id()`，拿到 `err_response` 非空立即 `return err_response`（401）；游客（只有 `is_guest`）会被这一层拦下，不会碰到 CRUD 函数。`view_history()` 是例外——它不是 JSON API，游客理应能看到自己的（客户端持有的）历史详情页，因此没有用 `_get_authenticated_user_id()` 这套"游客一律 401"的守卫，而是自己手写了三态分支（匿名/游客/已登录），添加新的页面路由（而非 JSON API）时应参考这个模式而不是照搬 `_get_authenticated_user_id()`。
2. 调用 `delete_chat_history`/`update_chat_history_title`/`toggle_pin_chat_history`/`get_chat_history_by_id` 时，`user_id` 参数必须取自 `session['user_id']`，不能相信前端传来的 `user_id`（否则数据层的归属校验形同虚设）。
3. 这 6 个函数内部已各自守卫 `FIREBASE_AVAILABLE`，路由层不必重复检查，但仍需把 fallback 返回值（`None`/`False`/`[]`）转换为合适的 HTTP 状态码或页面行为（JSON API 当前统一转换为 404，理由见 `auth/db.py` 一节的契约表；`view_history()` 转换为 flash + 重定向，因为它是页面路由不是 API）。
4. **前端 UI 已实现（2026-07-02 侧边栏 + 2026-07-03 只读详情页）**：`templates/index.html`/`templates/history.html` 的左侧边栏 Recents 列表消费前 4 个 JSON 端点，`/api/compare` 返回的 `history_id` 用于触发保存后即时刷新侧栏；点击 Recents 条目通过 `openHistoryEntry(id)` 整页导航到第 5 个页面路由。详见下方 `templates/index.html`/`templates/history.html` 条目的完整前端逻辑说明。

### 🟢 安全区：如何安全地添加新页面

添加新页面时，若该页面可能作为重定向的目标（如 `flash()` + `redirect()` 之后），必须在该页面加入以下 Flash 消息显示代码块，防止消息堆积：

```jinja2
{% with messages = get_flashed_messages(with_categories=true) %}
    {% if messages %}
        {% for category, message in messages %}
            <div class="flash flash-{{ category }}">{{ message }}</div>
        {% endfor %}
    {% endif %}
{% endwith %}
```

### 🔴 危险区（Danger Zones）：严禁触碰的逻辑

- 不要修改 `test_g4f_provider` 的返回值字典结构（7-key 契约）。`compare_providers` 在其外层追加 `peer_reviews`，不属于此契约范围，但同样不可删除。
- 不要修改互评 DTO 的 key 集合（`reviewer_provider`、`reviewer_model`、`score`、`comment`）。前端 `renderPeerReviews` 直接读取 `r.score` 和 `r.comment`，任何 key 变更将导致互评面板渲染空白。
- 不要修改 `test_g4f_image_provider` 的返回值字典结构（8-key 图片 DTO：`provider`/`success`/`url`/`b64_json`/`error`/`response_time`/`model`/`type`）。它与文本的 7/8-key 契约是两套完全独立的 DTO，字段名不通用，前端 `displayImageResults` 直接读取 `result.url`/`result.b64_json` 渲染 `<img>`，任何字段改名都会让图片渲染成空白/损坏图标。
- 不要让文本 Provider（`G4F_PROVIDERS`）和图片 Provider（`IMAGE_PROVIDERS`）共用同一个映射表、同一条并发调度路径，或让 `/api/compare`/`/api/generate-images` 中的任一者去匹配另一方的 Provider 名字空间。两者是 g4f 里两套完全不同的接口（`g4f.ChatCompletion.create()` vs `g4f.client.Client().images.generate()`），返回类型、超时策略、失败模式都不同，混在一起会让 `determine_actual_model`/`determine_actual_image_model` 的降级规则相互污染。
- 不要给图片模式的 Provider 勾选框（`.image-provider-checkbox`/`.image-provider-trigger`）复用文本表单同名的 `.provider-checkbox`/`.provider-trigger` class。文本表单的 `providerTriggers`/`providerCards` 是用不加容器限定的 `document.querySelectorAll('.provider-trigger')`/`('.provider-checkbox')` 全局查询的，复用同名 class 会让图片模式的勾选框被这两个全局查询意外扫入文本表单的已选 Provider 列表和 `clearBtn` 重置逻辑里，即便两个模式从不同时可见。
- 不要把 `save_chat_history(user_id, prompt, results)` 的 `results` 参数替换成/混入图片 8-key DTO，也不要让 `generate_images()` 调用 `save_chat_history`。`history` 集合当前的 Firestore 文档结构是围绕文本 result 设计的，`get_chat_history_by_id`/Recents 侧边栏渲染逻辑都假设 `results` 里的每一项含 `response`/`peer_reviews` 字段——混入图片 DTO 会让这些既有读取路径读到 `undefined` 而不是优雅降级。若未来要支持图片历史，需要先设计 schema 演进（如判别字段），见第 10 节"已知范围限制"条目。
- 不要给 `/api/generate-images` 或前端下载按钮加一个"服务端按 URL 抓取图片再转发"的代理接口。图片 URL 来自客户端可控的第三方地址，服务端凭空信任并抓取任意 URL 是 SSRF 攻击面（可被诱导访问内网/云元数据地址）。下载必须继续留在浏览器端用 `fetch()`/Blob（或 base64 本地解码）完成，跨域失败时降级为新标签页打开，而不是绕道服务器。
- 不要把 `IMAGE_GENERATION_ADVISORY_TIMEOUT`（当前 `40`）和 `generate_images()` 里 `future.result` 的外层超时 `IMAGE_GENERATION_OUTER_TIMEOUT`（当前 `45`）改成不同步的数值。外层必须始终留有缓冲（当前 5 秒），原理与互评阶段 `25`/`32` 的同步要求一致（见下一条）；HuggingFace Space 后端的真实冷启动延迟远高于文本对话，不要凭直觉把这两个值调低。
- 不要移除根路由 `/` 中的 `provider_models_json` 注入。它是前端模型联动过滤机制的唯一数据源。
- 不要把 `test_g4f_provider`/`run_peer_review` 异常分支里 `CONTENT_POLICY_ERROR_KEYWORDS` 的判定顺序挪到 `NETWORK_ERROR_KEYWORDS`（或 `PEER_REVIEW_NETWORK_ERROR_KEYWORDS`）之后。Azure OpenAI 等 Provider 的内容审查报错文本有时会与网络类关键词同时出现在同一条错误信息里，内容策略必须优先判定——这类拦截重试没有意义，一旦被误判成"系统正忙"文案会诱导用户重试一个注定再次被拦截的相同请求（2026-07-03 用户报告的原始问题）。
- 不要把 `run_peer_review` 内部 `g4f.ChatCompletion.create` 的 advisory `timeout`（当前 `25`）和 `compare_providers` 第二阶段 `future.result` 的外层超时（当前 `32`）改成不同步的数值。外层必须始终留有约 7 秒的调度缓冲，且要同步核算 429 重试场景的总耗时上限（2-3s 等待 + 一次完整 advisory timeout）是否仍小于外层超时，否则会重新引入 2026-07-03 修复的"较慢 Provider 在互评阶段被误判超时"问题（`Peer review for <provider> timed out after Ns`）。
- 不要在 session 中同时设置 `user_id` 和 `is_guest`。两个键必须互斥。任何改变身份状态的路由都必须在写入新键的同时清除旧键。
- 不要在 auth 路由中直接调用 CRUD 函数而不先检查 `FIREBASE_AVAILABLE`。若 Firebase 未初始化，`db` 对象为 `None`，直接调用会触发 `AttributeError`。
- 不要修改 `GET /home` 路由的行为（即不要让它清除 `user_id`）。该路由专为"返回欢迎页"设计，已登录用户误触不应导致退出登录。
- 不要将 `run_peer_review` 的返回结构改回含 `response_time` 字段。互评阶段不计入前端展示的耗时统计，两者混用会使前端数据语义混乱。
- 不要移除 `delete_chat_history`/`update_chat_history_title`/`toggle_pin_chat_history` 内部的归属校验（`doc.to_dict().get('user_id') == user_id`）。这是防止跨用户操作他人对话历史的唯一防线，路由层不会重复校验。
- 不要让游客（`session['is_guest'] == True`）路径调用任何对话历史 CRUD 函数。游客数据不持久化是身份状态三层不变量的一部分。
- 不要在 `toggle_history_pin()` 中把 `toggle_pin_chat_history` 的返回值用 `if not new_pinned:` 判断失败。它的返回值是"翻转后的新状态"，`False` 是合法的成功结果（表示刚取消置顶），只有 `None` 才代表失败（不存在/不属于该用户/Firebase 不可用），必须用 `is None` 判断。
- 不要把 `history_id` 塞进 8-key result DTO（`test_g4f_provider` 返回值）或互评 4-key DTO 里。它是 `/api/compare` 响应体的顶层字段，与这两个既有契约无关。
- 不要让 `loadSidebarHistory()` 在 `isLoggedIn === false`（游客/匿名）时仍然调用 `fetch('/api/history...')`。后端会正确返回 401，但前端应主动跳过这次必然失败的请求——移除这个短路判断只会让游客控制台里多一条无意义的失败请求，不会造成安全问题，但会破坏"游客不触碰对话历史"这一体验层面的一致性。
- 不要移除 `.history-item-actions` 的 `:focus-within` 显现规则、只保留 `:hover`。键盘用户 Tab 到固定/重命名/删除按钮时若图标仍不可见，会显示一个看不见但可点击的"幽灵按钮"，破坏可访问性。
- 不要在游客分支（`!isLoggedIn`）把 `loadSidebarHistory()` 里的 `currentHistoryItems = window.guestHistory` 改成拷贝（如 `[...window.guestHistory]`）。乐观更新（pin/rename/delete）依赖 `currentHistoryItems` 与 `window.guestHistory` 是**同一个数组引用**，对条目对象的原地修改才能直接等价于"保存"；一旦拷贝，游客的操作会在下次 `loadSidebarHistory()` 时被真正的 `window.guestHistory` 覆盖丢失。
- 不要把 `toggleHistoryPin`/`deleteHistoryItem`/`commitRename` 里 "先本地改数据 + `renderCurrentHistory()`，再发请求" 的顺序颠倒成"先等待 fetch 成功再改 DOM"。这三个函数的乐观更新语义（点击后 UI 立即响应，失败才回滚）是 2026-07-02 步骤四的核心要求，颠倒顺序会让点击操作在网络慢时出现明显卡顿，且与 `showHistoryErrorToast` 的失败提示设计脱节（回滚逻辑假设 DOM 已经乐观更新过）。
- 不要恢复 `renameHistoryItem` 曾经使用的原生 `prompt()` 弹窗方案。`startInlineRename` 已在 2026-07-02 步骤四取代它，改为原地 `<input>` 编辑；混用两套机制会导致同一行同时存在旧的 `data-id` onclick 和新的 `data-action` 委托监听，重复触发或互相覆盖。
- 不要移除 `deleteHistoryItem` 失败回滚时的 `splice(index, 0, removedItem)` 精确插回位置，改成简单的 `push`/`unshift`。用户删除列表中间的条目失败后，该条目应该原地复位，而不是跳到列表首尾造成视觉跳动。
- 不要把 `body` 的 `min-height: calc(100vh / var(--page-zoom))` 改回 `min-height: 100vh`，也不要移除 `.app-layout { flex: 1; }` 或把 `body` 改回默认的块级布局（去掉 `display:flex; flex-direction:column;`）。三者任一被移除都会让结果区为空时 `.left-sidebar` 的深色背景在视口底部之前被截断（2026-07-02 用户报告的回归 Bug）。若以后要调整页面的 `zoom` 缩放比例，只改 `:root` 上的 `--page-zoom` 变量本身，不要绕开它直接给 `zoom` 或这里的 `calc` 硬编码新数值，否则两处会分叉。
- 不要在 `get_chat_history_list` 的 Firestore 查询上重新链式调用第二个 `.order_by()`（例如把 `is_pinned` 排序挪回 Firestore 端）。`.where()` + 两个 `.order_by()` 是复合查询，需要在每个 Firebase 项目的控制台里手动创建复合索引，缺失时会直接 500（`FAILED_PRECONDITION: The query requires an index`），2026-07-02 已在生产环境实际触发过一次。排序必须继续留在 Python 应用层完成。
- 不要移除 `renderCurrentHistory()` 里对 `sortHistoryItems(currentHistoryItems)` 的调用，也不要让它改成原地排序（`Array.prototype.sort` 直接作用于 `currentHistoryItems`/`window.guestHistory` 本身而非拷贝）。前者会让 pin/unpin 后条目不移动（2026-07-02 用户报告的回归 Bug，因为 `groupHistoryByDate` 只按日期分桶、桶内顺序完全依赖调用方传入数组的既有顺序）；后者会打乱 `deleteHistoryItem` 回滚时依赖的原始 index、以及 `window.guestHistory` 理应保持的插入顺序。
- 不要把 `save_chat_history`（`auth/db.py`）或前端游客模拟逻辑（`templates/index.html`）里的标题截断改回无条件 `prompt[:15] + '...'`。必须判断 `len(prompt) > 15`（前端对应 `prompt.length > 15`）才追加省略号，否则短 prompt（如 `"hi"`）会被误显示成加了省略号的样子，暗示了并不存在的截断（2026-07-02 用户报告的回归点）。
- 不要把 `.history-rename-input` 的配色改回深色文字配深色背景（或任何依赖全局 `input:focus` 规则自然生效的写法）而不同时显式声明 `.history-rename-input:focus`。全局 `input:focus` 会强制 `background-color:#ffffff`，其选择器特异性 `(0,1,1)` 高于单一类选择器 `(0,1,0)`；只有同时用 `.history-rename-input` 和 `.history-rename-input:focus` 两条规则显式锁定配色（特异性 `(0,2,0)`，稳赢 `input:focus`），聚焦态和非聚焦态才会保持一致可读，否则会复现 2026-07-02 用户报告的"文字看不清"回归。
- 不要把 `sortHistoryItems`（前端）或 `get_chat_history_list` 的 `sort_key`（后端，`auth/db.py`）里置顶组的排序字段从 `pinned_at` 改回 `created_at`。这是 2026-07-02 用户报告的第二次回归："第二个 Pin 顶替了第一个 Pin"——用 `created_at` 排置顶组会让排序结果取决于对话创建时间而不是置顶操作发生的时间，先置顶的条目可能被后置顶但创建时间更新的条目挤到下面。必须继续用 `pinned_at`（前端本地时间戳 + 后端 `firestore.SERVER_TIMESTAMP`）表示"这次置顶操作发生的时刻"，且是升序（最早置顶的在最上面）。
- 不要把已置顶条目的 Pin 图标重新塞回 `.history-item-actions`（hover-only 分组）里，或者反过来把未置顶条目的 Pin 按钮也做成常驻可见。已置顶条目必须用独立于 hover 分组之外、`opacity:1` 恒定可见的 `.pin-indicator`（2026-07-02 仿照 Gemini 网页版新增），未置顶条目必须保持 hover 才出现的灰色 Pin 按钮——这是"Pin/Unpin 图标要有区分"这一需求的核心，混在一起会导致置顶状态在不 hover 时又变得看不出来（复现用户报告的原始 Bug）。同一条目也不要同时渲染两个 Pin 控件（`renderHistoryItem` 里 `isPinned` 为真渲染 `.pin-indicator`、为假渲染 hover 分组内的 Pin 按钮，二选一，不可都渲染）。
- 不要恢复 `toggleHistoryPin` 的 `.then()` 回调里"只要 `data.is_pinned` 是布尔值就无条件 `renderCurrentHistory()`"的写法。必须加上 `data.is_pinned !== item.is_pinned` 的判断，只有服务端返回值和本地乐观值确实不同才二次渲染，否则每次点击 Pin 都会把整个 Recents 列表的 DOM 重建两遍，造成 2026-07-02 用户报告的"点击后条目快速闪烁两下"。
- 不要恢复 `openHistoryEntry(id)` 曾经的前身 `loadHistorySnapshot(id)`（把历史快照就地渲染进 `index.html` 自己的 `#prompt`/`#results`，表单原样留在下方）。那个实现已在 2026-07-03 被彻底取代为整页导航到只读的 `templates/history.html`，原因是就地渲染让一条"历史记录"被一套随时可编辑、可提交的表单包围，容易被误编辑后当成新请求提交（见第 9 节对应 Bug 条目与第 4 节 `templates/history.html` 专属说明）。`loadHistorySnapshot` 函数已不存在于代码库中，不要凭直觉重新引入同名/同语义的函数。
- 不要把 `#sidebarRecents` 委托 `click` 监听器里 `openHistoryEntry` 的 fallback 分支挪到 `[data-action]` 判断之前，或者反过来让它也响应对 `[data-action]` 图标、`.history-rename-input` 的点击。这三类点击目标必须严格互斥：pin/rename/delete 图标触发对应操作、重命名输入框内的点击不触发任何侧边栏级联行为（只是普通的光标定位/选中文字）、其余点击（含标题文字和行内空白）才导航到历史详情页。合并或颠倒判断顺序会导致点击图标时同时误触发导航，或者点击历史条目时误触发图标操作。
- 不要给 `templates/history.html` 重新加回 `<form id="compareForm">`、Provider 勾选、Model 下拉或任何形式的"再次提交"入口。这个模板存在的唯一目的就是提供一个物理上不可能误触发新对比请求的只读视图（2026-07-03 用户报告并修复的原始问题）；哪怕只是加一个看起来无害的"重新运行这个 Prompt"按钮，也应该通过跳转回 `index.html` 并预填 Prompt 来实现，而不是在 `history.html` 内部直接拼出新的 `/api/compare` 调用。
- 不要把 `history.html` 里 `deleteHistoryItem` 中"若删除的正是当前正在查看的条目（`id === targetHistoryId`）则 `location.href = '/'`"这条分支删掉，也不要在其他条目被删除时也触发同样的跳转。本页没有 `#clearBtn` 可以复用 `index.html` 那套"删除后联动清空界面"的手法，只有在用户删的正是自己正在看的这条历史时才需要离开页面；删除侧边栏里其他条目应该只是从列表中移除，不影响当前主内容展示。
- 不要移除 `persistGuestHistory()`/`loadGuestHistoryFromStorage()`（`index.html` 与 `history.html` 各自的顶层脚本里都有），也不要让 `window.guestHistory` 退回纯内存数组（去掉 sessionStorage 同步）。自从点击 Recents 条目变成整页导航（`openHistoryEntry`）之后，纯内存数组会在每次导航时被销毁——移除这套持久化会让游客一点历史记录就发现自己之前的对话全部消失。同样不要把存储介质换成 `localStorage`：`localStorage` 没有"标签页关闭即清空"的语义，会违反"游客数据不持久化"这条身份不变量。
- 不要移除 Recents 懒加载分页的任何一部分（`.sidebar-recents` 的 `scroll` 监听器、`loadMoreSidebarHistory()`、`hasMoreHistory`/`isLoadingMoreHistory` 状态、或 `renderCurrentHistory()` 触发的滚动条重建后必须紧跟着做的 `recents.scrollTop = previousScrollTop` 恢复），也不要把 `HISTORY_PAGE_SIZE`（当前 `20`）改到和后端 `/api/history` 默认 `limit` 不一致的数值而不同步修改判断"是否还有更多"的逻辑（`newItems.length >= HISTORY_PAGE_SIZE`）。这套分页是"新对话增多后更早记录从 Recents 消失"这一用户报告 Bug 的唯一修复点，移除任何一部分都会让该 Bug 复现。也不要给这套分页增加除 `currentHistoryItems` 本身之外的额外客户端缓存层（如 `localStorage`/`IndexedDB`）——用户明确要求保持轻量，不增加设备压力。

- 不要移除 `.left-sidebar` 的 `position: sticky`/`top: var(--sidebar-sticky-top, ...)`/`height: var(--sidebar-sticky-height, ...)`，也不要移除 `#sidebarRecents` 的 `min-height: 0;`，更不要把 `.left-sidebar` 的高度改回单纯依赖 `.app-layout` 的 `align-items: stretch` 被动拉伸。这三者是"Recents 分页/滚动条从未真正触发"这一 Bug（2026-07-03 用户复测报告）的唯一修复点：没有独立于 `.main-content` 的固定高度，`#sidebarRecents` 就永远不会真正溢出（`scrollHeight` 恒等于 `clientHeight`），`overflow-y:auto` 不生效、`scroll` 事件永远不触发，`loadMoreSidebarHistory()` 就会重新变成不可达代码——表面上看代码都在，但用户实际操作时完全无效，且这个回归很容易被忽略，因为它不会让任何 Python 测试失败（纯 CSS 布局问题）。同样不要移除页面脚本里的 `syncSidebarStickyMetrics()` 函数本身，也不要移除它在 `window.resize` 上的监听器，或把它内部"除以 `--page-zoom`"这一步去掉——去掉会导致侧边栏高度/顶部偏移在渲染后比真实导航栏矮/多，与真实导航栏产生几像素的缝隙或遮挡（原理与本文件其余 `calc(100vh/zoom)` 的陷阱同源）。`templates/index.html` 与 `templates/history.html` 两个文件里的这三处改动（`.left-sidebar`、`#sidebarRecents`、`syncSidebarStickyMetrics()`）必须保持同步——两个模板的 `<style>`/`<script>` 是逐行复制关系，只改一个文件会让另一个页面的 Recents 分页悄悄重新失效。

- 不要移除 `#sidebarRecentsScrollThumb`（自定义滚动指示器 DOM 节点）、`.sidebar-recents-scroll-indicator` CSS 规则，或 JS 函数 `updateSidebarScrollIndicator()` 及其全部四个调用点（页面加载时、`window.resize`、`#sidebarRecents` 的 `scroll` 事件、`renderCurrentHistory()` 末尾）。这是"滚动条在 Chrome 标签页里不可见"这一 Bug（2026-07-03 用户报告，紧接"Recents 分页从未真正触发"那次修复）的唯一修复点——已实测证明 Chrome 的原生 overlay 滚动条不占用任何布局空间且仅在主动交互时短暂淡入，`::-webkit-scrollbar` 自定义样式**不能**可靠地压制这一行为（`#sidebarRecents.offsetWidth === clientWidth` 恒成立，即滚动条从未真正占用布局宽度），因此不能仅靠调整原生滚动条 CSS 就宣称问题已解决——必须保留这个不依赖浏览器滚动条渲染的自定义指示器。也不要把 `#sidebarRecentsScrollThumb` 挪到 `#sidebarRecents` 内部（它必须是兄弟节点，不是子节点）——`renderCurrentHistory()` 通过 `recents.innerHTML = ...` 整体替换 `#sidebarRecents` 的内容，若指示器是子节点会被这次替换连带清空。不要移除 `updateSidebarScrollIndicator()` 内部"除以 `--page-zoom`"这一步（针对由 `getBoundingClientRect()` 得来的 `trackTop`/`trackHeight`，不包括 `scrollTop`/`scrollHeight`/`clientHeight` 三个纯比例来源——这个区分很关键，不要图省事对最终结果整体除一次 zoom）——去掉会重新引入本次修复时踩过的坑：指示器位置偏高、滚到底部时提前停在距真实底边约 160px 处。`templates/index.html`/`templates/history.html` 的这三处改动（HTML 节点、CSS 规则、`updateSidebarScrollIndicator()`）必须同步维护。
- 不要给 `#sidebarRecents` 恢复 `scrollbar-width:thin`/`scrollbar-color`/`::-webkit-scrollbar{width:10px;...}` 这类"只定制外观、仍保留原生滚动条"的写法，也不要给整页的 `html` 恢复 `overflow-y:scroll`。2026-07-03 第二轮修复已确认：只要原生滚动条（哪怕是外观被定制、平时以 Chrome overlay 模式不可见的那种）还占据任何一点可交互区域，它就会盖在页面内容之上抢先拦截 `mousedown`，同时自绘指示器（`#sidebarRecentsScrollThumb`/`#pageScrollThumb`）就永远做不到真正"可拖拽"，且用户主动滚动/悬停时原生滚动条仍可能短暂淡入、与自绘指示器同时出现造成"2 个 scrollbar"的观感。两处都必须保持彻底隐藏（`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`，在"是否溢出"两种状态下恒为 0 宽度），自绘指示器是当前**唯一**允许存在的滚动条视觉/交互层。也不要移除 `#sidebarRecentsScrollThumb`/`#pageScrollThumb` 的 `cursor:grab`/`.dragging{cursor:grabbing}`，或移除/精简 `setupThumbDrag()` 及其挂载的 `mousedown`/`mousemove`/`mouseup`/`touchstart`/`touchmove`/`touchend` 六个监听器——这些是"可拖拽"这一需求的唯一实现，删掉任何一半（鼠标或触摸）都会让对应输入方式下的拖拽失效。
- 不要把 `.left-sidebar` 的 `height` 从 CSS `calc(100vh / var(--page-zoom) - var(--sidebar-header-height) + 2px)` 改回由 JS 在 `syncSidebarStickyMetrics()` 里手工计算 `(window.innerHeight - realHeaderHeight) / zoom` 后注入的字面量像素值（旧变量名 `--sidebar-sticky-height`，已废弃）。2026-07-03 第二轮修复已确认：`window.innerHeight` 是 JS 侧取整后的整数，与浏览器自身的页面缩放（不同于页面自带的 `--page-zoom`）叠加会产生舍入误差，这既是"自绘滚动指示器需要缩放到约 110% 才会被判定为溢出而显示"的根因，也是"150% 等高缩放下滚动到页面底部、`.left-sidebar` 底部露出空缺"的根因——恢复旧写法会同时让这两个 Bug 复现。同样不要移除 `calc()` 末尾的 `+ 2px` 安全余量，它让残余的亚像素误差稳定偏向"侧边栏略高于必要值"而非"偏矮、露出缝隙"。`syncSidebarStickyMetrics()` 现在只应该测量并写入 `--sidebar-header-height` 一个 CSS 变量（对应的 `top`），不应该再去计算或写入任何"高度"相关的字面量像素值——`100vh` 部分必须留给 CSS 原生解析。`templates/index.html`/`templates/history.html` 的这一改动必须同步维护。
- 不要移除挂在 `document.body` 上的 `ResizeObserver`（驱动 `updatePageScrollIndicator()` 在任意内容高度变化时重算），也不要把它替换成"在每个可能改变文档高度的地方手动调用 `updatePageScrollIndicator()`"这种散落式写法——2026-07-03 引入这个 `ResizeObserver` 就是为了不必在 `displayResults()`/`clearBtn`/flash 消息淡出等所有现有和未来的调用点里逐一插入刷新调用，移除它会让整页自绘滚动指示器在结果区展开/收起等场景下不再实时更新位置/可见性，直到下一次 `resize`/`scroll` 事件才会被动纠正。
- 不要把 `deleteHistoryItem` 恢复成直接调用浏览器原生 `confirm()`。2026-07-03 起删除确认已改为自定义居中模态框 `showDeleteConfirmModal(entryTitle)`（见第 4 节 `templates/index.html` 条目），原生 `confirm()` 样式无法定制、且不会点名具体条目标题，与本项目其余弹层（历史操作失败 Toast、侧边栏抽屉）风格不一致。同理不要让 `showDeleteConfirmModal` 的 Delete 按钮改用 `innerHTML` 注入条目标题——必须继续用 `textContent`，否则标题中含 `<script>`/引号等字符的历史记录会造成 DOM 结构破坏或（在更宽松的场景下）XSS。
- 不要移除 `showDeleteConfirmModal` 里 Cancel 按钮、遮罩背景点击（`e.target === overlay`）、`Escape` 键三条等价的"取消"路径中的任何一条，也不要让它们在解绑监听器（`cleanup()`）之外各自重复一份收尾逻辑。四条收尾路径（Cancel/Delete/背景点击/Escape）必须共享同一个 `cleanup(result)`，否则容易出现某条路径忘记移除 `document` 上的 `keydown` 监听器，导致多次打开模态框后监听器越叠越多。
- 不要移除 `clearBtn` 监听器里 `updateModelDropdown()` 调用之前的 `modelSelect.value = '';`。这一行是 2026-07-03 新增的修复：`updateModelDropdown()` 有"尽量保留刷新前已选中模型"的逻辑，清空 Provider 勾选后它会退化成"显示全部 Provider 模型并集"，用户之前选中的具体模型大概率仍在并集里，若不先清空 `modelSelect.value`，清空/新建对话后 Model 下拉会诡异地保留上一次选择而不是回到"使用 Provider 默认模型"。也不要把这一行改成只重置 `customTrigger.textContent` 而不重置 `modelSelect.value` 本身（或反之）——两者必须一起交给 `updateModelDropdown()` 的 `else` 分支统一处理，单独改一个会让原生 `<select>` 的实际值和自定义下拉组件显示的文本不一致。
- 不要移除 `deleteHistoryItem` 里 `showDeleteConfirmModal(...)` 确认通过之后新增的 `document.getElementById('clearBtn').click()`。这是 2026-07-03 新增的行为：确认删除一条历史记录后，屏幕上任何正在显示的结果（当次提交的或 `loadHistorySnapshot` 加载的历史快照）都应联动清空，避免残留内容与"记录已删除"的状态矛盾。这一行必须放在 `if (!confirmed) return;` 之后（取消删除不应重置界面），且必须在原有的乐观删除 `splice`/`fetch DELETE`/失败回滚逻辑之前或独立于其后都可以，但不能删除这一行本身或把它替换成手写的、与 `clearBtn` 监听器逻辑不同步的重置代码——复用 `clearBtn.click()`（`newChatBtn` 已验证过的同款手法）能保证两处"清空界面"的语义永远一致，不会随 `clearBtn` 未来逻辑变化而分叉。
- 不要把 Model 自绘下拉框（`templates/index.html`）里 `.custom-option` 的 `click` 监听器改回同步执行 `classList.remove('selected')`/`classList.add('selected')`。2026-07-03 已把黑色高亮的变更从"点击时立即执行"改为通过 `syncSelectedOptionHighlight()` 在 `setTimeout(..., MODEL_HIGHLIGHT_DELAY_MS)`（`150`）之后才执行，目的是让高亮跳变发生在 `.custom-options` 收起动画（CSS `transition: all 0.15s ease`）结束、面板已完全不可见之后，避免用户看到选项间黑色背景瞬间跳动。若把 `MODEL_HIGHLIGHT_DELAY_MS` 改小或改大，必须同步修改 `.custom-options` 的 CSS transition 时长，保持两者一致，否则要么高亮仍在面板可见时跳变（延迟太短），要么面板已经隐藏很久高亮才更新、下次打开时会有一瞬间显示旧选中项（延迟太长且未在打开时兜底同步）。也不要移除 `customTrigger` 的 `click` 监听器里"打开时立即 `syncSelectedOptionHighlight()` 并清理旧计时器"这一分支，它是处理"选中后 150ms 内又重新打开下拉框"竞态的唯一保障。
- 不要移除 `html { overflow-y: scroll; }`（2026-07-03 新增，紧跟 `:root` 之后）。这是"加载一条较长的历史快照再点击 Clear Results/+ New Chat，整页出现水平位移"这一 Bug 的修复点：没有它，滚动条随内容高度出现/消失会导致 `.nav-container`/`.container` 这类居中元素的可用宽度来回变化。也不要把它错误地塞进 `--page-zoom` 相关的 `calc()` 里——它在真实视口宽度上生效，与 `zoom` 缩放无关。**不要把它换成 `scrollbar-gutter: stable`**（同一天试过、已回退的方案）：那个属性只做静默的空间预留、不绘制任何内容，内容不需要滚动条时会在紧贴视口右边缘的 `.navbar`/`.guest-banner`/`.app-layout` 这类通栏元素旁边留出一条无法解释的空白缝隙，复现了"导航栏白色背景右侧出现缺口"这一回归（同样是 2026-07-03 用户报告）。`overflow-y: scroll` 预留的空间宽度虽然相同，但会用一个真实滚动条控件填满这段空间而非留白，因此不会有这个副作用。
- 不要把 `.nav-container` 恢复成 `max-width:1200px; margin:0 auto;` 的居中窄条写法，也不要移除 `.nav-left` 上的 `width:260px; flex-shrink:0;`。两者必须配合：`.nav-container` 边到边铺满（左侧无 padding）+ `.nav-left` 定宽 260px 且 `justify-content:center`，logo 才能精确居中在 `.left-sidebar`（同样 260px、同样从 `x=0` 起算）正上方；恢复旧写法会让 logo 的水平位置重新退化为"取决于视口宽度"而非"取决于侧边栏"，复现 2026-07-03 用户报告的原始问题。若要调整 `.nav-container` 右侧留白，只改 `padding-right`（当前 `2rem`），不要重新引入 `max-width`。
- 不要忘记移动端 `@media (max-width: 520px)` 断点内的 `.nav-left { width:auto; flex-shrink:initial; justify-content:flex-start; }` 覆盖规则。移动端侧边栏是 `position:fixed` 浮层抽屉、不占据静态两栏布局的 260px，若缺少这条覆盖，`.nav-left` 会在窄屏上强行保留 260px 定宽，挤压本就局促的 `.nav-links` 空间。
- 不要移除 `templates/history.html` 里 `#promptDisplayContainer .form-group { margin-bottom: 0; }` 这条覆盖，也不要把它错误地搬到共享的 `.form-group` 规则本身上（那会影响 `index.html` 的 `#compareForm` 里几个确实需要靠这份 `margin-bottom:26px` 相互分隔的字段）。这是"Prompt 展示区下方空白明显宽于上方"这一 Bug（2026-07-03 用户报告）的唯一修复点——`#promptDisplayContainer` 内只有这一个 `.form-group`，它后面没有任何兄弟节点需要靠这份下边距分隔，移除覆盖会让下边距重新叠加到容器自身的 `padding` 上、复现视觉不对称。
- 不要把 `templates/history.html` 顶部说明文案里的 `<br>` 去掉、合并回一整句英文。2026-07-03 用户明确要求这两句话"各一行，不要挤在一行"；同理不要移除 `.header p` 新增的 `line-height:1.5`（`index.html`/`history.html` 两份 `<style>` 需同步维护这一行，缺一处会造成两个页面 `.header p` 渲染行距不一致）。
- 不要把 `login.html`/`register.html`/`profile.html` 的页面标题 `<h2>` 改回内联 `style="color: #333; margin-bottom: 2rem;"`，也不要给这三处各自新写一份不同的内联样式。必须继续使用 `auth/base.html` 里的 `.card-title` 复用类——这是 2026-07-03 UI 比例统一审计发现并修复的问题：内联样式的 `#333` 颜色和默认字重与全站统一的 `#09110e` + `font-weight:800` 排版体系不一致，若日后要调整这三处标题的样式，应该改 `.card-title` 这一处，而不是重新散落成三份可能互相分叉的内联样式。

## 11. 🛠️ BUILD, RUN & TEST COMMANDS

### 本地开发环境搭建

```bash
# 1. 创建并激活虚拟环境（首次）
python3 -m venv env
source env/bin/activate          # Linux/macOS
# env\Scripts\activate           # Windows

# 2. 安装依赖（严格锁版本）
pip install -r requirements.txt
```

### 本地运行前置条件

启动前必须确认以下三项：

1. `firebase-key.json` 存在于项目根目录（从 Firebase Console 下载服务账号密钥）。
2. `.env` 文件存在于项目根目录，包含持久化的 `SECRET_KEY`（格式：`SECRET_KEY="your-fixed-key"`）。
3. `firebase-admin==7.4.0` 和 `python-dotenv==1.2.2` 已安装（包含在 `requirements.txt` 中）。

### 本地运行

```bash
# 直接用 Flask 开发服务器启动（默认端口 8080）
python main.py

# 指定端口
PORT=5000 python main.py

# 用 gunicorn 模拟 GAE 运行环境（需额外安装 gunicorn）
pip install gunicorn
gunicorn -b :8080 main:app
```

**⚠️ 编辑 `templates/*.html` 后，已经在跑的开发服务器不会自动生效**：`main.py` 以 `app.run(debug=False, ...)` 启动，Flask/Jinja2 的模板自动重载（`TEMPLATES_AUTO_RELOAD`）默认跟随 `app.debug`，因此为 `False`——已经启动的进程会把 Jinja2 编译后的模板永久缓存在内存里，此后对磁盘上 `.html` 文件的任何修改都不会被这个已运行的进程感知，直到手动重启它（`Ctrl+C` 再重新 `python main.py`）。2026-07-03 曾在这里踩坑排查过一次：用户报告 CSS/JS 修复"没生效"，实际代码早已正确写入磁盘，问题出在正在被测试的那个开发服务器进程是修复之前就启动的旧进程。改一次模板就必须重启一次服务器（浏览器端的强制刷新/清缓存解决不了这个问题，因为服务端本身返回的就是旧内容）；用 `curl`/`grep` 直接核对服务器实际返回的 HTML 里是否包含新改动的字符串（如某个新增的 `id`/class 名）是比反复截图更快确认"到底是代码问题还是没重启"的办法。

启动后访问 `http://localhost:8080`（未认证时显示欢迎页），`http://localhost:8080/health` 验证服务状态。

### 自动化测试框架

项目使用 Python 内置的 `unittest` 框架，测试文件存放于 `tests/` 目录。

- **test_main_whitebox.py**：直接测试 `main.py` 的核心内部函数。测试内容包括模型降级规则 A/B/C 的全部边界条件、结果字典的 key 完整性、`test_g4f_provider` 的成功路径与异常路径、`detect_and_truncate` 的句级与滑动窗口重复检测、`ROUTE_PROMPTS_MAP` 路由注入行为、`parse_peer_review_json` 的 JSON 解析/夹值/容错 fallback 全部边界条件，以及 `run_peer_review` 的 429 重试逻辑（`TestRunPeerReview`，429 触发重试且成功、queue-full 同样触发重试、非 429 不重试、重试后仍失败返回友好消息、结果 4-key 契约始终完整）。`TestTestG4fProvider` 2026-07-03 新增 2 个用例：`test_content_policy_error_shows_friendly_message`（模拟 Azure OpenAI `content management policy` 拦截报错，断言 `result['error']` 含 `'content filter'` 友好文案而非原始异常文本）、`test_content_policy_error_takes_priority_over_network_wording`（异常文本同时含内容策略关键词与网络关键词 `'remote'`，断言最终文案是内容策略版本，验证判定顺序未颠倒）。`TestRunPeerReview` 2026-07-03 同步新增 2 个用例：`test_content_policy_error_does_not_retry_and_returns_friendly_comment`（断言内容策略错误只调用 1 次 `g4f.ChatCompletion.create`、不触发 429 重试逻辑、`comment` 含 `'content filter'` 友好文案）、`test_advisory_timeout_passed_to_g4f_is_25_seconds`（断言 `g4f.ChatCompletion.create` 实际收到的 `timeout` 关键字参数已从 `15` 上调为 `25`，锁定"互评超时调优"的具体数值不被意外改回）。2026-07-03 文生图功能落地新增 `TestDetermineActualImageModel`（4 个用例，覆盖规则 A/B 及"未知 Provider 无通用兜底、返回 `None`"这一与文本侧规则 C 的关键差异）、`TestInitImageResultObject`（9 个用例，8-key 契约完整性/独立实例）、`TestTestG4fImageProvider`（16 个用例，覆盖 `url`/`b64_json` 两种成功形式、`url` 优先于 `b64_json`、空数据/无 `url`/`b64_json`均为空两种失败形态、异常与网络错误友好文案、`'auto'` 模型不透传给 `images.generate()`、非 `'auto'` 模型正确透传、`provider` 实例正确透传、成功/异常路径 key 集合完整、未知 Provider 的 `model` 展示兜底为 `'default'`），全部通过 `patch('main.G4FImageClient', return_value=fake_client)` 隔离真实网络调用，不依赖 `main.G4F_AVAILABLE`（因为这些是纯函数/隔离过外部依赖的单元测试，不像 `TestPeerReview` 等黑盒测试类那样需要 `@unittest.skipUnless(main.G4F_AVAILABLE, ...)` 跳过条件）。
- **test_main_blackbox.py**：通过 Flask `test_client()` 以 HTTP 协议测试 `main.py` 暴露的全部对外 API 端点，包含互评触发条件、`peer_reviews` 字段存在性、互评 DTO key 集合（`{reviewer_provider, reviewer_model, score, comment}`）以及 `score` 类型为 `int` 的断言。新增对 `/health` 接口的 `routing_rules_loaded` 和 `peer_review_rules_loaded` 两个字段的存在性与类型断言；新增对 `/api/test-single` 的 `ROUTE_PROMPTS_MAP` 隐形路由生效验证（检查 g4f 实际收到的 prompt 含 style suffix）以及 `detect_and_truncate` 被调用验证。`TestPeerReview` 新增 `test_default_judge_prefix_used_when_model_not_in_peer_review_map`（2026-07-02 新增）：将 `PEER_REVIEW_PROMPTS_MAP` patch 为空字典，断言两个成功 Provider 互评时实际发给 g4f 的 prompt 均以英文默认裁判前缀 `'Please evaluate the quality of the following answer'` 开头，覆盖该默认值由中文改为英文后的 fallback 分支。新增对话历史相关测试（2026-07-02，共 33 个用例）：`TestCompareHistoryPersistence`（4 个用例）验证已登录用户触发 `save_chat_history` 且传参正确、响应含 `history_id`、游客/匿名均不触发保存；`TestHistoryAuthGuard`（6 个用例）验证全部 4 个历史路由（含游客）在未登录时统一返回 401；`TestGetHistoryEndpoint`（8 个用例）覆盖分页参数换算为 `offset`、`limit` 夹到 100 上限、`page` 夹到 1 下限、非数字参数回退默认值、500 兜底；`TestUpdateHistoryTitleEndpoint`（6 个用例）覆盖成功、`user_id` 取自 session 而非请求体、404、空标题 400、500 兜底；`TestDeleteHistoryEndpoint`（4 个用例）与 `TestTogglePinEndpoint`（5 个用例，含关键用例 `test_response_reflects_new_is_pinned_false_not_confused_with_failure` 断言翻转为 `False` 时仍返回 200 而非误判为 404）。新增 `TestIndexPageSidebarMarkup`（2026-07-02，7 个用例）：验证 `GET /` 在已登录/游客身份下渲染出侧边栏关键标记（`left-sidebar`、`sidebar-overlay`、`main-content`、`app-layout`、`hamburger-btn`、`sidebarRecents`、`newChatBtn`、`sidebar-skeleton`）、匿名身份仍正确路由到无侧边栏的 `home.html`（防止身份路由回归）、以及 `isLoggedIn` JS 常量根据 `session.user_id` 正确注入 `true`/`false`。**侧边栏的运行时交互（骨架屏替换、hover 淡入、汉堡抽屉动画、时间分组算法 `groupHistoryByDate`/`formatGroupLabel`、乐观更新的 pin/rename/delete 及其失败回滚、原地重命名的 `<input>` 替换与 Enter/Escape 分支、游客 `window.guestHistory` 全流程模拟、事件委托的 `data-action` 分发）不在 Python `unittest` 覆盖范围内**——本项目没有 JS 测试框架（无 `package.json`/Jest/Vitest），这部分逻辑通过 Node.js + jsdom 手动验证过（模拟 DOM 加载 `page_guest.html`/`page_loggedin.html`、mock `fetch`、断言 class 切换与分组渲染结果；2026-07-02 步骤四新增验证覆盖：pin/delete/rename 的乐观更新在 mock 请求成功/失败两条路径下的 DOM 状态与 `showHistoryErrorToast` 弹出情况、编辑图标与双击标题两种方式触发的原地重命名、Enter 提交与 Escape 取消、游客提交 `/api/compare` 后 `window.guestHistory` 被正确写入且全部后续 pin/rename/delete 操作零网络请求），但不是可重复运行的自动化测试套件；后续若引入前端测试框架，应把这些验证转成正式用例。步骤四未修改任何 Python 代码（`main.py`/`auth/db.py`/`auth/routes.py` 均未触碰），因此未新增/修改任何 black/white/grey box 测试用例。2026-07-02 的侧边栏"空结果区背景截断"修复同样未改动任何 Python 代码，纯 CSS（`body`/`.app-layout` 的 flex 与高度声明），因此也未新增/修改 black/white/grey box 测试用例；但这类问题本质是**渲染后的像素高度**是否正确，而 jsdom **不做真实布局计算**（没有实现 CSS 盒模型渲染，`getBoundingClientRect`/`offsetHeight` 等在 jsdom 下不返回真实数值），此前几轮验证一直只能靠 jsdom 断言 class 切换和 DOM 结构，无法发现这类高度截断问题。这次改用真实 headless Chromium（Playwright）加载运行中的 Flask 页面，直接测量 `.left-sidebar`/`.app-layout` 的 `boundingBox()` 高度，分别在"未提交 Prompt"与"提交后出结果"两种状态下断言侧边栏底部到达视口底部，并截图核对视觉效果（桌面 1400×900 与移动 400×800 两种视口，含汉堡抽屉展开/收起）。此沙箱环境没有预装浏览器可执行文件所需的系统共享库（`libnspr4`/`libnss3`/`libasound2`），且没有 root 权限跑 `apt-get install`；通过 `apt-get download` 单独拉取这几个 `.deb` 包、`dpkg -x` 解压到用户目录、再设置 `LD_LIBRARY_PATH` 指向解压出的 `.so` 文件，绕开了这一限制，未使用任何持久化的系统级安装。这一验证方式没有留下可重复运行的自动化测试文件（纯手动脚本 + 截图核对），后续如果引入前端测试框架并接入真实浏览器环境，应把这类"渲染高度是否正确"的断言转成正式的视觉回归测试用例。2026-07-02 的三个 Recents 小 Bug 修复（短 prompt 标题误加省略号、重命名输入框文字不可读、Pin 后条目未移动到顶部）中，第一个是纯 Python 逻辑（有正式 whitebox 用例覆盖，见 `test_auth_whitebox.py` 条目），后两个是纯前端逻辑：重命名输入框的可读性问题用 jsdom 的 `window.getComputedStyle(input)` 读取 `.history-rename-input` 在 `focus()` 之后的实际 `color`/`backgroundColor`，验证不再是浅灰字叠白底；Pin 排序问题用 jsdom 构造三个同日期分组的条目，触发 `toggleHistoryPin` 后断言 `#sidebarRecents` 内 `.history-item` 的 DOM 顺序（而非仅断言 class 是否切换）确实把目标条目移到了最前面，取消置顶后又跌出置顶位置——这类"DOM 元素相对顺序"和"computed style"断言 jsdom 是可以正确处理的（不同于此前 CSS 布局高度截断那个 Bug，那类问题才需要真实 Chromium），所以延用 jsdom 手动脚本验证即可，未使用 Playwright。这三个修复均未改动 Python 侧的对话历史 CRUD 契约或路由，因此除 `TestSaveChatHistory` 新增的两个用例外，未新增/修改其余 black/grey box 测试用例。2026-07-02 本轮进一步修复的三个 Pin 相关 Bug（第二个 Pin 顶替第一个、Pin/Unpin 无图标区分、点击 Pin 后闪烁两下）中，排序逻辑（`pinned_at` 升序）同时改了前端 `sortHistoryItems` 和后端 `get_chat_history_list`/`toggle_pin_chat_history`——后端部分有正式 whitebox 用例覆盖（`TestGetChatHistoryList`/`TestTogglePinChatHistory` 新增的 4 个用例，见上方 `test_auth_whitebox.py` 条目），`main.py` 的 `/api/history/<id>/toggle-pin` 路由本身未改动（仍只是把 `toggle_pin_chat_history` 的返回值包一层 JSON），因此 `TestTogglePinEndpoint`（`test_main_blackbox.py`）无需更新。图标持久化显示和防闪烁两个纯前端修复继续用 jsdom 手动脚本验证：用三个同日期分组的条目模拟"先 Pin 一个、间隔后再 Pin 另一个"，断言 `#sidebarRecents` 内 `.history-item` 的 DOM 顺序（先置顶的稳定在最前，后置顶的排在其后而非顶替）；用 `MutationObserver` 监听 `#sidebarRecents` 的 `childList` 变化次数，断言一次点击 Pin 只触发一次整体 DOM 重建（而非修复前的两次），验证"闪烁两下"确实消失；用选择器断言未置顶条目只有 hover 分组内的 Pin 按钮、置顶条目只有独立的 `.pin-indicator`，且取消置顶后二者会互相替换。这类"DOM 元素相对顺序""DOM 变更次数"和"元素是否存在"的断言 jsdom 可以正确处理（不同于此前 CSS 布局像素高度那个 Bug，那类问题才需要真实 Chromium），因此继续沿用 jsdom 手动脚本，未使用 Playwright。2026-07-03 新增的 `loadHistorySnapshot(id)`（点击 Recents 条目加载历史快照到 Prompt 输入框与结果区）同样是纯前端修复，未改动任何 Python 代码：jsdom 脚本构造带完整 `results` 快照的历史条目，先在 `#prompt`/`#resultsContainer` 里塞入"当前正在显示的、与历史条目无关"的占位数据，再触发点击，断言 `#prompt` 的值、`.stat-value`（重算的 `total_providers`/`successful_providers`）、`.provider-name` 列表被整体替换为该条目的快照内容而非追加或保留旧值；点击第二个条目时断言第一个条目的结果不再残留（验证"完全替换"而非"合并"）；点击 pin/rename/delete 图标时断言 `#prompt` 未被触碰（验证三类点击目标互斥）；`results` 为空数组的条目点击后断言结果区被隐藏且 `#resultsContainer.innerHTML` 清空；游客身份下重复以上关键断言并确认零 `fetch` 调用。这类"表单值/DOM 内容是否被替换"的断言同样是 jsdom 能正确处理的范畴，未使用 Playwright。2026-07-03 新增的删除确认自定义模态框（`showDeleteConfirmModal`，取代原生 `confirm()`）同样是纯前端修复，未改动任何 Python 代码：jsdom 脚本通过真实的 `loadSidebarHistory()` 流程（mock `fetch('/api/history?...')` 返回种子数据，而非直接对 `let currentHistoryItems` 赋值——该变量是 `<script>` 顶层的词法作用域绑定而非 `window` 属性，从测试脚本外部赋值不会影响闭包内读到的值）渲染出历史条目，点击 🗑️ 图标后断言：`#confirmModalOverlay` 被创建且带有 `.visible`；标题为 `"Delete this chat?"`；正文里的 `<strong>` 通过 `textContent`（而非 `innerHTML`）承载条目标题，用一个标题里含 `<script>` 字样的条目验证 `innerHTML` 中并未出现可执行的 `<script>` 标签；点击 Cancel、点击遮罩背景（构造 `target === overlay` 的 `MouseEvent`）、按 `Escape` 键三条路径均关闭模态框且不触发任何 `DELETE` 请求、条目仍在 DOM 中；点击 Delete 按钮才会真正移除该条目并且只对该条目的 `id` 发出一次 `DELETE /api/history/<id>` 请求；标题为空字符串时模态框正文回退显示 `"this conversation"`。游客身份下重复关键路径（打开模态框可点名条目标题、确认删除后 `window.guestHistory` 被原地移除、全程零 `fetch` 调用）。这类"元素是否存在/`textContent` 内容/请求是否被触发及触发几次"的断言同样是 jsdom 能正确处理的范畴（不涉及像素级布局计算），未使用 Playwright。2026-07-03 新增的两处联动重置行为（`clearBtn` 重置 Model 下拉、`deleteHistoryItem` 确认后联动清空界面）同样是纯前端修复，未改动任何 Python 代码：这次 jsdom 脚本不再用手写的最小化 HTML 片段，而是先用 `main.app.test_client()`（`session_transaction` 写入 `user_id`）实际渲染 `GET /` 拿到真实的 `index.html` 输出，再喂给 jsdom（`beforeParse` 钩子里预置 `window.fetch` 桩，因为页面脚本末尾会同步调用 `loadSidebarHistory()`，若不在解析最外层 `<script>` 执行前就注入 fetch 会报 `ReferenceError: fetch is not defined`——jsdom 对内联 `<script>` 是在 `new JSDOM(html, ...)` 构造函数内同步执行的，构造完成后再赋值 `window.fetch` 为时已晚）；断言点包括：选中一个非默认 Provider 模型后点击 `#clearBtn`，`#modelSelect` 的 `value` 变回 `''` 且自定义下拉组件 `#customSelectTrigger` 的文本变回 `"-- Use Provider's Default Model --"`；以及弄脏界面（结果区可见、Prompt 有值、Provider 勾选、Model 选中非默认值）后 stub `showDeleteConfirmModal` 直接 `resolve(true)` 并调用 `deleteHistoryItem(id)`，断言这四项状态全部被重置、同时该历史条目对应的 `.history-item` DOM 节点确实被移除（验证联动重置没有影响原有的删除行为本身）。同样因为 `let currentHistoryItems` 是脚本顶层词法绑定、不是 `window` 属性，测试改为通过 `beforeParse` 里的 fetch 桩让页面自己的 `loadSidebarHistory()` 加载种子数据，而非从测试代码外部直接赋值。2026-07-03 新增的 Model 自绘下拉框黑色高亮延迟切换（`syncSelectedOptionHighlight()` + `modelHighlightTimer`）同样是纯前端修复，未改动任何 Python 代码：这次验证的核心断言是"点击后某个精确时间点（150ms 延迟窗口内 vs 之后）`.custom-option.selected` 挂在哪个元素上"，属于**真实定时器行为**（`setTimeout` 的实际触发时机）而非单纯的 DOM 结构或 computed style，jsdom 的定时器模拟与真实浏览器事件循环存在差异，为避免验证结果失真，这次沿用了"空结果区背景截断"那次已经验证过可行的真实 headless Chromium（Playwright）路径：复用同一套 `apt-get download libnspr4 libnss3 libasound2t64 libasound2-data` + `dpkg -x` 解压到用户目录 + `LD_LIBRARY_PATH` 指向解压出的 `.so` 文件的免 root 沙箱变通方案（无需重新摸索，说明该方案在本沙箱环境内可重复使用），驱动一次真实的 `PORT=8091 python main.py` 本地服务器 + 真实点击事件，分别在点击后 0ms（`.custom-select-wrapper` 的 `open` class 已被移除但 `.selected` 仍在旧选项）和 200ms（`.selected` 已切换到新选项）两个时间点读取 DOM 状态断言；另外验证了点击后 150ms 内立刻重新打开下拉框（`customTrigger` 再次点击）不会导致高亮显示过期值。这一验证方式同样没有留下可重复运行的自动化测试文件（纯手动脚本），后续如果引入前端测试框架并接入真实浏览器环境，应把这类"依赖真实定时器时序"的断言转成正式用例。2026-07-03 新增的两处纯前端修复——`html { overflow-y: scroll; }`（消除加载历史快照后点击 Clear 导致的整页水平位移；当天第一版曾用 `scrollbar-gutter: stable` 实现，因引入"导航栏白色背景右侧出现缺口"回归而改回这个经典写法，见下方追加说明）与导航栏对齐重构（`.nav-container` 去掉 `max-width`/`margin:auto`、`.nav-left` 改为定宽 260px 居中，logo 与 `.left-sidebar` 对齐）——均未改动任何 Python 代码，因此未新增/修改任何 black/white/grey box 测试用例。两者本质都是**渲染后的像素级布局位置**是否正确（滚动条是否让出/收回轨道宽度、元素中心点坐标是否重合），与前面"空结果区背景截断"那次同属 jsdom 无法验证的范畴（jsdom 不做真实盒模型渲染），因此复用同一套真实 headless Chromium（Playwright）+ `apt-get download libnspr4 libnss3 libasound2t64 libasound2-data` + `dpkg -x` 解压至用户目录 + `LD_LIBRARY_PATH` 指向解压出的 `.so` 文件的免 root 沙箱方案（第三次在本沙箱环境复用该方案，方案本身具备跨会话可重复性，但每次仍需重新执行 `apt-get download`/`dpkg -x`，因为解压产物落在 `/tmp` 下的 session 级 scratchpad 目录，不跨会话持久化）。滚动条位移的验证：向 `window.guestHistory` 注入一条 `response` 含 400 行文本的超长记录，点击该 Recents 条目加载后，用 `document.documentElement.scrollHeight > window.innerHeight` 确认滚动条确实被触发（`scrollHeight` 从初始的 900 涨到 9137），随后点击 `#clearBtn`，断言 `document.documentElement.clientWidth` 在"初始 → 加载长结果 → Clear 后"三个时间点均恒为视口宽度（未修复版本这里会在中间状态出现细微差值），并另外断言 `.container`/`.nav-container` 的 `boundingBox().x` 在 Clear 前后完全相等。导航栏对齐的验证：在 1400×900 桌面视口下测得 `.left-sidebar` 与 `.nav-logo` 的 `boundingBox()` 水平中心点坐标 diff 为 0px（`sidebar_center=104.0` = `logo_center=104.0`），`.nav-links` 右边缘相比 `max-width:1200px` 居中的旧写法更贴近视口右边缘；另在 1920×900（超宽）、700×900（窄桌面/平板）、400×800（移动，含汉堡抽屉展开态）三种视口下截图核对无错位或溢出。这一验证方式没有留下可重复运行的自动化测试文件（纯手动脚本 + 截图核对），后续如果引入前端测试框架并接入真实浏览器环境，应把这类"像素级位置/尺寸是否正确"的断言转成正式的视觉回归测试用例。2026-07-03 同日又追加一次修复（"导航栏白色背景右侧出现缺口"，用户在验证完上一条位移修复后立刻报告）：诊断出上一条修复第一版所用的 `scrollbar-gutter: stable` 会在内容不需要滚动条时留出一段无渲染内容的空白，紧贴视口右边缘的 `.navbar`/`.guest-banner`/`.app-layout` 因此在其右侧出现宽度缺口。用 Playwright 测得缺口的确切成因：`document.documentElement.clientWidth` 报告满宽 `1400px`，但 `.navbar.getBoundingClientRect().width` 只有 `1385px`——两者本该相等而不等，说明依赖 `width:auto` 布局的通栏元素被静默压缩了 `15px`（即被预留但未渲染的滚动条轨道宽度）。改为 `overflow-y: scroll` 后重新测量，`.navbar`/`.guest-banner`/`.app-layout` 三者的宽度在 400/1400/1920 三种视口下均与 `clientWidth` 完全相等（无缺口），且重新跑了一遍前一条修复的"整页水平位移"断言（`clientWidth` 在长结果加载/Clear 前后保持恒定）确认没有把已修好的位移问题再次改坏。同样是纯 CSS 改动，未改动任何 Python 代码，未留下可重复运行的自动化测试文件。2026-07-03 本轮新增只读历史详情页（`GET /history/<history_id>`，`view_history()` + `history.html`）与 Recents 懒加载分页，前者是一次真正的 Python 路由改动，有正式黑盒用例覆盖：新增 `TestViewHistoryPage`（14 个用例）验证匿名访客被重定向且模板变量不泄漏（`isGuestView` 等字符串不出现在响应体里）、已登录找到时返回 200 且 `get_chat_history_by_id` 被以 `session['user_id']`（而非 URL 或请求体里的任何值）+ URL 路径参数调用、prompt/results 被正确注入模板供 JS 消费、`isGuestView = false`；已登录未找到时重定向并带 `"History entry not found"` flash；游客身份下不查询 Firestore（`mock_get.assert_not_called()`）、正确注入 `isGuestView = true` 与 URL 里的 `history_id`；以及三种身份下响应体均不含 `id="compareForm"`/`id="providerSelection"`/`id="customSelectWrapper"`/`id="clearBtn"`（断言"历史只读、没有可提交表单"这一需求的静态标记）。分页部分（`loadMoreSidebarHistory`/`hasMoreHistory`/滚动条样式、`persistGuestHistory`/`loadGuestHistoryFromStorage`、`openHistoryEntry` 取代 `loadHistorySnapshot`）纯前端，无 Python 改动，因此没有新增 black/grey box 用例，验证方式见下方"手动浏览器验证"说明。**手动浏览器验证（2026-07-03，本轮新增功能，未留下可重复运行的自动化测试文件）**：本轮涉及真实的整页导航（`openHistoryEntry` 从内联渲染改为 `window.location.href` 跳转）和跨页面持久化（`sessionStorage`），这类"导航后状态是否正确延续"的行为 jsdom 无法模拟（jsdom 的每个 `JSDOM` 实例对应一个孤立文档，不支持跨实例模拟浏览器同标签页导航），因此复用第 11 节记录过的真实 headless Chromium（Playwright）+ `apt-get download libnspr4 libnss3 libasound2t64 libasound2-data` + `dpkg -x` 解压至用户目录 + `LD_LIBRARY_PATH` 指向解压出的 `.so` 文件这套免 root 沙箱方案（第四次在本沙箱环境复用，方案本身具备跨会话可重复性）驱动一个真实运行中的 `python main.py` 实例，验证了：(1) 游客点击 Recents 条目后整页导航到 `/history/guest-<id>`，目标页面 `#promptDisplay`/`#resultsContainer` 正确显示该条目的 prompt/结果，且页面上确实不存在 `#compareForm`/`#providerSelection`/`#customSelectWrapper`/`#clearBtn` 任何一个元素；(2) 点击该页 `#newChatBtn` 导航回 `/` 后，`window.guestHistory` 仍完整保留（验证 `sessionStorage` 持久化确实生效，而非整页导航后归零）；(3) 访问一个不存在的 guest history id 时 `#historyNotFound` 正确显示；(4) 已登录身份下用伪造的 Flask session cookie（用运行中服务器同一份 `SECRET_KEY` 通过 `SecureCookieSessionInterface` 签名，不写入任何真实 Firestore 数据）+ mock `/api/history` 网络响应验证了 Recents 懒加载分页——滚动到列表底部会按序请求 `page=2`、`page=3`，返回条目数不足 `limit` 后自动停止请求，全程只对 `.sidebar-recents` 的 `scroll` 事件做响应，不做提前预取；(5) 单独验证了游客侧边栏在条目数已经全部本地持有（40 条注入 `window.guestHistory`）的情况下滚动到底不会发出任何 `/api/history` 请求，且 `.sidebar-recents` 的 `scrollbar-width` computed style 确实是 `thin`。**2026-07-03 同日晚些时候用户复测后报告上一轮 Recents 分页修复本身没有真正生效**（新记录增多时旧记录仍逐渐被吞掉、`#sidebarRecents` 右侧不出现 scrollbar、无法无限下滑找回它们）：诊断出根因是纯 CSS 布局问题——`.left-sidebar` 此前一直靠 `.app-layout` 的 `align-items:stretch` 被动拉伸到与 `.main-content` 相同高度，只要历史条目足够多，`#sidebarRecents` 自身内容高度就会成为这次拉伸计算里的最大值，导致它从未真正溢出过（`scrollHeight` 恒等于 `clientHeight`），`overflow-y:auto` 不生效、`scroll` 事件监听器永远不触发，`loadMoreSidebarHistory()` 从一开始就是不可达代码——这正是"渲染后的布局/溢出行为是否正确"这一类问题（与之前"空结果区背景截断"同源），jsdom 无法检测（不做真实盒模型渲染），因此复用第 11 节记录的同一套真实 headless Chromium（Playwright）+ `apt-get download libnspr4 libnss3 libasound2t64 libasound2-data` + `dpkg -x` 解压至用户目录 + `LD_LIBRARY_PATH` 指向解压出的 `.so` 文件的免 root 沙箱方案（第五次在本沙箱环境复用）验证修复：给 `.left-sidebar` 加上 `position:sticky` + 由新增的 `syncSidebarStickyMetrics()` 实测写入的 `top`/`height`（详见第 4 节 `templates/index.html` 与第 9 节对应条目），`#sidebarRecents` 加上 `min-height:0`。验证覆盖：(a) 游客身份下 `sessionStorage` 注入 40 条记录后 `#sidebarRecents.scrollHeight`（1551px）确实大于 `clientHeight`（912px，修复前二者恒等）；(b) 用 `flask.sessions.SecureCookieSessionInterface().get_signing_serializer()` + 运行中服务器同一份 `SECRET_KEY` 签发伪造 session cookie（`{'user_id': ..., 'username': ...}`，不写入任何真实 Firestore 数据）模拟已登录身份，配合拦截 `window.fetch('/api/history?...')` 的 JS 桩返回分好页的假数据（共 55 条），在 1400×650 视口下驱动真实的 `scrollTop = scrollHeight` + 派发 `scroll` 事件，确认确实依次请求了 `page=2`、`page=3`（分别渲染累计到 40、55 条），且在 `page=3` 返回不足一整页（15<20）后 `hasMoreHistory` 正确停止请求，不会再发 `page=4`；(c) 复测"侧边栏背景不截断"这条已有不变量在改为 `position:sticky` 后依然成立，并新增覆盖了此前从未测过的"结果区异常长"场景（注入 80 段结果文本、`scrollHeight` 达视口高度 6 倍以上），滚动到页面顶部/1/3/1/2/底部等多个位置测量 `.left-sidebar` 的 `boundingBox()`，底边与视口底边的差值始终 `< 3px`；(d) 400×800 移动视口下确认 `.left-sidebar` 的 computed `position` 仍是 `fixed`（桌面端新增的 `sticky` 规则未污染移动端媒体查询覆盖），汉堡按钮点击后抽屉仍正常滑入滑出。这一验证方式同样没有留下可重复运行的自动化测试文件（纯手动脚本），未新增/修改任何 Python black/white/grey box 测试用例——本次改动只涉及 `templates/index.html`/`templates/history.html` 的 CSS 与 JS（新增 `syncSidebarStickyMetrics()` 函数），未触碰 `main.py`/`auth/`，294 个既有 Python 用例改动前后全部保持通过。**同一天用户紧接着又报告"滚动/溢出机制确实生效了，但滚动条本身在 Chrome 标签页里还是看不到，VS Code 内置预览里却正常"**：这条对比线索直接指向 Chrome 的原生 overlay（悬浮式、不占布局空间、仅交互时短暂淡入）滚动条渲染模式，而非功能性回归。用真实 headless Chromium 实测确认：`#sidebarRecents.offsetWidth === #sidebarRecents.clientWidth`（差值 0px），证明此前写的 `::-webkit-scrollbar` 自定义样式并未能把 Chrome 压回经典（持久占位）滚动条模式——这是较新 Chromium 版本收紧非标准滚动条覆盖能力后的已知限制，不是配置疏漏。第一层修复（轨道从 `transparent` 改为半透明背景、thumb 透明度/宽度提升）保留作为对 Firefox 等仍支持标准 `scrollbar-color`/`scrollbar-width` 的浏览器的加成，但真正的修复是新增一个完全自定义、不依赖浏览器原生滚动条渲染的 JS 驱动视觉指示器：`#sidebarRecentsScrollThumb`（`#sidebarRecents` 的兄弟节点而非子节点，避免被 `renderCurrentHistory()` 的 `innerHTML` 整体替换清空）+ 新增函数 `updateSidebarScrollIndicator()`，按 `scrollTop`/`scrollHeight`/`clientHeight` 的比例实时计算并写入指示器的 `top`/`height`，挂载在页面加载、`window.resize`、`#sidebarRecents` 的 `scroll` 事件、`renderCurrentHistory()` 末尾四个触发点。**修复过程中的一次真实踩坑**：验证脚本最初测出指示器位置偏高约 15px、且滚到底部时提前停在距真实底边约 160px 处——这正是本文件反复出现的 `zoom` 二次缩放陷阱（`getBoundingClientRect()` 返回已缩放的物理像素，直接赋给 `style.top`/`style.height` 会被渲染时的 `zoom` 再乘一次），补上"由 `getBoundingClientRect()` 得来的长度需除以 `--page-zoom`、纯比例值（`clientHeight/scrollHeight` 等）不需要"这一区分后，误差降到 1px 以内。用真实 headless Chromium 验证：鼠标完全移开、不做任何滚动/悬停的静态状态下指示器 `.visible` class 已存在、`opacity` 计算值为 `1`（直接复现"完全不交互也能看见"这一诉求）；`scrollTop=0` 时指示器与 `#sidebarRecents` 顶边对齐（误差 0px）；滚到底部后指示器下移且与底边对齐（误差 <1px）；复测了此前几轮修复的全部既有断言（游客/已登录分页触发、背景不截断、移动端抽屉）均未受影响。纯 CSS+JS 修复，未新增/修改任何 Python 测试用例，294 个既有用例全部通过。**2026-07-03 晚些时候用户又报告三个相关问题："Recents 黑色列右侧现在有 2 个 scrollbar，只保留 1 个可以拖拽的"、"需要缩放到约 110% 才能看见这个自绘滚动条"、"150% 缩放后滚到整页底部时 Recents 黑色列下方会露出空缺"，以及"同样地整体右侧的 scrollbar 也要进行类似修改"**：全部四点均未改动任何 Python 代码，纯 CSS+JS 修复（`templates/index.html`/`templates/history.html` 逐字同步）。根因有两个：(1) `#sidebarRecents` 此前仍保留了原生滚动条（只是外观被定制、Chrome 默认按 overlay 模式隐藏），只要用户主动滚动/悬停就会与恒定可见的自绘指示器同时出现（"2 个 scrollbar"），且原生滚动条作为浏览器 UI 合成层的一部分天然盖在页面内容之上，只要它还占据任何可交互区域就会抢先拦截 `mousedown`，导致自绘指示器根本无法做到"可拖拽"——修复为彻底隐藏原生滚动条（`scrollbar-width:none`+`::-webkit-scrollbar{display:none}`，恒为 0 宽度）并新增 `setupThumbDrag()` 赋予自绘指示器真正的拖拽能力（`mousedown`/`mousemove`/`mouseup`+对应触摸事件），整页的 `html`（原 `overflow-y:scroll`）与新增的 `#pageScrollThumb`/`updatePageScrollIndicator()` 同步采用同一套方案；(2) `.left-sidebar` 的高度此前由 JS 对 `window.innerHeight`（取整后的整数）做手工像素运算再注入字面量像素值，与浏览器自身缩放叠加产生的舍入误差是"需 110% 缩放才可见"（算多了，误判未溢出）和"150% 缩放露出空缺"（算少了，实际渲染高度不足）两个问题的共同根因——修复为 `height` 改用纯 CSS `calc(100vh / var(--page-zoom) - var(--sidebar-header-height) + 2px)`，`100vh` 交给浏览器原生解析、不再经过 JS 侧取整数值的二次运算，`syncSidebarStickyMetrics()` 相应简化为只测量并写入 `--sidebar-header-height` 一个变量。用真实 headless Chromium（Playwright）验证：两处原生滚动条实测占用宽度均为 0；两个自绘指示器无需任何交互即可见（不再依赖特定缩放比例）；分别拖拽两个指示器确认真的驱动了对应的 `scrollTop`/`scrollY` 变化；模拟更高 `device_scale_factor` 后滚动到底部测得 `.left-sidebar` 底边与视口底边差值仅 0.42px；复测"加载长结果再 Clear 不引发水平位移"这条更早的不变量确认未被本轮改动破坏。294 个既有 Python 用例改动前后全部保持通过，未新增/修改任何 black/white/grey box 测试用例——这类问题同属"渲染后的滚动/溢出/拖拽行为是否正确"，jsdom 不做真实布局和指针事件模拟、无法覆盖，因此延续本节一贯的处理方式：只做手动 Playwright 验证，不留自动化测试文件。详见第 4 节 `templates/index.html`、第 9 节对应条目。**2026-07-03 晚些时候，用户要求把项目里所有 `--page-zoom: 0.8` 的地方统一改为 `1.1`（110% 缩放）**：改动前先用 `grep -rn "page-zoom"` 全仓库搜索确认唯二定义处是 `templates/index.html`/`templates/history.html` 各自 `:root` 块内的这一行，没有任何 `.py`/测试文件引用过这个 CSS 变量或其数值，因此这类改动天然落在本节反复强调的"jsdom/`unittest` 无法覆盖、纯渲染验证"范畴，不涉及新增/修改任何 black/white/grey box 用例；改动前先跑一遍 `python -m unittest discover -s tests` 确认基线是 294 个用例全部通过，改动后原样保持 294 个全部通过（预期结果，因为没有测试断言过这个值）。验证用真实 headless Chromium（Playwright，复用本节记录的 `apt-get download libnspr4 libnss3 libasound2t64 libasound2-data` + `dpkg -x` 解压至用户目录 + `LD_LIBRARY_PATH` 免 root 沙箱方案）驱动一个真实运行中的 `python main.py` 实例：读取 `getComputedStyle(document.documentElement).getPropertyValue('--page-zoom')` 和 `getComputedStyle(document.body).zoom` 确认两个页面均生效为 `1.1`；复测了"`.left-sidebar` 深色背景铺满到视口底部、不露出下方浅色背景"这条历次修复反复守护的核心不变量在新缩放值下依然成立（1400×900 视口下差值 `-2.19px`/`0.36px`，均未出现正值的可见缺口）。这一验证同样没有留下可重复运行的自动化测试文件，与本节其余纯 CSS 数值/渲染类改动的处理方式一致。详见第 4 节 `templates/index.html` 条目下"全局缩放比例统一调整为 110%"、第 13 节对应 Memory Anchor。2026-07-03 文生图功能落地新增 `TestGetImageProvidersEndpoint`（6 个用例，与 `TestGetProvidersEndpoint` 同构，另含一条 `test_expected_five_researched_providers_present` 锁定调研确认的 5 个 Provider 名字集合不被意外增删）与 `TestGenerateImagesEndpoint`（17 个用例，与 `TestCompareEndpoint` 同构：成功 200、响应体顶层 key 完整、图片 8-key result 字段完整、`total_providers` 与 `results` 长度一致、排序契约、`prompt` 回显、未选 Provider 时默认测试全部 `IMAGE_PROVIDERS`、缺 `prompt`/空 body/未知 Provider 名返回 400、`G4F_AVAILABLE=False` 返回 503、Provider 异常时错误信息透传，以及一条 `test_text_provider_name_is_rejected_as_invalid` 专门验证文本 Provider 名字（如 `Yqcloud`）不会被图片路由误识别为合法值，锁定两套 Provider 名字空间相互隔离这一设计），均通过 `patch('main.G4FImageClient')` mock 掉真实的 `g4f.client.Client`，不要求 `@unittest.skipUnless(main.G4F_AVAILABLE, ...)`。
- **test_main_graybox.py**：感知后端全局状态与线程池行为。测试内容包括排序契约、fallback 路径、`max_workers` 双重上限、`G4F_AVAILABLE` 降级响应，以及三 Provider 场景下 1 个失败时互评仅在 2 个成功者之间双向触发的行为（`TestPeerReviewPartialFailure`）。排序测试（`TestSortOrderInvariant`）在 setUp 中 mock `run_peer_review`，避免 2 个成功者触发真实网络调用。新增 `TestPeerReviewPhaseRobustness`（3 个用例）：通过令 `PEER_REVIEW_PROMPTS_MAP.get` 在任务构建阶段抛出 `RuntimeError`，模拟互评阶段整体崩溃，断言接口仍返回 200、第一轮结果完整、`peer_reviews` 字段为空列表。新增 `TestTestSingleRobustness`（1 个用例）：令 `test_g4f_provider` 直接抛出异常以触发 `test_single_provider` 外层 except，断言返回 500 英文友好消息（`Service temporarily unavailable...`，2026-07-02 起由中文改为英文）。新增 `TestHealthConfigFlags`（2 个用例）：分别将 `ROUTE_PROMPTS_MAP` 和 `PEER_REVIEW_PROMPTS_MAP` patch 为空字典，断言 `/health` 对应标志字段为 `False`。新增 `TestSaveHistoryRobustness`（2026-07-02，3 个用例）：令 `save_chat_history` 抛出异常或返回 `None`，断言 `/api/compare` 仍返回 200 且 `results` 完整，`history_id` 安全回退为 `null`，验证该调用的独立 try-except 隔离与互评阶段的隔离设计一致。新增 `TestPeerReviewOuterTimeoutValue`（2026-07-03，2 个用例，对应"互评阶段对较慢 Provider 频繁误判超时"的修复）：`test_peer_review_future_result_waits_up_to_32_seconds` 通过 `patch.object(concurrent.futures.Future, 'result', spy_result)` 包一层记录调用参数的 spy（内部仍调用原始 `result` 完成真实等待，不影响其他测试断言），驱动一次真实的两 Provider 互评流程，断言互评阶段确实以 `timeout=32` 调用 `future.result()`、且不再出现修复前的旧值 `25`（防止外层超时被意外改回）；`test_peer_review_timeout_error_does_not_crash_and_leaves_that_review_missing` 沿用 `TestThreadTimeoutFallback` 同款手法——让 `main.run_peer_review` 直接 `side_effect=concurrent.futures.TimeoutError(...)`（模拟"函数本身抛出超时异常"而非真的等待 32 秒），断言路由仍返回 200、第一轮结果的 `success` 与 `peer_reviews: []` 不受影响。2026-07-03 文生图功能落地新增四个测试类，均与既有的对应文本测试类同构：`TestImageSortOrderInvariant`（3 个用例，排序契约）、`TestImageThreadTimeoutFallback`（6 个用例，`TimeoutError` 与普通异常两种降级路径、8-key fallback 字段完整性、友好文案、`determine_actual_image_model` 降级规则）、`TestImageMaxWorkersConstraint`（2 个用例，`max_workers` 双重上限约束）、`TestImageGlobalDegradationState`（2 个用例，`G4F_AVAILABLE=False` 时 `/api/image-providers` 返回空列表、`/api/generate-images` 返回 503）；另新增 `TestImageOuterTimeoutValue`（2 个用例，与 `TestPeerReviewOuterTimeoutValue` 同款 spy 手法）锁定 `IMAGE_GENERATION_OUTER_TIMEOUT`（`45`）确实被传给 `future.result()`、`IMAGE_GENERATION_ADVISORY_TIMEOUT`（`40`）确实被传给 `images.generate()` 的 `timeout` 关键字参数，防止这两个为文生图冷启动延迟专门放宽的数值被后续改动意外调小又不同步。
- **test_auth_whitebox.py**：使用 `unittest.mock.patch` 绕过真实 Firebase 网络连接。测试内容包括 Werkzeug 密码哈希生成与校验，`get_user_by_username` 和 `get_user_by_email` 两个函数在"用户存在"和"用户不存在"两个分支下的行为，以及（2026-07-02 新增 5 个、2026-07-03 追加 `TestGetChatHistoryById` 共 6 个）对话历史 CRUD 函数的完整白盒测试（`TestSaveChatHistory`、`TestGetChatHistoryList`、`TestGetChatHistoryById`、`TestDeleteChatHistory`、`TestUpdateChatHistoryTitle`、`TestTogglePinChatHistory`，共 41 个用例）。`TestGetChatHistoryById`（2026-07-03 新增，6 个用例，供只读 `/history/<id>` 详情页使用）沿用与 `TestDeleteChatHistory` 完全相同的 mock 模式（`mock_db.collection().document().get()` 返回可控的 `mock_doc`），覆盖：找到且归属正确时返回含 `id` 键的完整字典、归属他人时返回 `None`、文档不存在时返回 `None`、`FIREBASE_AVAILABLE=False` 时返回 `None`、以及一个额外的"纯读取不触发任何写操作"断言（`update`/`delete` 均 `assert_not_called()`，区别于其余四个写操作函数）。其余用例覆盖 `title` 截断规则（`prompt[:15] + ('...' if len(prompt) > 15 else '')`；`TestSaveChatHistory` 含 `test_title_equals_prompt_verbatim_when_not_longer_than_limit` 和 `test_title_has_no_ellipsis_when_prompt_exactly_15_chars` 两个用例，2026-07-02 因短 prompt 误加省略号的 Bug 修复新增，断言 `len(prompt) <= 15` 时标题就是 prompt 原文、不追加省略号）、`is_pinned` 默认 `False`、`results` 原样保留、以及 delete/update/toggle 三者的归属校验分支（文档不存在、文档属于其他用户两种拒绝场景）与 `FIREBASE_AVAILABLE=False` 时各自的 fallback 返回值。`TestGetChatHistoryList`（2026-07-02 因生产事故重写，共 12 个用例）断言查询链**只**调用一次 `.where('user_id', '==', ...)`、**不**调用 `.order_by()`（`test_query_does_not_chain_order_by_avoiding_composite_index`，防止未来又串回需要复合索引的写法），并直接在 Python 侧验证排序正确性：置顶项无论新旧都排在未置顶项之前（`test_sorts_pinned_items_before_unpinned_regardless_of_recency`）、未置顶组内按 `created_at` 降序（`test_sorts_by_created_at_descending_within_unpinned_block`）、`offset`/`limit` 在排序后的完整列表上做 Python 切片而非交给 Firestore（`test_applies_limit_and_offset_in_python_after_sorting`）、缺失 `created_at` 字段时排序不崩溃（`test_missing_created_at_does_not_crash_sort`）；2026-07-02 本轮新增 `test_sorts_pinned_items_by_pinned_at_ascending_oldest_pin_first`（两个置顶项，先置顶的 `pinned_at` 更早但 `created_at` 反而更晚，断言排序仍以先置顶的排最前——直接复现并验证修复了"第二个 Pin 顶替第一个"这一用户报告的 Bug）和 `test_missing_pinned_at_for_pinned_item_does_not_crash_sort`（置顶项缺失 `pinned_at` 字段时的防御性兜底）。`TestTogglePinChatHistory`（共 7 个用例）2026-07-02 本轮新增 `test_pin_sets_server_timestamp_sentinel_not_a_python_datetime` 和 `test_unpin_clears_pinned_at_with_delete_field_sentinel`，断言置顶时 `update()` 调用参数里的 `pinned_at` 是 `firestore.SERVER_TIMESTAMP` 哨兵值（而非 Python 端计算的 `datetime.now()`）、取消置顶时是 `firestore.DELETE_FIELD` 哨兵值（而非设为 `None`）；原有的 `test_toggle_pins_when_currently_unpinned`/`test_toggle_unpins_when_currently_pinned` 两个用例的 `update.assert_called_once_with(...)` 断言也同步更新为包含 `pinned_at` 键。
- **test_auth_blackbox.py**：通过 Flask `test_client()` 测试 auth 蓝图的全部路由。测试内容包括登录成功后 `session['user_id']` 的写入与重定向、注册成功后 `session['is_guest']` 的清除、`/profile` 路由的未登录重定向以及已登录用户的正常访问。对话历史路由的黑盒测试放在 `test_main_blackbox.py`（而非本文件），因为这些路由挂载于 `main.py` 而非 `auth_bp` 蓝图。**2026-07-03 本轮 UI 比例规范化调整**（`history.html` 说明文案两行化 + `.header p` 补 `line-height`、`#promptDisplayContainer .form-group` 的 `margin-bottom:0` 覆盖、`auth/base.html` 新增 `.card-title` 并让 `login.html`/`register.html`/`profile.html` 三个页面的 `<h2>` 改用它取代内联样式）均未改动任何 Python 代码，未新增/修改任何 black/white/grey box 测试用例：改动前逐一 `grep` 确认没有任何既有用例断言过 `Read-only view of a saved comparison. Nothing here can be edited or resubmitted.`（旧的单行文案）、`card-title`、`promptDisplayContainer`、或 `form-group` 相关的具体样式/文案字符串，改动后running `python -m unittest discover -s tests` 294 个用例全部仍然通过；另外通过 `app.test_client()` 直接渲染 `/history/<id>`、`/login`、`/register` 三个页面并断言响应体确实包含新文案/新 class、且三个 `<h2>` 上不再残留旧的内联 `style="color: #333..."`，作为改动生效的快速核对（非正式测试用例，未写入 `tests/` 目录）。这批改动都是纯 CSS 数值/文案/class 名调整，不涉及像素级布局算法变化（不同于本节其余需要真实 headless Chromium 才能验证的布局 Bug），因此手动核对渲染结果已足以确认改动生效，未额外动用 Playwright。

```bash
# 发现并运行 tests/ 目录下的全部测试（共 357 个用例，5 个测试文件；2026-07-03 文生图功能落地新增 63 个）
python -m unittest discover -s tests

# 带详细输出运行
python -m unittest discover -s tests -v

# 单独运行 main.py 相关测试
python -m unittest tests.test_main_whitebox
python -m unittest tests.test_main_blackbox
python -m unittest tests.test_main_graybox

# 单独运行 auth 模块相关测试
python -m unittest tests.test_auth_whitebox
python -m unittest tests.test_auth_blackbox
```

### 快速冒烟测试（手动 curl）

```bash
# 健康检查
curl http://localhost:8080/health

# 获取 Provider 列表
curl http://localhost:8080/api/providers

# 单 Provider 测试
curl -X POST http://localhost:8080/api/test-single \
  -H "Content-Type: application/json" \
  -d '{"prompt": "What is 2+2?", "provider": "Yqcloud"}'

# 多 Provider 对比
curl -X POST http://localhost:8080/api/compare \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Hello", "providers": ["Yqcloud", "OperaAria"]}'

# 建立游客 session（需 Cookie 支持）
curl -X POST http://localhost:8080/api/auth/guest
```

### Provider 可用性探测脚本（g4f 库升级后使用）

```bash
cd availability_g4f

# 第一步：扫描 g4f 所有 working Provider
python find_providers_models.py
# 结果写入 available_providers_models.txt（当前目录 availability_g4f/ 内，不是 ../，脚本内路径不含 ../）

# 第二步：对候选 Provider 发真实请求验证可用性
python test_providers.py
# 结果写入 ../provider_test_results_v2.txt
```

### 文生图 Provider 可用性探测脚本（[NEW，2026-07-03]，g4f 库升级后复测用）

```bash
cd availability_g4f

# 第一步：扫描 g4f 所有具备图片生成能力标记的 Provider（不限 working/needs_auth，原样记录供人工判断）
python find_image_providers.py
# 结果写入 available_image_providers_models.txt（当前目录 availability_g4f/ 内，不是 ../，脚本内路径不含 ../）

# 第二步：对 working=True 且 needs_auth=False 的候选发真实请求验证是否真的能生成图片
python test_image_providers.py
# 结果写入 ../image_provider_test_results.txt（单次全量运行约需数分钟，每个候选最长等待 45 秒硬超时）
```

上一轮（2026-07-03，g4f 7.6.4）的结论已人工汇总进 `availability_g4f/available_free_image_providers.txt`（gitignored，不随仓库分发），确认可用的 5 个免 Key 组合与 Gemini/Google AI Studio 不可用的证据详见第 4 节 `availability_g4f/` 条目下的"文生图 Provider 调研结论"。仅在 g4f 库升级后怀疑现有结论过期时才需要重新运行这两个脚本。

### 依赖管理

- `requirements.txt` 采用完全锁版本策略（`pip freeze` 输出格式），所有间接依赖均固定。例外：`gunicorn` 仅写包名不含版本（GAE 运行时负责安装，本地开发环境通常不安装）。
- 更新依赖时：在虚拟环境中 `pip install <package>`，然后 `pip freeze > requirements.txt`。
- 不要手动编辑 `requirements.txt` 中的版本号，避免依赖冲突。
- 新增依赖后，必须同步检查 `app.yaml` 的 runtime 是否支持该依赖。

### 部署到 Google App Engine

```bash
# 需已安装并配置 Google Cloud SDK (gcloud)
gcloud app deploy app.yaml

# 查看实时日志
gcloud app logs tail -s default
```

- GAE 使用 `app.yaml` 中的 `entrypoint: gunicorn -b :$PORT main:app` 启动服务。
- Runtime 为 `python312`，自动缩放 1 到 10 个实例，CPU 目标利用率 60%。
- `SECRET_KEY` 必须在 `app.yaml` 的 `env_variables` 块中设置，GAE 环境无 `.env` 文件。
- `firebase-key.json` 不部署到 GAE，GAE 使用 Application Default Credentials 自动获取凭据。

---

## 12. ✏️ CODE STYLE & CONVENTIONS

### Python 代码规范

- **命名约定**：
  - 全局常量：`UPPER_SNAKE_CASE`（如 `G4F_AVAILABLE`、`FIREBASE_AVAILABLE`、`PROVIDER_MODELS_MAP`）
  - 函数/变量：`lower_snake_case`（如 `test_g4f_provider`、`provider_name`）
  - Flask 路由函数名与路径语义对齐（如 `compare_providers` 对应 `/api/compare`，`guest_login` 对应 `/api/auth/guest`）

- **日志规范**：
  - 使用模块级 `logger = logging.getLogger(__name__)`，不使用 `print`。
  - `INFO` 记录正常流程节点（如请求开始、Provider 完成、Firebase 初始化成功）。
  - `ERROR` 用于异常，必须带 `exc_info=True` 以打印完整堆栈。
  - 日志消息中截断长字符串：`prompt[:50]`，避免日志膨胀。

- **错误处理模式**：
  - LLM 路由函数顶层用 `try/except Exception` 兜底，返回标准 JSON 错误体 `{'error': '...'}` 和对应 HTTP 状态码。
  - auth 路由函数顶层用 `try/except Exception` 兜底，通过 `flash()` 反馈错误，`render_template` 返回页面。
  - `test_g4f_provider` 内部用 `try/except/finally`，在 `finally` 中计算 `response_time`，确保耗时字段始终有值。
  - Firebase 初始化用两层 except（`ImportError` 和 `Exception`）分别处理库缺失和初始化异常。

- **结果字典结构不变原则**：`test_g4f_provider` 返回的字典 key 集合（`provider`、`success`、`response`、`error`、`response_time`、`model`、`type`）为前后端契约，严禁增删 key。

### JavaScript / 前端规范

- 使用 Vanilla JS，不引入任何前端框架或构建工具。
- 前端通过 Jinja2 变量 `{{ provider_models_json | tojson }}` 接收后端数据，在页面初始化时解析为 JS 对象。
- Fetch 请求必须先检查 `response.ok` 再调用 `response.json()`。非 2xx 响应时先尝试解析 JSON `error` 字段，解析失败则回退到 `Server error: 状态码`。
- 游客切换相关的 Fetch（`/api/auth/guest`）：`home.html` 中的专属游客按钮实现完整交互（disabled + "Loading..." 文案 + 失败时恢复原始文案 + 显示 `#guestError`）。`login.html` 和 `register.html` 中的 "Continue as guest" 是简化实现（仅在 `res.ok` 时跳转，无 Loading 状态），因为这两处是辅助入口而非主路径。

### 提交规范

- 提交信息用中文或英文均可，项目历史两者混用。
- 保持原子提交：一次提交只做一件事。

## 13. 🧠 MEMORY ANCHORS (FOR CLAUDE CODE)

- **LLM 核心调用链路（两阶段 + 可选持久化）**：`index.html (Form Submit)` + `POST /api/compare` + `ThreadPoolExecutor[1]` + `test_g4f_provider()` + `g4f.ChatCompletion.create()` → 收集结果 → `ThreadPoolExecutor[2]` + `run_peer_review()` + `parse_peer_review_json()` → 挂载 `peer_reviews` → 排序 → 若已登录调用 `save_chat_history()`（独立 try-except，2026-07-02 新增）→ 挂载 `history_id` → 返回 JSON。
- **认证核心调用链路**：`home.html (Login/Register/Guest)` + `auth Blueprint` + `Firebase Firestore` + `session 写入` + `redirect url_for('index')`。
- **身份状态入口**：根路由 `/` 的 `index()` 函数是唯一的身份状态路由器，所有重定向最终都回到这里。
- **关键不变量 1**：LLM 对比结果的排序始终是"成功在前，耗时短在前"。
- **关键不变量 2**：`session['user_id']` 和 `session['is_guest']` 永远不同时存在。
- **关键不变量 3**：所有作为 Flash + redirect 目标的页面，必须包含 Flash 消息显示区，否则会产生消息堆积 Bug。
- **关键不变量 4**：`test_g4f_provider` 的返回值严格为 7-key 契约；`peer_reviews` 字段由 `compare_providers` 在外层追加，使最终 result 共有 8 个 key；`run_peer_review` 的返回值严格为 `{reviewer_provider, reviewer_model, score, comment}` 4-key 契约。
- **异常文案判定顺序（2026-07-03 新增，`test_g4f_provider` 与 `run_peer_review` 共用）**：`CONTENT_POLICY_ERROR_KEYWORDS` 必须先于 `NETWORK_ERROR_KEYWORDS`/`PEER_REVIEW_NETWORK_ERROR_KEYWORDS` 判定，因为 Provider 底层内容审查（如 Azure OpenAI `content management policy`）报错重试无意义，误判成"系统正忙"会诱导用户做无效重试。
- **互评超时调优（2026-07-03）**：`run_peer_review` 内部 `g4f.ChatCompletion.create` 的 advisory `timeout` 由 `15` 上调到 `25`，`compare_providers` 第二阶段 `future.result` 的外层等待由 `25` 上调到 `32`（约 7 秒调度缓冲），解决 Yqcloud 等较慢 Provider 在互评阶段（prompt 含完整被评回答、比首轮更慢）频繁被误判超时（`Peer review for <provider> timed out after 25s`）的问题。两值必须同步调整，且需重新核算 429 重试场景的总耗时上限仍小于外层超时。
- **互评触发条件**：`tested >= 2` 且 `success >= 2`，缺一不触发。只有成功者相互点评，失败者不参与任何方向。
- **`parse_peer_review_json` 容错行为**：任何解析失败（格式错误、缺失 score、异常）均返回 `(80, raw_text)`，不抛出异常，接口永不因互评解析失败而崩溃。
- **核心文件**：LLM 后端入口为 `main.py`，认证后端为 `auth/routes.py` 和 `auth/db.py`，三个前端入口分别为 `templates/home.html`（未认证）、`templates/index.html`（已认证或游客）、`templates/auth/` 目录（认证流程页）。
- **自适应策略**：指定模型不匹配时，自动降级为映射表中的第一个模型，最终兜底为 `"gpt-3.5-turbo"`。
- **Firebase 初始化顺序**：key 文件优先于 ADC，不能反转，因为 ADC 的构造函数不立即抛出异常。
- **`get_chat_history_list` 复合索引事故修复（2026-07-02）**：曾经的实现对 `history` 集合做 `.where(user_id).order_by(is_pinned).order_by(created_at)` 复合查询，这需要在 Firebase 控制台手动创建复合索引（不随代码库提交），本地/生产各环境若未创建就会让 `GET /api/history` 恒定 500（`FAILED_PRECONDITION: The query requires an index`），表现为侧栏卡在"Could not load history."且新对话存档后侧栏也刷新不出来。现已修复为只做单字段等值查询 + Python 应用层排序分页，不再依赖任何手动创建的 Firestore 索引，新环境开箱即用。详见第 9 节风险审计。
- **对话历史持久化现状（2026-07-02，全栈落地）**：数据层 `auth/db.py` 实现 5 个 CRUD 函数（`save_chat_history`、`get_chat_history_list`、`delete_chat_history`、`update_chat_history_title`、`toggle_pin_chat_history`），操作 `history` 集合；路由层 `main.py` 挂载对应端点（`/api/compare` 自动保存 + `/api/history` 系列 4 个端点）；前端 `templates/index.html` 的左侧边栏（`.left-sidebar`，260px 深色 `#171717`）消费这些端点，按 `Today`/`Yesterday`/`Previous 7 Days`/`Older` 时间分组展示 Recents，条目 hover/focus-within 时淡入固定/重命名/删除三个操作图标。三个写操作类函数（delete/update/toggle）内部自带归属校验，路由层调用时 `user_id` 必须取自 `session['user_id']`，不可信任前端传入值。`toggle_pin_chat_history` 返回值判空须用 `is None`，`False` 是合法成功结果不是失败（前端与后端路由两处都遵守这条）。`save_chat_history` 的调用在 `compare_providers()` 中被独立 try-except 包裹，失败不影响 `/api/compare` 本次结果返回，`history_id` 安全回退为 `None`。移动端（`<=520px`）侧边栏切换为 `position:fixed` 抽屉，由 `.hamburger-btn`（嵌在 `.nav-left` 内，与 `.nav-logo` 分组）通过 `sidebar-open`/`visible` class 驱动 `transform:translateX()` 滑入滑出，桌面端这两个 class 不产生任何视觉效果。
- **侧边栏交互层：乐观更新 + 游客内存模拟（2026-07-02 步骤四，纯前端，未改动任何 Python 代码）**：`currentHistoryItems` 是渲染 `#sidebarRecents` 的唯一数据源——已登录时是从 `/api/history` 拉取的数组，游客时**直接是 `window.guestHistory` 的同一引用**（非拷贝），因此原地改条目字段即等价于写入 `window.guestHistory`。`toggleHistoryPin`/`deleteHistoryItem`/`commitRename` 均先本地改数据 + `renderCurrentHistory()`（DOM 立即响应），已登录用户的请求再异步发出；失败时精确回滚到操作前状态并 `showHistoryErrorToast()` 弹出 3 秒提示，游客分支因为没有网络请求所以没有回滚路径。原地重命名 `startInlineRename` 取代了旧的 `prompt()` 弹窗方案，点击 ✏️ 或双击标题都会把 `.history-item-title` 换成 `<input>`，`Enter` 走 `blur` 提交、`Escape` 走独立取消路径（用 `settled` 标志位防止两者重复触发）。游客提交 `/api/compare` 成功且 `history_id` 为空时，前端会把结果 `unshift` 进 `window.guestHistory` 并重渲染，完整模拟"新对话出现在 Recents 顶部"的已登录体验，且游客的任何后续 pin/rename/delete 都不会碰 `/api/history*` 端点。条目按钮从内联 `onclick` 重构为 `data-action` 属性 + `#sidebarRecents` 上的委托 `click`/`dblclick` 监听器（只挂载一次，不受 `renderCurrentHistory()` 反复覆盖 `innerHTML` 影响）。此层逻辑无 Python 测试覆盖，通过 jsdom 手动验证（详见第 11 节）。
- **侧边栏空结果区背景截断修复（2026-07-02）**：`.app-layout` 的 `align-items:stretch` 只能让 `.left-sidebar` 与 `.main-content` 彼此等高，不能让二者一起撑满视口——结果区为空时两者一起矮下去，深色侧边栏就在视口底部之前截断。修复为 `body{display:flex;flex-direction:column;min-height:calc(100vh / var(--page-zoom))}` + `.app-layout{flex:1}`，为空结果区提供一个视口高度的地板；`--page-zoom`（`:root` 定义，此次修复发生时为 `0.8`，2026-07-03 当天先被统一调整为 `1.1`、随后又改回 `0.8`、再改为当前值 `0.88`，见 §4 `templates/index.html` 条目下"全局缩放比例统一调整为 110%"、"全局缩放比例改回 80%"与"全局缩放比例从 80% 调整为 88%"三条目）必须参与这个 `calc`，因为 `body` 本身还叠了 `zoom:var(--page-zoom)` 这个非标准缩放，`vh` 不会跟着 `zoom` 一起放大，直接用 `100vh` 会在视口底部留出空白（`zoom < 1` 时）或让内容溢出视口（`zoom > 1` 时）。纯 CSS 修复，无 Python 改动；用真实 headless Chromium（而非 jsdom，jsdom 不做真实布局渲染）测量 `boundingBox()` 高度验证。
- **Recents 三个小 Bug 修复（2026-07-02，用户报告）**：(1) 短 prompt 标题误加省略号——`save_chat_history`（`auth/db.py`）与前端游客模拟均已改为 `len(prompt) > 15` 才追加 `'...'`，短 prompt（如 `"hi"`）直接用原文做标题；(2) 重命名输入框文字不可读——`.history-rename-input` 原本深色配色（`#ececec` 字 + `#262626` 底）在 `input.focus()` 触发全局 `input:focus` 规则（选择器特异性 `(0,1,1)` 高于单一类选择器）后背景被强制变白、浅灰字糊在白底上，修复为改用浅色配色（`#1a1a1a` 字 + `#ffffff` 底）并新增 `.history-rename-input:focus`（特异性 `(0,2,0)`，稳赢全局规则）显式锁定；(3) Pin 后条目未移到顶部——`toggleHistoryPin` 只翻转 `is_pinned` 就重渲染，但 `renderCurrentHistory()` 此前直接对 `currentHistoryItems` 分组，数组元素顺序从不因 pin 状态变化而重排，修复为新增 `sortHistoryItems(items)` 在分组前对拷贝重新排序。三者中只有第一项涉及 Python 改动（`TestSaveChatHistory` 新增 2 个用例），后两项是纯前端修复，通过 jsdom 的 `getComputedStyle` 和 DOM 顺序断言验证（不需要真实 Chromium，因为验证的是 computed style 和元素相对顺序，不是像素级布局高度）。
- **Pin 逻辑第二轮修复：仿照 Gemini 网页版（2026-07-02，用户进一步报告）**：上一条修复的 `sortHistoryItems` 仍用 `created_at` 降序排置顶组，导致"后置顶但对话创建时间更新的条目"会顶替"先置顶但对话创建时间更早的条目"。本轮引入 `pinned_at` 字段专门记录"置顶操作发生的时刻"（后端 `toggle_pin_chat_history` 置顶写 `firestore.SERVER_TIMESTAMP`、取消置顶用 `firestore.DELETE_FIELD` 删除；前端 `toggleHistoryPin` 本地记录 `new Date().toISOString()`），置顶组改按 `pinned_at` **升序**排序（最早置顶的稳定在最上面，后续新置顶依次排在下面）。同时仿照 Gemini 网页版新增持久化置顶图标：已置顶条目的绿色大头针图标脱离 hover-only 的 `.history-item-actions` 分组，改用独立、`opacity:1` 恒定可见的 `.pin-indicator`（固定在行最右侧），未置顶条目则仍是 hover 才出现的灰色 Pin 按钮，二者互斥渲染，解决了"Pin/Unpin 无图标区分"的问题。另外修复了点击 Pin 后条目闪烁两下的 Bug：`toggleHistoryPin` 的服务端确认回调之前无条件二次 `renderCurrentHistory()`（哪怕状态没变也重建整个 DOM），改为只有 `data.is_pinned !== item.is_pinned` 时才二次渲染。后端排序 + `pinned_at` 写入逻辑有 whitebox 用例覆盖（`TestGetChatHistoryList`/`TestTogglePinChatHistory` 各新增 2 个用例），`main.py` 路由未改动故黑盒测试无需更新；图标持久化与防闪烁是纯前端修复，通过 jsdom（DOM 顺序断言 + `MutationObserver` 计数）手动验证。
- **点击 Recents 条目加载历史快照（2026-07-03，用户报告，纯前端）**：修复前，侧边栏 Recents 列表只有 pin/rename/delete 三个悬浮图标响应点击，条目本身（标题文字、行内空白）点击没有任何效果，用户点开一条历史记录后页面毫无反应。新增 `loadHistorySnapshot(id)`，挂在 `#sidebarRecents` 委托 `click` 监听器里 `[data-action]`/`.history-rename-input` 都未命中时的 fallback 分支：把 `#prompt` 替换为该条目的 `prompt` 字段（不是可能被改过的 `title`），把结果区通过既有的 `displayResults()` 整体替换为该条目 `results` 字段（`/api/compare` 保存时的完整 8-key result 数组快照）对应的渲染结果，`total_providers`/`successful_providers` 两个统计字段现算（Firestore 历史文档从未存这两个字段）。是"完全替换"语义，不是追加/合并——点击第二条会让第一条的结果彻底消失。`results` 为空数组时清空/隐藏结果区而非保留残留内容。游客点击复用同一套逻辑（`currentHistoryItems === window.guestHistory`），零网络请求。`.history-item` 的 `cursor` 同步从 `default` 改为 `pointer`。未改动任何 Python 代码，纯前端修复，通过 jsdom 验证（表单值/结果区内容替换断言、点击图标不触发加载、空快照清空、游客路径）。
- **删除确认改为自定义居中模态框（2026-07-03，用户要求，纯前端）**：`deleteHistoryItem` 原先用浏览器原生 `confirm('Delete this conversation?')` 拦截删除操作——样式无法定制（系统默认的蓝白弹窗）、且只有一句通用文案，不点名具体删除的是哪条记录。新增 `showDeleteConfirmModal(entryTitle)`，惰性创建并复用一个居中的 `.confirm-modal-overlay` + `.confirm-modal`（深色配色，与 `.left-sidebar` 同源），标题固定 `Delete this chat?`，正文 `This will delete ` + `<strong>`（`textContent` 承载条目标题，标题为空时兜底 `this conversation`），底部 Cancel/Delete 两个按钮；返回 `Promise<boolean>`，Cancel、点击遮罩背景、`Escape` 键三条路径均 `resolve(false)` 且不触碰任何数据，只有点击 Delete 才 `resolve(true)`。`deleteHistoryItem` 相应改为 `async function`，在原来 `confirm()` 所在位置改为 `await showDeleteConfirmModal(...)`，为 `false` 时直接 `return`；其后的乐观删除+失败回滚逻辑完全未变。未改动任何 Python 代码，纯前端修复，通过 jsdom 验证（模态框文案与转义、Cancel/背景点击/Escape 三条取消路径均不触发 `DELETE` 请求、确认删除只对目标 `id` 发一次 `DELETE` 请求、空标题兜底文案、游客路径零网络请求）。
- **清空/新建对话联动重置 Model 下拉，删除历史后联动清空界面（2026-07-03，用户要求，纯前端）**：两处独立增强。(1) `clearBtn` 的 click 监听器新增 `modelSelect.value = '';`（在调用 `updateModelDropdown()` 之前），解决"清空 Provider 勾选后 Model 下拉仍保留上次选中的具体模型"的问题——`updateModelDropdown()` 有一段"尽量保留刷新前已选中项"的逻辑，清空勾选后 `selectedProviders` 为空会退化成"全部 Provider 模型并集"，用户之前选的模型大概率仍在这个并集里，若不预先清空 `modelSelect.value`，`updateModelDropdown()` 就会误判为"应该保留"。预先设为 `''` 后，函数内部读到的 `currentSelectedValue` 是空字符串，自然走进重置分支，Model 下拉和它的自定义显示组件（`customTrigger.textContent`）都会回到"使用 Provider 默认模型"。`newChatBtn` 通过 `clearBtn.click()` 复用同一监听器，无需单独处理。(2) `deleteHistoryItem` 在 `showDeleteConfirmModal` 确认通过（`confirmed === true`）之后，新增 `document.getElementById('clearBtn').click()`，等价于用户手动点了一次"Clear results / + New Chat"：隐藏结果区、清空 Prompt、取消 Provider 勾选、重置 Model 下拉（含上一条的行为）。原有的历史记录删除本身（本地乐观 `splice`、`DELETE /api/history/<id>` 请求、失败按原 index 回滚并 `showHistoryErrorToast`）完全未改动，这次界面重置只是在确认删除之后追加的一个独立副作用；取消删除时函数在 `if (!confirmed) return;` 提前返回，不会触发这次重置。两处均未改动任何 Python 代码，纯前端修复，通过 jsdom 加载真实渲染出的页面（Flask `test_client()` 渲染 + `beforeParse` 注入 `fetch` 桩）验证：(1) 选中一个非默认模型后点击 `clearBtn`，断言 `modelSelect.value` 变回 `''` 且 `customTrigger.textContent` 变回默认文案；(2) 弄脏界面状态（结果区可见、Prompt 有内容、Provider 勾选、Model 选中非默认值）后，stub `showDeleteConfirmModal` 直接 `resolve(true)`、stub `fetch` 的 `DELETE` 请求成功，调用 `deleteHistoryItem(id)`，断言界面四项状态全部被重置，且该历史条目仍然从 DOM 中被正确移除（验证删除本身的行为未被这次改动影响）。
- **Model 自绘下拉框黑色高亮延迟到面板收起后再切换（2026-07-03，用户报告，纯前端）**：修复前点击 `.custom-option` 时，黑色高亮（`.selected` class）与面板收起动画（0.15s CSS transition）同时启动，用户能看到高亮在选项间瞬间跳动之后面板才淡出，视觉紊乱。新增 `syncSelectedOptionHighlight()` 把高亮的重新计算从"点击时同步执行"改为通过新增的 `modelHighlightTimer`（`setTimeout(..., MODEL_HIGHLIGHT_DELAY_MS)`，`150`，与 CSS transition 时长对齐）延迟到面板已完全不可见之后才执行；点击时仍会同步更新功能性状态（`customTrigger.textContent`/`modelSelect.value`/触发 `change` 事件/关闭面板），只有视觉高亮被推迟。为处理"选中后 150ms 内又重新打开下拉框"的竞态，`customTrigger` 的 `click` 监听器在检测到本次是"打开"操作时会立即调用一次 `syncSelectedOptionHighlight()` 并清理旧计时器；`updateModelDropdown()` 重建选项列表时也会清理旧计时器（避免其后续触发时操作已过期的分组）。未改动任何 Python 代码，纯前端修复；用真实 headless Chromium（Playwright，沿用第 11 节记录的 `apt-get download` + `dpkg -x` + `LD_LIBRARY_PATH` 免 root 解压系统共享库的方法）驱动一次真实点击验证：点击瞬间高亮仍停留在旧选项、面板已同步开始收起（`open` class 已移除）、延迟窗口过后高亮才切换到新选项；并验证了"选中后立刻重新打开"场景下高亮已经是最新值且不会再跳变。
- **消除历史快照加载后 Clear 导致的整页水平位移 + 导航栏与两栏布局对齐重构（2026-07-03，用户报告，纯前端）**：两处独立的布局 Bug 修复。(1) `html { overflow-y: scroll; }`（紧跟 `:root` 之后）：`html`/`body` 此前未声明任何 `overflow`，点击 Recents 里内容较长的历史记录会撑高文档触发纵向滚动条出现，浏览器为其让出轨道宽度，`.nav-container`/`.container` 这类居中元素可用宽度变窄、整体左移几像素；点击 Clear Results 后内容变矮滚动条消失，两者又整体右移回原位，这一来一回就是用户报告的"点 Clear 后页面整体往右挪了一点"。这条规则让浏览器始终为纵向滚动条预留同一份轨道空间（渲染一个真实滚动条控件，而非仅仅静默预留），与内容是否真的溢出无关，与 `body` 上非标准的 `zoom:var(--page-zoom)` 缩放无关（在真实视口宽度上生效，不需要参与 `--page-zoom` 的 `calc()`）。**这不是当天的第一版方案**：最初用的是更新的 `scrollbar-gutter: stable`，效果上同样能防止水平位移，但它只做静默空间预留、不绘制任何东西，导致内容不需要滚动条时那段预留空间是空白的，紧贴视口右边缘的 `.navbar`（白色通栏）因而被这段空白往左挤出一条视觉裂缝，被用户在验证完前一个修复后紧接着报告为"顶部导航栏右侧白色没有铺满"——当天改回 `overflow-y: scroll` 解决，详见第 9 节"导航栏白色背景右侧出现缺口"条目与第 11 节对应测试说明。(2) `.nav-container` 去掉 `max-width:1200px; margin:0 auto`，改为边到边铺满（与下方 `.app-layout` 同宽），`.nav-left` 新增 `width:260px; flex-shrink:0; justify-content:center`（与 `.left-sidebar` 同宽、同样从 `x=0` 起算），让 logo 精确居中在黑色侧边栏正上方；`.nav-links` 因不再被 1200px 封顶而自然贴近视口右侧。移动端断点内新增 `.nav-left { width:auto; flex-shrink:initial; justify-content:flex-start; }` 覆盖桌面端定宽，因为移动端侧边栏是浮层抽屉、不占据静态两栏布局。两处均未改动任何 Python 代码，也未新增/删除任何 DOM 节点或 class 名；用真实 headless Chromium（Playwright，同一套沙箱免 root 解压方案）验证：注入超长 guest 历史记录后 `document.documentElement.clientWidth` 在"初始→加载长结果（滚动条确实出现，`scrollHeight` 从 900 涨到 9137）→ Clear 后"三个时间点恒定不变；`.left-sidebar`/`.nav-logo` 的 `boundingBox()` 水平中心点坐标 diff 为 0px；`.navbar`/`.guest-banner`/`.app-layout` 三个通栏元素的宽度在 400/1400/1920 三种视口宽度下均等于 `clientWidth`（无缺口）；另截图核对无错位。
- **文生图（text-to-image）Provider 可用性调研（2026-07-03，用户要求，仅调研未落地）**：新增 `availability_g4f/find_image_providers.py`（静态扫描 g4f 库内具备 `image_models`/`default_image_model` 属性的 Provider）+ `availability_g4f/test_image_providers.py`（对 `working=True` 且 `needs_auth=False` 的候选发真实 `Client().images.generate()` 请求验证），模式完全复用既有的 `find_providers_models.py`/`test_providers.py` 两步流程。实测（g4f 7.6.4）确认 5 个免 Key 可用组合：`PollinationsImage`（默认 model，最快）、`BlackForestLabs_Flux1Dev`（`flux-dev`）、`AnyProvider`（`flux`，与 `BlackForestLabs_Flux1Dev` 命中同一后端，不建议重复接入）、`StabilityAI_SD35Large`（`sd-3.5-large`）、`OperaAria`（`aria`，走 `images.generate()` 而非其现有文本集成用的 `ChatCompletion.create()`，两套调用不可混用）。**用户特别要求验证的 Gemini 生图 / Google AI Studio（`https://aistudio.google.com/prompts/new_chat`）路径已实测确认不可免费使用**：`AnyProvider`+`gemini-2.5-flash-image-preview` 报 "Model not found in any provider"（清单里有模型名但无免 Key 后端真正实现）；`GeminiPro`（g4f 对 Google AI Studio 官方 API 的封装，`login_url` 指向 `aistudio.google.com/u/0/apikey`，源码 `models_needs_auth=True`）报 "Error 404 ... not found for API version v1main"（后端在无真实 API Key 时直接拒绝）。三个新增的 txt 产物（`availability_g4f/available_image_providers_models.txt` 扫描结果、仓库根目录 `image_provider_test_results.txt` 完整测试日志、`availability_g4f/available_free_image_providers.txt` 人工汇总结论）均已加入 `.gitignore`，模式与既有的 `available_providers_models.txt`/`provider_test_results*.txt` 一致。本轮**只做调研**：`main.py`、`templates/index.html` 均未新增任何图片生成路由/UI，因此没有新增或修改任何 black/white/grey box 测试用例——两个新脚本与 `find_providers_models.py`/`test_providers.py` 属于同一类不被 `tests/` 覆盖、不被 `main.py`/`auth/` 导入的独立开发辅助工具（已用 `grep` 确认零引用关系），且全量 274 个既有测试用例在改动后依然全部通过。若后续要将调研结论落地为实际功能，参照第 10 节"如何将文生图功能接入项目"小节，且落地时必须补充新的图片结果 DTO 契约与对应测试。
- **Recents 懒加载分页修复"更多今日对话把'昨天'挤出可见范围"的 Bug（2026-07-03，用户报告）**：根因是侧边栏只在页面加载时请求过一次 `/api/history?page=1&limit=20`，从没有任何机制去请求更多页——超过 20 条历史时，更早的记录不是丢失，只是从未被再次请求过。修复为 `.sidebar-recents` 新增 `scroll` 监听器，滚动到距底部 48px 内触发 `loadMoreSidebarHistory()` 递增 `page` 参数继续调用同一个既有的 `/api/history` 端点并 `.concat()` 追加结果，直至某页返回条目数不足 `limit`。同时给列表加了可见的深色细滚动条样式，明示"可以往下滑动"。纯前端改动，未涉及任何后端契约变更；游客的 `window.guestHistory` 本就是完整数组，不参与这套分页。详见第 4 节 `templates/index.html`、第 9 节对应 Bug 条目。
- **上一条修复的分页 JS 逻辑其实从未被触发过：`.left-sidebar` 缺少独立高度是真正根因（2026-07-03 同日用户复测后再次报告，紧接上一条）**：`loadMoreSidebarHistory()`/`scroll` 监听器等 JS 本身没有问题，但 `.left-sidebar` 一直靠 `.app-layout` 的 `align-items:stretch` 被动拉伸匹配 `.main-content` 的高度，只要历史条目够多，`#sidebarRecents` 自身内容高度就会成为这次拉伸计算的最大值，导致它从未真正溢出（`scrollHeight` 恒等于 `clientHeight`），`overflow-y:auto` 不生效、`scroll` 事件永不触发，`loadMoreSidebarHistory()` 因此是不可达代码——分页机制形同虚设，滚动条也自然不会出现。修复为让 `.left-sidebar` 拥有独立于 `.main-content`、绑定视口的固定高度：新增 `position: sticky; top: var(--sidebar-sticky-top, 0px); height: var(--sidebar-sticky-height, calc(100vh / var(--page-zoom))); align-self: flex-start;`，`#sidebarRecents` 新增 `min-height: 0;`；用 `sticky` 而非普通定高是为了保留"结果区异常长时深色背景仍铺满到视口底部、不截断"这条已有不变量。`top`/`height` 由新增的 JS 函数 `syncSidebarStickyMetrics()`（实测 `.navbar`+可选 `.guest-banner` 的真实渲染高度，除以 `--page-zoom` 换算成 CSS 期望的"逻辑"像素后写入两个 CSS 自定义属性，页面加载与 `window.resize` 时都会重算；`window.innerWidth<=520` 时直接跳过，移动端抽屉的 `position:fixed` 覆盖规则不受影响）同步，而非写死某个 `rem`/`vh` 常量，因为顶部占用高度随登录态/`.guest-banner` 是否存在/视口宽度动态变化。`templates/index.html`/`templates/history.html` 的 CSS/JS 是逐行复制关系，两处改动各自独立同步了一份。纯 CSS+JS 修复，未改动任何 Python 代码，未新增/修改任何 black/white/grey box 测试用例（这类问题是渲染后的布局/溢出行为是否正确，jsdom 无法检测，用真实 headless Chromium 手动验证，详见第 9/11 节对应条目）。
- **溢出机制修好了，但滚动条在 Chrome 里还是看不见：Chrome 原生 overlay 滚动条不占布局空间，`::-webkit-scrollbar` 样式压制不了它，改用自定义 JS 滚动指示器（2026-07-03 同日用户第三次报告，紧接上一条）**：用户提供的关键线索——"Chrome 标签页里看不到，VS Code 内置预览里正常"——直接指向 Chrome 的 overlay（悬浮式）滚动条渲染模式：不占布局空间，仅主动滚动/悬停时短暂淡入，静态标签页必然看不到；VS Code webview 默认经典（持久占位）滚动条，因此同一份 CSS 观感不同。实测 `#sidebarRecents.offsetWidth === clientWidth`（差值 0px）证实自定义 `::-webkit-scrollbar` 样式并未能压制 overlay 模式——较新 Chromium 版本收紧了这项非标准样式的覆盖能力，不是配置问题。修复为新增完全自定义、不依赖浏览器原生滚动条渲染的视觉指示器：`#sidebarRecentsScrollThumb`（`#sidebarRecents` 的兄弟节点，避免被 `renderCurrentHistory()` 的 `innerHTML` 替换清空）+ 新增函数 `updateSidebarScrollIndicator()`，按滚动比例实时计算并写入 `top`/`height`，挂在页面加载/`window.resize`/`#sidebarRecents` 的 `scroll` 事件/`renderCurrentHistory()` 末尾四个触发点。**修复过程中真实踩中一次坑**：最初直接把 `getBoundingClientRect()` 的差值赋给 `style.top`/`style.height`，导致指示器位置偏高 ~15px、滚到底部时提前停在距真实底边 ~160px 处——本文件反复出现的 `zoom` 二次缩放陷阱（`getBoundingClientRect()` 已是缩放后的物理像素，`style.top`/`style.height` 是逻辑像素、渲染时还会再乘一次 `zoom`），补上"由 `getBoundingClientRect()` 得来的长度需除以 `--page-zoom`，`clientHeight`/`scrollHeight`/`scrollTop` 三个纯比例来源不需要"这一区分后，误差降到 1px 内。`templates/index.html`/`templates/history.html` 逐字同步了一份。纯 CSS+JS 修复，未改动任何 Python 代码，未新增/修改任何测试用例（同属 jsdom 无法检测的渲染行为问题，用真实 headless Chromium 手动验证：静态不交互状态下指示器已可见、初始位置与滚到底部后的位置均与 `#sidebarRecents` 实际边界对齐在 1px 内）。
- **只读历史详情页 `templates/history.html` + `GET /history/<history_id>`（2026-07-03，用户报告"点击历史条目会加载进可编辑表单、有被误提交的风险"并要求重构）**：新增 `main.py` 的 `view_history()` 路由与 `auth/db.py` 的 `get_chat_history_by_id(user_id, history_id)`（归属校验模式与 delete/update/toggle-pin 一致）。点击 Recents 条目从"就地把快照渲染进 `index.html` 自己可编辑的对比表单"（`loadHistorySnapshot`，已删除）改为整页导航（`openHistoryEntry(id)` → `location.href = '/history/' + id`）到这个物理上不含任何 Provider 勾选/Model 下拉/Compare/Clear 表单元素的独立只读页面。已登录用户的数据在服务端渲染时就已取好（模板内联注入，无需二次请求）；游客没有服务端记录可查，`view_history()` 对游客分支不查询 Firestore，渲染空壳后由客户端 JS 从 `sessionStorage` 里按 URL 中的 `history_id` 查找。三种身份（匿名重定向 / 游客渲染 client-only 壳 / 已登录服务端预取+归属校验）与 `index()` 的身份路由风格一致但独立实现（未复用 `_get_authenticated_user_id()`，因为游客理应能看，不是"一律 401"）。有正式黑盒/白盒用例覆盖（`TestViewHistoryPage`/`TestGetChatHistoryById`），详见第 4 节 `templates/history.html` 专属条目、第 11 节对应测试说明。
- **游客历史改为 `sessionStorage` 持久化，不再是纯内存数组（2026-07-03，因引入整页导航而必需的配套改动）**：点击 Recents 条目从内联渲染改为 `openHistoryEntry` 整页导航后，纯内存的 `window.guestHistory` 会在每次导航时被销毁。新增 `persistGuestHistory()`/`loadGuestHistoryFromStorage()`，把 `window.guestHistory` 持续镜像进 `sessionStorage`（键名 `guestHistory`），在 pin/rename/delete/新提交对话之后的游客分支里调用写入，页面加载时优先复用仍存活的旧数组引用、否则从 `sessionStorage` 反序列化重建。`sessionStorage` 天然"随标签页关闭清空、同标签页跨页面导航存活"的语义与"游客数据不持久化"这条身份不变量吻合，只是把持久化范围从"单页面内存生命周期"扩大到"单标签页会话生命周期"。`index.html` 与 `history.html` 各自独立声明这套相同的辅助函数（项目无共享 JS 模块约定）。**不要**把存储介质换成 `localStorage`——那没有"标签页关闭即清空"的语义，会违反身份不变量。详见第 4 节 `templates/index.html` 条目下的对应说明与第 10 节危险区。
- **UI 比例规范化第一轮（2026-07-03，用户报告，纯前端）**：三处独立调整，均未改动 Python 代码。(1) `templates/history.html` 顶部说明文案由挤在一行的整句改为 `<br>` 分隔的两行（`Read-only view of a saved comparison.` / `Nothing here can be edited or resubmitted.`），`.header p` 补 `line-height:1.5`（`index.html` 同款规则同步补齐，两份 `<style>` 保持一致）；(2) `#promptDisplayContainer`（只读 Prompt 展示区）内唯一的 `.form-group` 新增 `margin-bottom:0` 覆盖——该元素沿用了 `index.html` 里"多个 `.form-group` 依次堆叠"场景设计的 `margin-bottom:26px`，但本页它后面没有兄弟节点，这份下边距只是单纯叠加在容器自身 36px 的底部 `padding` 上，让 Prompt 下方空白明显宽于上方，归零后上下都收敛为 36px；(3) 审计发现 `login.html`/`register.html`/`profile.html` 三个页面标题 `<h2>` 一直用内联 `style="color: #333; margin-bottom: 2rem;"`，脱离了全站统一的 `#09110e` + `font-weight:800` + 负字距排版体系，改为 `auth/base.html` 新增的 `.card-title` 复用类。三处改动都是纯 CSS 数值/文案/class 名调整，不涉及像素级布局算法，因此未使用 Playwright，仅用 `app.test_client()` 渲染核对；294 个既有 Python 用例改动前后全部保持通过，未新增/修改任何 black/white/grey box 测试用例。
- **Recents 侧边栏 + 整页原生滚动条彻底替换为自绘可拖拽指示器（2026-07-03 晚些时候，用户报告"2 个 scrollbar"/"需 110% zoom 才可见"/"150% zoom 滚到底部露出空缺"，并要求整页滚动条同步整改，纯前端）**：三个问题、两个根因。**根因 1**：`#sidebarRecents` 此前仍保留原生滚动条（只是外观被定制，Chrome 判定为默认隐藏的 overlay 模式），用户交互时会与恒定可见的自绘指示器 `#sidebarRecentsScrollThumb` 同时出现（"2 个 scrollbar"），且原生滚动条渲染在浏览器 UI 合成层、天然盖在页面内容之上，只要它还占据任何可交互区域，自绘指示器的 `mousedown` 就永远抢不到，"可拖拽"根本做不到。修复：`#sidebarRecents`/`html` 均改为彻底隐藏原生滚动条（`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`，在"是否溢出"两种状态下恒为 0 宽度），新增通用的 `setupThumbDrag(thumbEl, opts)` 函数（`mousedown`/`mousemove`/`mouseup` + 对应触摸事件，`opts` 抽象"被滚动的对象"）分别赋予 `#sidebarRecentsScrollThumb` 和新增的整页专属 `#pageScrollThumb` 真正的拖拽能力。**根因 2**：`.left-sidebar` 的高度此前由 JS 对 `window.innerHeight`（取整后的整数）做手工减法/除法再注入字面量像素值（旧变量 `--sidebar-sticky-height`），与浏览器自身缩放叠加产生的舍入误差——算多了会让"是否溢出"被误判为"未溢出"（对应"需 110% 才可见"），算少了会让侧边栏实际渲染高度不足、滚到长页面底部时露出缝隙（对应"150% 滚到底部露出空缺"）。修复：`.left-sidebar` 的 `height` 改用纯 CSS `calc(100vh / var(--page-zoom) - var(--sidebar-header-height) + 2px)`，`100vh` 交给浏览器原生解析、不再经过 JS 侧整数取整的二次运算，`syncSidebarStickyMetrics()` 相应简化为只测量并写入 `--sidebar-header-height`（对应 `top`）一个变量；`+2px` 安全余量让残余误差偏向"侧边栏略高"而非"露出缝隙"。整页新增的 `#pageScrollThumb`/`updatePageScrollIndicator()` 挂载在页面加载/`window.resize`/`window.scroll`/`document.body` 的 `ResizeObserver`（覆盖任意改变文档高度的场景，无需逐一在调用点手动插入刷新）四个触发点上。用真实 headless Chromium（Playwright）验证：两处原生滚动条实测占用宽度均为 0；两个自绘指示器无需任何交互即可见；拖拽分别驱动了 `#sidebarRecents.scrollTop`/`window.scrollY` 变化；模拟更高 `device_scale_factor` 后滚动到底部测得 `.left-sidebar` 底边与视口底边差值仅 0.42px；复测"加载长结果再 Clear 不引发水平位移"这条更早的不变量未被破坏。纯 CSS+JS 修复，`templates/index.html`/`templates/history.html` 逐字同步，未改动任何 Python 代码，294 个既有 Python 用例改动前后全部保持通过，未新增/修改任何 black/white/grey box 测试用例（同属"渲染后的滚动/溢出/拖拽行为是否正确"，jsdom 无法检测的范畴）。详见第 4 节 `templates/index.html`、第 9 节、第 11 节 `test_main_blackbox.py` 条目下的对应说明。
- **全局缩放比例 `--page-zoom` 统一从 `0.8` 改为 `1.1`（2026-07-03，用户要求，纯前端）**：`templates/index.html`/`templates/history.html` 各自 `:root` 块内的 `--page-zoom` 声明（驱动 `body { zoom: var(--page-zoom); }`）由 80% 缩小改为 110% 放大，各改一行数值；顺带修正了周围引用具体数值（`0.8`/`80%`/`1/0.8`/`20%`）的 CSS/JS 注释，改写为对任意 zoom 值都成立的通用表述——`zoom<1` 会露出空白、`zoom>1` 会产生多余可滚动空间，`calc(100vh/var(--page-zoom))`（`body`/`.left-sidebar` 高度）与"`getBoundingClientRect()` 结果除以 zoom"（`syncSidebarStickyMetrics()`/`updateSidebarScrollIndicator()`/`updatePageScrollIndicator()`）这两套公式本身不受具体数值影响，这正是当初把它们设计成读取 CSS 变量而非硬编码字面量的原因（详见第 10 节危险区"若以后调整 zoom 的数值，必须只改 `--page-zoom` 这一处"）。全仓库搜索确认无其他文件引用 `--page-zoom`/相关 zoom 数值，也没有任何 Python 测试断言过该 CSS 变量的具体值。用真实 headless Chromium（Playwright，复用第 11 节记录的免 root 沙箱方案）验证：游客身份下 `index.html`/`history.html` 渲染后 `--page-zoom`/`body` computed `zoom` 均为 `1.1`；1400×900 视口下"`.left-sidebar` 深色背景铺满到视口底部、不露出下方浅色背景"这条既有不变量在新缩放比例下依然成立（`.left-sidebar` 底边与视口底边差值分别为 `-2.19px`/`index.html`，即略微超出视口 2px，符合 `+2px` 安全余量设计；`0.36px`/`history.html`）；`history.html` 额外验证了通过 `sessionStorage` 注入的游客历史条目仍正确渲染（`#promptDisplay` 文本匹配），确认改动未影响与 zoom 无关的功能。294 个既有 Python 测试全部保持通过，未新增/修改任何 black/white/grey box 测试用例（纯 CSS 数值/注释调整，无任何现存测试断言过 `--page-zoom` 的具体值，也没有可测的 Python 行为变化）。这一验证同样没有留下可重复运行的自动化测试文件（纯手动 Playwright 脚本）。详见第 4 节 `templates/index.html` 条目下"全局缩放比例统一调整为 110%"。

- **`--page-zoom` 当天晚些时候又改回 `0.8`（2026-07-03，用户要求，纯前端；该值已于同日晚些时候被下一条"从 80% 调整为 88%"条目取代，不再是当前值）**：与上一条同一天内的第二次调整，`0.8 → 1.1 → 0.8`。用户明确要求"确保所有 scrollbar 在 80% zoom、Chrome 里可以看到，避免只能 zoom 到 110% 才出现 scrollbar 再手动缩小"——但这个诉求在此之前就已经被"Recents 侧边栏 + 整页原生滚动条彻底替换为自绘可拖拽指示器"那轮修复（见上方对应 Memory Anchor）解决：两处原生滚动条已彻底隐藏（恒为 0 宽度），改用完全自绘、JS 驱动的 `#sidebarRecentsScrollThumb`/`#pageScrollThumb`，其可见性判定与位置计算读取 `--page-zoom` 动态换算、不含任何硬编码 zoom 数值假设，"需 110% 才能看到指示器"的历史根因（`.left-sidebar` 高度曾经靠 JS 对 `window.innerHeight` 做整数运算）已被纯 CSS `calc(100vh/var(--page-zoom))` 消除，这套机制本身与 `--page-zoom` 具体数值无关。因此本轮只是把变量值改回 `0.8`（`templates/index.html`/`templates/history.html` 各改一行），同步把三处此前写死"当前值"具体数字（而非泛化表述）的注释互换回来：`body` 规则上方 `min-height` 由来注释里的 `zoom<1`/`zoom>1` 举例、`zoom: var(--page-zoom);` 上方"即可实现全屏 X% 缩放"提示、`syncSidebarStickyMetrics()` 内解释 56px navbar 换算的举例（`history.html` 对应处只反向引用 `index.html` 的注释，未硬编码数值，不受影响）。用真实 headless Chromium（Playwright，同一套免 root 沙箱方案）验证：两个页面 `--page-zoom`/`body` computed `zoom` 均读取为 `0.8`；40 条历史记录使 `#sidebarRecents` 产生真实溢出后，`#sidebarRecentsScrollThumb` 在零交互下即可见；两处原生滚动条占用宽度均为 0；拖拽两个自绘指示器确认真实驱动了 `scrollTop`/`scrollY`；`.left-sidebar` 铺满视口底部的不变量在 `index.html`（差值 0.42px）与叠加 `device_scale_factor=2` 的 `history.html`（差值 0.41px）下均成立；水平位移不变量复测通过。改动前后 294 个既有 Python 测试全部保持通过，全仓库 `grep` 确认无任何 `.py`/测试文件引用 `--page-zoom` 具体值，因此未新增/修改任何 black/white/grey box 测试用例。详见第 4 节 `templates/index.html` 条目下"全局缩放比例改回 80%"。
- **`--page-zoom` 再次调整为 `0.88`（2026-07-03 又更晚些时候，用户要求，纯前端；当前最终值）**：同一天内的第三次调整，`0.8 → 1.1 → 0.8 → 0.88`。用户要求把 88% 缩放定为最终比例。原理与上一条"改回 80%"完全一致——这套 zoom 无关的机制（`.left-sidebar` 高度用纯 CSS `calc(100vh / var(--page-zoom) - var(--sidebar-header-height) + 2px)`、两处原生滚动条彻底隐藏后改用读取 `--page-zoom` 动态换算的自绘可拖拽指示器 `#sidebarRecentsScrollThumb`/`#pageScrollThumb`）本身与 `--page-zoom` 的具体数值无关，因此本轮同样只需把变量值从 `0.8` 改为 `0.88`（`templates/index.html`/`templates/history.html` 各改一行），同步把三处写死"当前值"具体数字的注释（`body` 的 `min-height` 由来注释、`zoom: var(--page-zoom);` 上方的"即可实现全屏 X% 缩放"提示、`syncSidebarStickyMetrics()` 内 56px navbar 换算举例）同步更新为 `0.88`/`88%`。用真实 headless Chromium（Playwright，同一套免 root 沙箱方案）验证：两个页面 `--page-zoom`/`body` computed `zoom` 均读取为 `0.88`；40 条历史记录使 `#sidebarRecents` 产生真实溢出（`scrollHeight`=1552 > `clientHeight`=812）后自绘指示器零交互下即可见；两处原生滚动条占用宽度均为 0；拖拽 `#sidebarRecentsScrollThumb` 确认真实驱动了 `scrollTop`（`0→573.86`）；`.left-sidebar` 铺满视口底部的不变量在 `index.html`（差值 0.25px）与 `history.html`（差值 -1.75px，符合 `+2px` 安全余量设计）下均成立；`history.html` 额外验证了 `sessionStorage` 里的游客历史条目仍正确渲染。改动前后 294 个既有 Python 测试全部保持通过，全仓库 `grep` 确认无任何 `.py`/测试文件引用 `--page-zoom` 具体值，因此未新增/修改任何 black/white/grey box 测试用例。详见第 4 节 `templates/index.html` 条目下"全局缩放比例从 80% 调整为 88%"。
- **文生图（text-to-image）聚合功能落地（2026-07-03，用户要求实现新功能，非 Bug 修复）**：把同日早些时候完成的 g4f 免 Key 文生图调研正式落地为产品功能，系统不再只支持文本对话。**后端**（`main.py`）：新增 `IMAGE_PROVIDERS`（复用调研确认的 5 个免 Key 组合）+ `IMAGE_PROVIDER_MODELS_MAP`、`from g4f.client import Client as G4FImageClient` 模块级导入、`determine_actual_image_model()`（规则 A/B，无规则 C 式通用兜底）、`init_image_result_object()`（8-key 图片 DTO：`provider`/`success`/`url`/`b64_json`/`error`/`response_time`/`model`/`type`）、`test_g4f_image_provider()`（走 `G4FImageClient().images.generate()`，与文本对话的 `g4f.ChatCompletion.create()` 完全独立）、`POST /api/generate-images`（与 `compare_providers()` 同构的单阶段并发骨架，无互评，Provider 名字空间严格限定在 `IMAGE_PROVIDERS`）、`GET /api/image-providers`；`index()` 路由新增注入 `image_providers`/`image_provider_models_json`；`/health` 新增 `image_providers` 字段。**前端**（`templates/index.html`）：左侧边栏 "+ New Chat" 正下方新增 "🎨 Generate Image" 按钮（`#generateImageBtn`），点击触发 `switchToImageMode()` 在 `#compareModeContainer`/`#imageModeContainer` 两个互斥容器间切换；图片模式复用文本对比表单同款的 Provider 勾选卡片交互，但**必须用不同的 class 名**（`.image-provider-checkbox`/`.image-provider-trigger`，而非 `.provider-checkbox`/`.provider-trigger`）——因为文本表单的 `providerTriggers`/`providerCards` 是不加容器限定的全局 `querySelectorAll`，复用同名 class 会让两个模式的勾选框互相污染；结果区是固定两列的 `.image-results-grid`，每张图（`.image-result-card`）用 `<img src="result.url || data:image/png;base64,...">` 渲染并附一个 "Download PNG" 按钮。**下载机制刻意不做服务端代理**：`downloadImage()` 全程在浏览器端用 `fetch()`+Blob（或本地 base64 解码）完成，跨域被拦截时降级为新标签页打开——服务端"按客户端传入的任意 URL 发起请求"的代理接口是典型 SSRF 攻击面，本次设计上主动规避。**已知范围限制**：图片结果不写入 `history` 集合（`save_chat_history` 的参数契约是围绕文本 result 设计的），因此不出现在 Recents 侧边栏/`/history/<id>` 里；不支持 Gemini 生图（原因与调研阶段结论一致：无免 Key 路径）。**测试**：`tests/test_main_whitebox.py`/`test_main_blackbox.py`/`test_main_graybox.py` 三个文件共新增 63 个用例（`TestDetermineActualImageModel`/`TestInitImageResultObject`/`TestTestG4fImageProvider`/`TestGetImageProvidersEndpoint`/`TestGenerateImagesEndpoint`/`TestImageSortOrderInvariant`/`TestImageThreadTimeoutFallback`/`TestImageMaxWorkersConstraint`/`TestImageGlobalDegradationState`/`TestImageOuterTimeoutValue`），覆盖模型降级规则、DTO 契约完整性、并发调度、`max_workers`/超时熔断、全局降级、Provider 名字空间隔离，全部通过 `patch('main.G4FImageClient', ...)` 隔离真实网络调用；294 个既有用例全部保持通过，全仓库测试从 294 个增至 357 个。前端交互额外用真实 jsdom（Node.js，非 Playwright——这次不涉及像素级布局，纯 DOM 结构/事件/class 切换断言，jsdom 足以覆盖）跑通了一遍"点击 Generate Image → 切换模式可见性 → 勾选 Provider 卡片 → 渲染结果网格 → 下载按钮存在 → Clear 重置 → New Chat 切回文本模式"的完整交互链路，未留下正式测试文件（与本文件其余多次前端手动验证的处理方式一致），后续如需转成正式回归测试，可复用同一套 jsdom 脚本思路。
